// MV3 service worker. Phase 1 scope: native-messaging connect + handshake,
// built in plan Step 7. This stub keeps the Step 1 scaffold buildable.

export const HOST_NAME = "com.scribe.cliniko_host";
