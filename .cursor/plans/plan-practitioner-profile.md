# Feature Implementation Plan
**Feature:** practitioner-profile
**Overall Progress:** `80%`

## Lifecycle State
- Active

## Completion Status
- Completion timestamp:
- Main implementation complete: No
- Ready for archive: No

## Plan Lineage
- Parent plan: None (sibling of `plan-phase3a-note-pipeline.md`, which keeps the practitioner-owned gates 2.3 / D-S1 / 9.1 / 9.2; this plan records the Task 9.1 run PAUSE there by pointer — Task 0.1 — and never duplicates those tasks)
- Follow-up plans: None yet (Phase 3B — the local `gpt-oss-20b` note model — is scoped in the Phase 3A plan's `Deferred — Actionable Later` and will consume this plan's cue file and learned phrasing; see `Deferred — Actionable Later` below)

## Goal
Make the app adapt to the one practitioner who uses it, without ever keeping anything about a patient. Three pieces, built in this order: (1) **voice enrolment** — a one-time read-aloud at first run produces an encrypted, locally stored voice fingerprint, so every stretch of speech in a consultation is classified "the practitioner" or "someone else" instead of guessed from a two-way clustering plus wording hints, and the clinician role on the Transcript screen is confirmed automatically from it (practitioner-decided 2026-09-05: unconditionally whenever a profile exists, with a visible one-click change); (2) a **per-practitioner cue file** — the phrases that route a spoken sentence into a note section become a fourth clinician config file, bound into the note's config digest, so each practitioner's phrasing works on their own install; (3) **consented phrase learning** — during review the practitioner can add a transcript line to a section (a clinician-owned section takes only their own statements); when learning is on and the line was their own, the phrasing is saved to their cue file automatically when the note is saved (names, numbers, dates and medication names refused; everything learned listed on the Practitioner tab for one-click deletion — D9 as amended 2026-09-16). All local, all encrypted or clinician-authored, all deletable. The Task 9.1 shipping-gate RUN is paused (its rubric v1 stays ratified and frozen) until enrolment ships, so ONE set of mock recordings serves the gate, Task 2.3 and the enrolment measurement.

## Planning Extraction Summary
Populated 2026-09-05 from the confirmed `/explore` scratch `.cursor/plans/explore-practitioner-profile.md` (practitioner profile, 2026-09-05) plus the three practitioner answers and the two follow-up decisions recorded in that session.

**Workflow Schema:** v22

**Executor tier:** entirely premium — planned on Fable 5.1; executor Fable/Opus-class via `/execute-loop`; tier-gap dosing applied (design decisions locked, edge-case inventory, contracts, per-task acceptance criteria, executor-facts block); every task is premium-only in substance (DPAPI custody, a biometric artefact, the windowed plaintext bound in `transcribe_session`, the confirm-first guards, the security docs)

### Agreed Scope (Build Now)
- **Sequencing (practitioner-decided 2026-09-05): enrolment FIRST, then the cue file, then learning.** The Task 9.1 shipping-gate RUN is PAUSED — rubric v1 stays ratified and frozen, nothing is re-ratified — until enrolment ships, so that ONE set of mock recordings serves three purposes: the gate's scoring, Task 2.3's before/after speaker measurement, and the enrolment measurement. Recording protocol for that set (Task 6.1): each mock consultation is recorded through the app (the gate's instrument) AND in parallel by an external recorder to a 16 kHz mono 16-bit WAV with an Audacity role-label track (the harness's input contract), with a second real voice speaking the patient part (round 1 PR-MED-013), plus one separate enrolment WAV of the practitioner reading aloud; the WAVs fall under the Task 2.3 retention decision.
- **Piece 1 — voice enrolment** (Phases 0–3): (a) a local ONNX speaker-embedding model added to `scripts/setup-models.py` (SHA-256-pinned, offline-asserted, `SileroVad`-style load contract, UNC refusal), fetched by the practitioner from a normal terminal, with the existing spectral 2-means path as the VISIBLE fallback when the model or profile is absent; (b) a **Practitioner** tab plus a first-run prompt carrying the consent/disclosure text and checkbox, a ~60 s read-aloud captured through `CaptureBackend` directly into memory (never through `SessionController`; PCM embedded, then dropped — no audio written anywhere), re-enrol and delete; (c) a profile store `%LOCALAPPDATA%\ClinikoScribe\profile\` holding `key.dpapi` (DPAPI-wrapped, the session-key primitives) and `voice.enc` (AES-GCM: the embedding, model id + SHA, created_at, the consent record) — deletion is key deletion; (d) in `transcribe_session`, per-segment model embeddings computed inside the existing windowed loop; with a profile present each segment is scored against the enrolled vector, the practitioner-matched segments get one label and the remaining segments are all `speaker_2` until D-S1 (amended 2026-09-16, Task 2.6 — formerly 2-means-clustered among themselves) (labels: `speaker_1` is the practitioner whenever a profile is applied, the whole remainder `speaker_2` until D-S1 — D13 as amended 2026-09-16, Task 2.6); when NO segment clears the threshold the ordinary 2-means over all segments runs and the cluster with the higher mean similarity is the practitioner (D4 is unconditional); both transcription entry points — live Finish (`build_transcriber`) AND crash recovery (`build_recovery_runner` → `recover_session_transcription`) — apply the profile; without a profile the current path runs unchanged; (e) the Transcript screen confirms the clinician role AUTOMATICALLY from the profile — the best-matching cluster, unconditionally whenever a profile exists (practitioner-ratified 2026-09-05; Design Decision D4) — showing "Clinician: confirmed from your voice profile" with a one-click change back to the manual radios; the manual radios remain the path when no profile exists; (f) `speaker_eval.py` gains an "enrolled" condition and an auto-confirm outcome, documented in `docs/testing/speaker-measurement.md`; (g) threat model (a new surface: the practitioner's own biometric at rest; the auto-confirm relaxation as a practitioner-ratified responsibility boundary), retention row, data-flow-map flow, design-system convention for the first-run surface, `AGENTS.md` Local Run Steps.
- **Piece 2 — the per-practitioner cue file** (Phase 4): a fourth clinician config file `section_cues.json` (`schema_version: 1`, `{section_key: [phrase, ...]}`) shipped as `config_defaults/section_cues.json` carrying today's 98 phrases across the 17 canonical keys; loaded by `load_note_config` with the SAME per-file whole-file replacement (practitioner-confirmed 2026-09-05), first-run default, loud failure and `_TriggerText` validation; a new `NoteConfig.section_cues` field so `config_digest` binds every note to the cue set that routed it; duplicate normalised phrases (within or across sections) refused at validation; `build_note_generator` constructs the provider FROM the config; `DEFAULT_SECTION_CUES` derived from the shipped file (one source); the gate-run cue file `docs/testing/shipping-gate-config/section_cues.json` authored from the practitioner's phrases.
- **Piece 3 — consented phrase learning, with review edits** (Phase 5): an explicit "Edit the note" control group on the Note tab (the transcript panel itself stays a non-interactive text box): (i) **Add a transcript line** — a chooser listing the eligible utterances, a section chooser limited to what that utterance may enter under the existing ownership rules (a clinician-owned section accepts only the confirmed clinician's non-question utterances), and Add, which inserts the whole utterance as a `transcript`-provenance assertion (contiguous coords, its own assertion-id scheme, refused when the utterance is already anywhere in the note); (ii) **Remove line** and **Move line to section…** for any `transcript`-provenance assertion the router produced (practitioner-decided 2026-09-05, Fix now at the hardening gate) — removal only ever subtracts, is reversible until Save, and any resulting omission warning is acknowledgeable, never blocking (D14); a move is remove + add under the same ownership rules; (iii) after an add or a move of one of the practitioner's OWN utterances, when learning opt-in is on, the phrase (the utterance's leading content tokens) is queued and appended to the practitioner's `section_cues.json` automatically when the note is SAVED — refused when any source token is name-like, numeric, date-shaped or medication-shaped; an add undone before Save teaches nothing; a removal teaches nothing (D9 as amended 2026-09-16: auto-learn, review later). The Practitioner tab lists and deletes learned phrases. Automatic pattern mining is out. The gate doc's R3 wording is clarified for a run in which removal exists — the precise population and exclusions are Task 5.5's (round 2 PR-LOW-018) — a clarification of rubric v1, not a threshold change.
- **Cross-cutting**: the consent text (drafted in `Config / Environment / Deployment Impact`, ratified by the practitioner in Task 0.2), the Phase 3A plan + `AGENTS.md` pause pointer (Task 0.1), CHANGELOG, and the measurement run once the recording set exists (Phase 6).

### Deferred — Actionable Later
- Estimated speaker count among the NON-practitioner voices (D-S1's estimated-k)
  - Why deferred: enrolment removes the worst failure (the practitioner merged with someone else); with a profile applied the non-practitioner remainder is ONE label (`speaker_2`, D13 as amended 2026-09-16 — Task 2.6 retired the remainder 2-means) and the no-profile path stays 2-means, until the recording set shows whether more is needed. Already deferred in the Phase 2 and 3A plans; this plan changes the problem, not the deferral.
  - Intended future outcome: k estimated (silhouette or agglomerative threshold) over the non-practitioner segments only.
  - Relevant files / subsystems: `transcription.py` `_cluster_embeddings`, `speaker_eval.py`, the Phase 3A plan's D-S1 task.
  - Dependencies / prerequisites: the Task 6.1 recording set measured under the enrolled condition.
  - Recommended next action: decide D-S1 in the Phase 3A plan with this plan's harness numbers.
  - Risk if deferred: ux-degradation: two non-practitioner voices share one label and the clinician corrects attribution on review.
  - Revisit by: the Phase 6 measurement run
- Phase 3B model-side personalisation (showing the local note model the practitioner's confirmed phrasing at generation time)
  - Why deferred: no model yet; belongs to the Phase 3B plan, which consumes this plan's cue file and learned phrases as its per-practitioner input.
  - Intended future outcome: per-practitioner drafts in the practitioner's own phrasing without training any model.
  - Relevant files / subsystems: the Phase 3B provider behind `NoteModelProvider`; `note_config.py` cue and phrase data.
  - Dependencies / prerequisites: Phase 3B planned; this plan's Phases 4–5 shipped.
  - Recommended next action: name it in the Phase 3B plan's scope when that plan is created.
  - Risk if deferred: minor: extractive routing stays the only personalisation until 3B.
  - Revisit by: Phase 3B planning
- Ship the local ML models with the installer (practitioner-decided 2026-09-16 at the Phase 3 smoke: option 1 — bundle, auto-installed; not a first-run download)
  - Why deferred: today every model (silero, Whisper, the WeSpeaker speaker model) is fetched once by `scripts/setup-models.py`, a dev-machine step; the Phase 3 smoke hit exactly this — the promoted `.onnx` sat in the MSIX-virtualised cache the agent shell sees, invisible to the Explorer-launched app, until the practitioner re-ran the fetch from a normal terminal. A customer install must never depend on that step.
  - Intended future outcome: the installer carries the pinned model files (~25 MiB speaker model; Whisper `small` ≈ 460 MiB — an installer-size decision) and writes them into the real `%LOCALAPPDATA%\ClinikoScribe\models` cache, SHA-256-verified against the same pins at install time; the app itself still never touches the network (the threat model's no-network property is kept — no in-app download path).
  - Relevant files / subsystems: `scripts/setup-models.py` (the pins + verify logic to reuse), `benchmark.default_models_root`, `speaker_embedding.speaker_embedder_available`, the future installer/packaging plan.
  - Dependencies / prerequisites: the commercial packaging plan; a licence check on the WeSpeaker export before redistribution (the "MIT-licensed" claim is unverified — round 8).
  - Recommended next action: name it in the commercialisation/packaging plan's scope when that plan is created; until then `setup-models.py` from a normal terminal is the documented dev step.
  - Risk if deferred: minor: a dev-only setup step; blocks nothing before commercialisation.
  - Revisit by: the packaging plan
- Multi-practitioner profiles on one Windows login
  - Why deferred: the app is single-practitioner per Windows user account today (per-user named mutex, per-user config and sessions roots); a second practitioner on the same login would be classified "someone else", which is visible at the auto-confirm line and correctable with one click.
  - Intended future outcome: a profile chooser at start of session, if commercialisation puts two practitioners on one login.
  - Relevant files / subsystems: `practitioner_profile.py` (new), `ui/practitioner.py` (new).
  - Dependencies / prerequisites: a commercial deployment model.
  - Recommended next action: none until commercialisation.
  - Risk if deferred: minor: not a supported deployment today.
  - Revisit by: commercialisation

### Excluded — Revisit Only If Needed
- Any cloud or network path for learning, embeddings or telemetry
  - Why excluded: PLAN.md — no cloud processing of audio or transcripts; the ONE sanctioned network step stays `scripts/setup-models.py`, run by the practitioner.
  - When to revisit: never for clinical data; a hosted commercial variant would be its own plan.
  - Relevant files / subsystems: `benchmark.assert_offline_env`, `speech.py`, the new `speaker_embedding.py`.
  - Recommended next action (if any): none.
- Training or fine-tuning any model on consultations or on the practitioner's phrases
  - Why excluded (practitioner-confirmed 2026-09-05 after discussion): the training data would be consultations (patients' words) kept and replayed, which the app's retention promise forbids; the local 20B model needs GPU-class hardware and a torch stack the project avoids; a few hundred practitioner sentences barely move such a model while showing them at write time works immediately; and a model trained to sound like the practitioner learns to GUESS like them — the opposite of the ratified "never add what was not said" rule. Learning here is a stored phrase list and one voice vector, both approved by the practitioner.
  - When to revisit: only if Phase 3B's evidence shows generation-time conditioning on approved phrasing is insufficient — then as its own project with its own privacy design.
  - Relevant files / subsystems: Phase 3B provider.
  - Recommended next action (if any): none.
- Retaining enrolment audio or any patient audio/text for learning; enrolling anyone but the practitioner
  - Why excluded: the profile holds a numeric fingerprint, never a recording; patients are never profiled. Structural in Tasks 1.3 and 5.1.
  - When to revisit: never.
  - Relevant files / subsystems: `enrolment.py` (new), `practitioner_profile.py` (new).
  - Recommended next action (if any): none.
- Automatic learning from PATIENT or other-speaker lines, or from lines the practitioner removed ("the app noticed and saved it" from anything but the practitioner's own added/moved lines) — NARROWED 2026-09-16: automatic learning from the practitioner's OWN utterances is now the design (D9 as amended)
  - Why excluded: the practitioner's rule is unchanged for everyone else's words — nothing is learned from a patient, a carer or an interpreter, and the ownership rule that gates the learner is structural (the utterance must be attributed to the confirmed clinician); the practitioner's own phrasing is theirs to keep.
  - When to revisit: never for other speakers; a "suggestions queue" for the practitioner's own lines would be a refinement of D9 as amended.
  - Relevant files / subsystems: `ui/note.py`, `ui/practitioner.py`, `note_config.append_user_cues`.
  - Recommended next action (if any): none.
- Inferring cue DELETIONS from removals ("negative learning")
  - Why excluded: a removed line says the router was wrong once, not that the phrase never belongs there; silent narrowing of the practitioner's own cue file is the unapproved-learning class in another coat. The practitioner deletes phrases explicitly on the Practitioner tab.
  - When to revisit: if Phase 6's R3 numbers show one cue misrouting repeatedly — then as a proposed deletion with approval, never inferred.
  - Relevant files / subsystems: `ui/note.py`, `ui/practitioner.py`.
  - Recommended next action (if any): none.
- Writing "practitioner" into `TranscriptSegment.speaker`
  - Why excluded: three pinned guards and the note checker assume opaque cluster labels plus a SEPARATE confirmed role; the enrolment result travels as a separate document field (Schema / Data Changes) that the UI turns into the confirmed role.
  - When to revisit: never.
  - Relevant files / subsystems: `transcription.py`, `note.py` `speaker_role`, `note_check.py`.
  - Recommended next action (if any): none.

### Accepted Assumptions — Revalidate Later
- An ungated, redistributable ONNX speaker-embedding model of WeSpeaker / ECAPA class runs on onnxruntime CPU under Python 3.12–3.14 with acceptable per-segment latency, and its input front-end (Kaldi-style 80-bin log-mel fbank, 25 ms / 10 ms, per-utterance mean normalisation) can be reproduced in numpy to the accuracy the model needs.
  - Why accepted for now: the Phase 2 plan already names this model class as the in-family upgrade; onnxruntime is already a dependency; the front-end is a bounded numeric recipe. Resolved early by Tasks 0.3–0.5 (practitioner fetch + smoke + decision D-P1).
  - Risk if assumption becomes false: enrolment falls back to the CMN'd spectral embedding, which is loudness-invariant but not microphone/room-invariant; the harness shows it; the plan carries the fallback as a measured, VISIBLE degradation (Design Decision D2), never a silent one.
  - Trigger for revisit: D-P1's smoke shows same-speaker cosine similarity not clearly separated from different-speaker similarity on the practitioner's own recordings.
  - Recommended next action: try the second candidate; if both fail, ship enrolment on spectral features and re-open the model choice as a follow-up.
- The practitioner's voice is stable enough across days, rooms and microphones for cosine matching plus re-enrolment.
  - Why accepted for now: speaker-embedding models are trained for exactly this invariance; re-enrolment is one minute.
  - Risk if assumption becomes false: misattribution reappears; with unconditional auto-confirm (D4) a patient's sentence can be attributed to the practitioner until the practitioner clicks "change" — mitigated by the visible similarity value, the checker, review at signing, and re-enrolment.
  - Trigger for revisit: the Phase 6 measurement shows auto-confirm correctness below the Phase 3A harness's current role accuracy, or the practitioner reports wrong-side attribution in use.
  - Recommended next action: re-enrol on the current microphone; if still wrong, revisit D4's unconditional trigger (a margin-based fallback is drafted in D4's rejected alternatives).
- One practitioner per Windows user account.
  - Why accepted for now: the app is single-instance per user, config and sessions are per user; both clinics are one practitioner.
  - Risk if assumption becomes false: a second practitioner is "someone else" — visible, correctable, deferred above.
  - Trigger for revisit: commercialisation.
  - Recommended next action: none.
- Task 2.3's labelled recordings (still owed) double as the enrolment measurement set and the gate's consultations (Task 6.1's single recording protocol).
  - Why accepted for now: practitioner-decided 2026-09-05 — one recording set, everything built from it.
  - Risk if assumption becomes false: enrolment ships measured only on synthetic tests until the set exists; Phase 6 MEASURES auto-confirm and can trigger a reassessment of D4 (Task 6.2); D4 itself is on whenever a profile exists (round 2 PR-LOW-018).
  - Trigger for revisit: the recording set is not produced before Phase 6.
  - Recommended next action: none — the practitioner owns Task 6.1.
- Practitioner-decided 2026-09-05: storing the practitioner's own voice embedding (encrypted) and approved phrases (plain-text config) locally, with disclosure and consent at first run, is acceptable.
  - Why accepted for now: the practitioner asked for it, with disclosure from start-up.
  - Risk if assumption becomes false: none for patients (nothing of theirs is stored); the practitioner deletes both from the Practitioner tab at any time.
  - Trigger for revisit: the consent text ratification (Task 0.2) changes the terms.
  - Recommended next action: none.

### Key Design Decisions
- Enrolment is evidence that the UI turns into the confirmed role; the transcript keeps opaque cluster labels (D1).
  - Why: three pinned guards and the checker assume labels are opaque and the role is separate; the enrolment result travels as its own document field.
  - Alternatives rejected: a `practitioner` label written by the pipeline.
  - Still applies to follow-up work: Yes
- Practitioner-vs-other first; the rest is one `speaker_2` label until D-S1 (amended 2026-09-16, Task 2.6 — "then cluster the rest" is retired); no profile → today's path unchanged (D2/D3).
  - Why: no behaviour change until the practitioner enrols; the fallback is the shipped pipeline, visibly reported.
  - Alternatives rejected: estimated-k as the first step (D-S1 stays deferred).
  - Still applies to follow-up work: Yes
- Auto-confirm the clinician role UNCONDITIONALLY whenever a profile exists, best-matching cluster wins, one-click change visible (D4 — practitioner-ratified 2026-09-05 after the guarded alternative was offered and declined).
  - Why: the practitioner's product decision; the manual step's protection is replaced by the visible confirmation line, the similarity value, the checker and review at signing.
  - Alternatives rejected: margin-gated auto-confirm with manual fallback (offered; declined); auto-confirm removed entirely (the practitioner's original ask).
  - Still applies to follow-up work: Yes — recorded in the threat model as a responsibility boundary.
- The profile store mirrors session custody, not config (D5).
  - Why: the embedding is biometric; DPAPI-wrapped key + AES-GCM blob; deletion is key deletion; the consent record lives inside the blob.
  - Alternatives rejected: plaintext JSON beside the config files.
  - Still applies to follow-up work: Yes
- Cue file semantics = whole-file replacement, like the other three config files (D6 — practitioner-confirmed 2026-09-05).
  - Why: one precedence rule; the loader's rationale ("merge semantics are where never-partially-applies goes to die") is kept; the shipped defaults become a real file the practitioner or Phase 5 edits.
  - Alternatives rejected: an additive user file merged over defaults.
  - Still applies to follow-up work: Yes
- Cues enter the config digest (D7).
  - Why: every note is bound to the cue set that routed it, exactly as for autofill rules.
  - Alternatives rejected: cues as a constructor argument outside the digest.
  - Still applies to follow-up work: Yes
- Enrolment capture bypasses the session store by construction (D8).
  - Why: a short capture through `CaptureBackend` into memory, embedded, then released; nothing on disk but the encrypted vector.
  - Alternatives rejected: reusing `SessionController.start` (creates a recoverable audio artefact).
  - Still applies to follow-up work: Yes
- Learning is AUTO-LEARN, REVIEW LATER — practitioner utterances only, opt-in-gated, refusal-filtered, written on Save, visible after the fact (D9 — AMENDED 2026-09-16 by practitioner decision at the Phase 4/5 boundary; scoped `/review-plan`; was propose-then-approve).
  - Why: the practitioner's call — the click-per-phrase prompt was friction without safety value for THEIR OWN lines; the eligibility check stays structural (the utterance is attributed to the confirmed clinician — a patient's line can never reach the learner), the stored artefact stays a phrase in the practitioner's own cue file, the name/number/date/medication REFUSAL FILTER becomes the enforcing control (tested as a class), the phrase is written only when the note is SAVED (an add undone before Save teaches nothing), and the Practitioner tab shows everything learned ("recently learned" with dates, one-click delete) so the practitioner reviews after the fact instead of before.
  - Alternatives rejected: propose-then-approve (the ratified v1 design; declined 2026-09-16 as friction); fully silent with no "recently learned" surface (declined: unauditable); mining declined/removed lines or any other speaker's lines (still excluded); a context menu on the transcript panel (the panel is a non-interactive text box).
  - Still applies to follow-up work: Yes — consent text v2 states the new terms; Phase 3B consumes the learned phrases as before.
- Label scheme with a profile applied: `speaker_1` = the practitioner, `speaker_2` = the whole remainder until D-S1 (amended 2026-09-16, Task 2.6); zero matches → ordinary 2-means, highest mean similarity wins (D13).
  - Why: D4 is unconditional, so `enrolled_speaker` must always be set when a profile is applied; a fixed practitioner label keeps the document self-describing; the UI renders any label string (verified).
  - Alternatives rejected: leaving `enrolled_speaker` unset on zero matches (contradicts D4); keeping "first segment is speaker_1" on the profile path (meaningless once labels carry a role).
  - Still applies to follow-up work: Yes
- Review edits — remove and move a routed line — subtract only, reversible until Save, warnings acknowledgeable (D14; practitioner-decided 2026-09-05, Fix now).
  - Why: a wrongly routed line no longer has to wait for Cliniko; removal cannot introduce content, so the confirm-first guarantees are untouched; a move is remove + add under the ownership rules.
  - Alternatives rejected: editing in Cliniko only (the original exclusion); free-text editing of assertions (would break byte-identical reconstruction, Check 1).
  - Still applies to follow-up work: Yes
- Recording before enrolment stays allowed; the first-run surface asks, never blocks (D10).
  - Why: with no profile the pipeline is today's; blocking would stop a practitioner with an incomplete model setup for no safety gain.
  - Alternatives rejected: mandatory enrolment before any recording.
  - Still applies to follow-up work: Yes — the practitioner may harden it later.
- Model candidate verification is a practitioner-run step, then a `[decision]` (D11 — practitioner-confirmed 2026-09-05).
  - Why: agent shells never fetch models (MSIX-virtualised LOCALAPPDATA; the one network step is user-run).
  - Alternatives rejected: the executor fetching from a probe.
  - Still applies to follow-up work: Yes

## Key Findings

### Files / Symbols Involved
Verified 2026-09-05 at `main` `8527ce9` (re-probed at plan-write time; all present and tracked).

**Speaker labelling — what enrolment replaces the guess in**
- `desktop/src/scribe_desktop/transcription.py`: `_segment_embedding(pcm, np, *, sample_rate=16000, cepstral_mean_normalisation=True)` (24 mel-band log powers + low-band centroid over 25 ms Hann windows, CMN per segment); `_kmeans_two`; `_cluster_embeddings(embeddings, np) -> list[str]` (z-score, 2-means, first segment ALWAYS `SPEAKER_1`, degenerate → all `SPEAKER_1`); `label_speakers(...)` whole-list wrapper (tests + harness). `transcribe_session` (`:886`) embeds each VAD segment INLINE per ~30 s window (`:960-990`) to bound plaintext and clusters once at the end; the degenerate policy is duplicated at both sites with a "change together" comment (round 42 LOW-011). `SPEAKER_1/2` constants; `TranscriptSegment.speaker: str`; `TranscriptDocument(schema_version=1, session_id, created_at, model_name, sample_rate, transcript_segments)`.
- Pinned behaviours (`tests/test_transcription.py:455-511, 700, 865`): single/identical → one speaker; two voices alternate; first segment is speaker one; empty PCM degrades; gain shift leaves the embedding unchanged; labels survive batching.
- `desktop/src/scribe_desktop/note.py`: `speaker_role(document) -> SpeakerRolePreselection(preselected_clinician_speaker, margin, speaker_evidence)` (`:976`; question rate 0.60 / first speaker 0.25 / talk share 0.15; `None` for <2 clusters or a tie); the confirmed-role guard on `compose_draft` (`:1783-1795`); `CLINICIAN_OWNED_SECTIONS` (`:152`). Tests in `tests/test_note.py` pin the question-asker preselection, first-speaker tie-break, merged-cluster → none, "the result is not itself a speaker label"; `tests/test_note_check.py` pins "a lying speaker field cannot defeat the role check".
- `desktop/src/scribe_desktop/ui/transcript.py`: `_populate_generation_controls` (`:235`) calls `speaker_role(document)`, one `QRadioButton` per speaker from `models.speaker_quotations(document)` with " (suggested)", none pre-checked; `set_role`, `_selected_role`, `generate()` (`:394`) → `controller.begin_generation()` → `_note_generator_factory(clinician_speaker=role, template_profile_id, prefill_id)` on a `TaskThread`; `_config_loader` seam.
- `desktop/src/scribe_desktop/speaker_eval.py`: `evaluate_recording`, `_transcribe_in_temporary_store`, `cluster_metrics`, `role_outcome` (scores `speaker_role`'s preselection against Audacity role labels), `_score_condition` (two conditions: shipped and CMN-off), `render_report`; text-free results; fail-closed teardown. Input contract in `docs/testing/speaker-measurement.md`.

**Note routing — what the cue file parameterises**
- `note.py` `_RAW_SECTION_CUES` (`:729-841`; 17 keys, 98 phrases), `DEFAULT_SECTION_CUES` (normalised at import via `content_tokens`), `ExtractiveNoteProvider(cues=DEFAULT_SECTION_CUES)` (`:1067`), `_route` (first section in `request.section_keys` order whose cue is a contiguous token run — `_contains_phrase` `:871`), `provider_name = "extractive-v1"`; `note_check.py:140-146` records the lexicons as a FIRST CUT.
- `ui/models.py` `build_note_generator(..., config_root=None, provider_factory=ExtractiveNoteProvider)` (`:751-786`) — the ONLY production construction site: `load_note_config(config_root)` then `compose_draft(document, config, provider_factory(), ...)`; the provider is built WITHOUT the config today.
- `note_config.py`: per-file whole-file replacement, absent → shipped default via `importlib.resources` (`config_defaults/`), existing-but-unreadable/malformed → typed error (`_read_config_blob` `:771`); `NoteConfig(template_profiles, autofill_rules, prefill_templates)` frozen `extra="forbid"`; `to_bytes()` = `model_dump_json()`; `config_digest()` = `sha256-v1` over it; `*File` models with `schema_version: Literal[1]`; `_TriggerText` (≤200 chars, no control/format chars); duplicate-trigger detection under `content_tokens`; `default_config_root()` under LOCALAPPDATA with no UNC refusal (documented decision). `note_fill.py` `_first_phrase_match` shares the matching primitive.

**Custody, models, capture, app shell — what enrolment reuses**
- `session_store.py` `wrap_key_to_file` / `unwrap_key_from_file` (`:542-577`: `win32crypt.CryptProtectData(key, "ClinikoScribe session key", ...)`, `atomic_write_bytes`, `KeyCustodyError`, `key_blob_is_dead`), `delete_session_key`, `default_sessions_root()`; `secure_storage.SessionCrypto` (AES-256-GCM; `export_key`/`from_key`/`encrypt`/`decrypt(aad)`/`destroy`).
- `speech.py` `SileroVad` (`:161-225`): `assert_offline_env` BEFORE `import onnxruntime`; `SessionOptions`; `disable_telemetry_events()`; UNC refused; load failures → `VadModelError` at construction; `default_vad_model_path()`; `segment_probabilities` (VAD); `iter_frames`.
- `scripts/setup-models.py`: `fetch_silero_vad` (URL + `SILERO_VAD_SHA256` pin, `.part` temp, size check), `fetch_whisper` (HF snapshot, pinned revision, `allow_patterns`), `--only` validated against `{"silero-vad", *WHISPER_CANDIDATES}` (`:150`). User-run only.
- `audio_capture.py`: `CaptureBackend` (`list_input_devices`, `open_stream`), `SoundDeviceBackend`, `MockCaptureBackend`, `pcm16_rms_level`; `ui/microphone.py` opens its own monitor stream outside a session (device combo, level meter). `session.py` `SessionController.start(device_id)` always creates a session store — never the enrolment path.
- `ui/models.py` `build_transcriber(model_name)` (`:828`) and `build_recovery_runner(model_name)` (`:855`): BOTH construct `SileroVad()` + `WhisperSpeechProvider` inside the worker call and run `transcribe_session` / `recover_session_transcription` — the two entry points the profile must reach; `format_transcript_text(document)` (`:266`) renders the Note tab's transcript panel as plain text (no per-line widgets; the panel is `NoTextInteraction`); no UI code hard-codes `speaker_1`/`speaker_2`, so any label string renders. `ui/note.py` `_refinalise` (`:408`) rebuilds the note as `finalise_note(draft, self._build_resolutions(), document, config)` — review edits therefore operate on the draft the screen holds (a copy with assertions added or filtered), never on the finalised note. No test pins a literal `config_digest`; fourteen equality pins compare digests between objects (`test_note_config.py` 10, `test_note_fill.py` 2, `test_note_pipeline.py` 2) and keep passing when the domain grows.
- `ui/main_window.py`: tabs Microphone / Session / Recovery / Transcript / Note / Status; `_on_live_transcript`, `_on_draft_ready`, `closeEvent`; `note_screen.clear()` at four sites. `app.py`: mutex → sweep → `MainWindow`; no first-run hook; no `QSettings`. `ui/models.py` `model_report_lines()` (model-readiness lines for the Microphone screen's report panel — the visible-fallback surface).
- `ui/tasks.py` `TaskThread(fn)` — the off-GUI-thread worker pattern; `tests/test_ui_screens.py` `_process_until`, `FakeController`, `FakeBackend`, `_note_document`/`_note_result` fixtures — the offscreen test pattern.

**Docs that change**
- `docs/security/threat-model.md` (trust boundaries `:15-60`; Phase 3A surfaces 1–4 `:173-316`; review triggers `:328`), `docs/security/retention-schedule.md` (table `:12-28`; config row `:27`; pre-committed rules `:81`), `docs/security/data-flow-map.md` (components `:18`; flows 1–11 `:27-165`; explicit non-flows `:165`), `docs/design-system.md`, `docs/testing/speaker-measurement.md`, `AGENTS.md` (Local Run Steps step 3, Subsystem Documentation), `CHANGELOG.md`.

### Codebase Integration Notes
- **Where the model embedding is computed:** inside `transcribe_session`'s windowed loop, at the same point the spectral embedding is computed (`:960-990`), so the one-window plaintext bound is unchanged; the embedder is passed in like the provider (a `SpeakerEmbedder` protocol with a mock for tests), never imported at module import time (lazy ML imports, the `speech.py` convention).
- **Attribution output:** a new optional `TranscriptDocument` field set (Schema / Data Changes) — `enrolled_speaker: str | None`, `enrolment_similarity: float | None`, `speaker_model_id: str | None` — written by the pipeline when a profile was applied; consumed by `ui/transcript.py` to auto-confirm (D4) and by `speaker_eval.py` to score. `speaker_role()` stays pure and unchanged for the no-profile path; with the field set the UI does not call it for the selection (it may still show its evidence).
- **The role guard stays satisfied structurally:** `compose_draft(clinician_speaker=...)` receives the radio group's selection exactly as today; auto-confirm PRE-CHECKS the enrolled speaker's radio and renders the confirmation line — the selection is still a UI state the practitioner can change, and no code path passes `.preselected_clinician_speaker` or `.enrolled_speaker` straight into `compose_draft`.
- **Profile custody reuse:** generalise the two DPAPI helpers with a `description` parameter (or add `profile`-named siblings) rather than duplicating the win32crypt calls; `atomic_write_bytes` and `key_blob_is_dead` are reused as-is; a profile whose `model_sha256` differs from the installed model is treated as ABSENT with a visible "re-enrol" message (Design Decision D3 residue).
- **Cue injection is one site:** `build_note_generator`'s `provider_factory` becomes `Callable[[NoteConfig], NoteModelProvider]`; tests that construct `ExtractiveNoteProvider()` bare keep the shipped defaults (derived from the same file), so the fixture matrix keeps passing.
- **`speaker_eval.py` third condition:** the enrolment vector comes from a separate enrolment WAV (`--enrolment <wav>`, the practitioner's read-aloud) so the measured condition is exactly what the app does; leave-one-out from labelled clinician spans is a documented fallback for a set without an enrolment WAV. The harness contract (text-free results, temporary store, fail-closed teardown) is unchanged; the new condition adds a `RoleOutcome` for the auto-confirmed label.
- **Repo gotchas that bind here** (`docs/lessons.md`): agent shells are MSIX-virtualised — the model download and any `%LOCALAPPDATA%` write for the practitioner's own use are USER-run; never launch the GUI from an ephemeral terminal; codex peers get a basetemp outside the repo and do not run the suite; a claim in a docstring or security doc must state only what the structure enforces (this plan's custody and consent claims especially); a concurrency/custody "class-closed" claim needs an exhaustive consumer enumeration; the `/execute-loop` spawn notes (POSIX paths, benign EXIT:4, bare npm shim, verify=composer).
- **Shared helpers inventory (for the loop's later phases):** `speaker_embedding.SpeakerEmbedder` + `MockSpeakerEmbedder` (Phases 1, 2, 6); `practitioner_profile` load/save/delete + `ProfileAbsent`/`ProfileUnusable` (Phases 1, 2, 3, 5); the DPAPI helper with a description parameter (Phase 1); `note_config.SectionCuesFile` + `NoteConfig.section_cues` (Phases 4, 5); the Practitioner tab (Phases 3, 5).

### External / API Findings
- Not verified online during planning (agent shells never fetch models). Candidate ungated ONNX speaker-embedding models for Task 0.3, in order: (1) WeSpeaker VoxCeleb ResNet34-LM ONNX export (the `wespeaker-voxceleb-resnet34-LM` weights, MIT-licensed, ~26 MB; input Kaldi-style 80-bin fbank, output 256-dim); (2) an ECAPA-TDNN ONNX export (192-dim). The practitioner's fetch (Task 0.4) establishes the exact URL, size and SHA-256; the executor pins them (Task 0.5). Output vectors are compared by cosine similarity after L2 normalisation.

## Planned Workflow Summary

### Flow 1 — First run and enrolment
- The app starts with no profile: the Practitioner tab is selected and a banner explains what will be stored and why. The practitioner reads the consent text, ticks the box (and optionally the learning opt-in), picks the microphone, and records ~60 s of read-aloud; the capture goes straight into memory, VAD keeps the speech, the embedder averages one vector, the vector plus the consent record is encrypted under a fresh DPAPI-wrapped profile key, and the PCM is dropped. Re-enrol replaces the vector; Delete removes the key (cryptographic deletion). Recording is allowed before and after enrolment (D10), never during it (D15).

### Flow 2 — A consultation with a profile present
- Finish → `transcribe_session` embeds each VAD segment with the speaker model inside the windowed loop, scores each against the enrolled vector, labels the matched segments `speaker_1` and the whole remainder `speaker_2` (D13 as amended 2026-09-16, Task 2.6), and writes `enrolled_speaker` + `enrolment_similarity` into the transcript. The Transcript screen shows "Clinician: confirmed from your voice profile (similarity 0.xx) — change", pre-checks that speaker, and Generate becomes available once the template profile is chosen, as today; "change" reverts to the manual radios. The note pipeline is unchanged from there.

### Flow 3 — A consultation without a profile, or with the model missing
- Today's path, unchanged: spectral 2-means, `speaker_role` suggestion, manual confirmation. The Microphone screen's model report (`refresh_model_status`) says the speaker model is absent (or the profile needs re-enrolment) so the degradation is visible, never silent (round 3 PR-LOW-019).

### Flow 4 — Cue routing with the practitioner's file
- `load_note_config` reads `section_cues.json` (user file, else shipped default); `build_note_generator` constructs `ExtractiveNoteProvider` with the config's cues; every note's `config_digest` covers them.

### Flow 5 — Learning a phrase during review
- On the Note tab, in the "Edit the note" controls beside the transcript panel, the practitioner picks a transcript line from the chooser (any speaker's) and a section the ownership rule allows for it → Add puts the line into the working draft as a transcript assertion (re-finalised, checked as usual) — or, for a line the router placed wrongly, Remove line / Move line to section… (subtract only; Undo until Save) → after an add or a move of the practitioner's own line, when learning is on and consent is current, the phrase is queued (or a "not learned: contains a name/number/date/medication" note shows when the refusal filter fires) → Save appends every queued phrase to the user `section_cues.json` (created from the shipped default if absent; an add undone before Save teaches nothing) → the Practitioner tab lists it under "Recently learned", with Delete (D9 as amended 2026-09-16).

### Flow 6 — Measurement
- The practitioner records the single set (Task 6.1); `scripts/measure-speakers.py --enrolment <wav> <dir>` reports the three conditions (before 2.1, shipped, enrolled) and the auto-confirm correctness; the plan records the numbers; D-S1 and the gate resume.

## Design Decisions
- **D1 — Enrolment feeds the confirmed role through a separate transcript field; cluster labels stay opaque** because the pinned guards (`the result is not itself a speaker label`, the lying-speaker-field check, the confirmed-role guard on `compose_draft`) assume exactly that separation. Alternatives considered: a `practitioner` label written into `TranscriptSegment.speaker` (rejected — breaks three guards and couples the pipeline to a UI decision).
- **D2 — Model absent or unusable → the shipped spectral path, reported visibly** in `model_report_lines()` and the Transcript screen's status line ("speaker model not installed — run setup-models" / "profile needs re-enrolment"), the same shape as the whisper `medium` → `small` fallback. Alternatives considered: refusing to transcribe without the model (rejected — blocks the clinic for a non-clinical feature); silent fallback (rejected — the project's standing rule against silent degradation).
- **D3 — Attribution = practitioner-vs-other by cosine similarity against the enrolled vector, then — with at least one match — the WHOLE remainder is `speaker_2` until D-S1 (amended 2026-09-16, Task 2.6; the 2-means over the "other" segments is retired).** Segment scoring uses a fixed threshold constant set by D-P1 from the practitioner's own smoke and re-checked in Phase 6; when NO segment clears it, the ordinary 2-means over all segments runs and the cluster with the higher mean similarity is taken as the practitioner (D4 is unconditional, so `enrolled_speaker` is ALWAYS set when a profile is applied and the document has any transcribed speech); similarities are RAW cosines in [-1, 1] (finite; a zero-norm vector scores -1) and the threshold is compared on that scale — never a 0–1 clamp (round 1 PR-MED-007); the candidate clusters for `enrolled_speaker` are those holding at least one segment with transcribed text (a textless cluster has no radio and cannot be confirmed), and a document with no segments at all carries no attribution fields and leaves generation unavailable exactly as today (PR-MED-008); a profile whose `model_sha256` does not match the installed model's VERIFIED digest is ABSENT (re-enrol). Both transcription entry points apply the profile (`build_transcriber` and `build_recovery_runner`). Alternatives considered: estimated-k first (deferred, D-S1); a threshold learned per session (rejected — unmeasurable without labels).
- **D4 — Auto-confirm the clinician role UNCONDITIONALLY when a profile exists (practitioner-ratified 2026-09-05).** The cluster with the highest mean similarity to the enrolled vector is pre-checked as the clinician; the line "Clinician: confirmed from your voice profile (similarity 0.xx) — change" is always shown; "change" reverts to the manual radios with " (suggested)" from `speaker_role`; auto-confirm satisfies the ROLE predicate only — the template-profile choice, a valid config, no recovery in flight and the generation lease gate Generate exactly as today (round 1 PR-MED-009). Residual risk, stated for the threat model: with a weak best match (the practitioner absent, a very different microphone, a profile from another person on the same login) a patient's utterances can populate clinician-owned sections until the practitioner clicks "change" — the compensating controls are the visible similarity value, the Phase 5 checker (which still blocks unresolved errors), per-assertion review, the practitioner's review at signing, and re-enrolment. Alternatives considered: margin-gated auto-confirm with manual fallback when the best match is weak or two clusters both match (offered 2026-09-05; declined — kept here as the documented hardening if Phase 6 measures a problem); no auto-confirm (the original design; superseded by the practitioner's decision).
- **D5 — Profile custody mirrors session custody**: `%LOCALAPPDATA%\ClinikoScribe\profile\key.dpapi` (DPAPI, description "ClinikoScribe practitioner profile key") + `voice.enc` (AES-256-GCM under that key, AAD `b"clinikoscribe-practitioner-profile-v1"`); deletion = key deletion then blob unlink; re-enrolment replaces ONLY `voice.enc` (one `os.replace`) under the EXISTING key — the key is generated once at first enrolment and lives until Delete — so a failure between steps leaves the previous profile usable and no older generation can be revived (round 1 PR-MED-012); the blob holds the vector, `model_id`, `model_sha256`, `embedding_dim`, `created_at`, `enrolment_speech_seconds`, `device_name`, and the consent record (`accepted_at`, `consent_text_version`, `learning_opt_in`); nothing about it is ever logged (the log tripwire's field scan covers any new record fields by construction). Alternatives considered: plaintext JSON in `config\` (rejected — biometric); Credential Manager (rejected — a vector is not a secret string and the store is size-limited).
- **D6 — `section_cues.json` is a whole-file replacement config file** (practitioner-confirmed): the shipped default carries all 98 phrases; a user file replaces it entirely; a section key missing from a user file means NO cues for that section (stated in the file header comment and the loader docstring); validation refuses unknown keys, blank/control-character phrases, and duplicate normalised phrases within or across sections (routing order would otherwise decide silently). Alternatives considered: additive merge (rejected — a second precedence rule).
- **D7 — Cues are part of `NoteConfig` and therefore of `config_digest`**; `ExtractiveNoteProvider` is constructed from the config at the one production site. Alternatives considered: provider-level constant (rejected — a note would not record which cues routed it).
- **D8 — Enrolment capture is a dedicated in-memory path** (`enrolment.py`) over `CaptureBackend.open_stream` on a `TaskThread`, VAD-gated to require at least 30 s of speech within a 60–90 s window, returning PCM bytes that are embedded and then dropped; no session, no store, no file. Alternatives considered: `SessionController.start` (rejected — recoverable audio artefact); saving the enrolment WAV (rejected — no audio retained).
- **D9 — Learning is AUTO-LEARN, REVIEW LATER, and structurally practitioner-only (AMENDED 2026-09-16 by practitioner decision at the Phase 4/5 boundary; was propose-then-approve — peer round 36 PR-LOW-035 brought this normative entry into line with `### Key Design Decisions`)**: a candidate arises only from an add or a move of an utterance whose speaker equals the confirmed clinician (`note.spoken_by_confirmed_clinician`; the add/move controls themselves are D14), and only while a READABLE profile carries the CURRENT consent version with `learning_opt_in` ticked (re-read at review start, at every add or move, and at Save); the phrase is the utterance's leading 2–4 content tokens, stored normalised (round 2 PR-MED-016); a candidate is REFUSED while any of its SOURCE tokens — the original-case transcript words, checked BEFORE `content_tokens` lowercases them, with `first_in_segment` taken from the first chosen word's REAL segment position (peer round 36 PR-HIGH-008) — is name-like (`transcription.is_name_like_token`), numeric, date-shaped or medication-shaped (`note_config.refuse_learning_candidate`, the enforcing control, tested as a class; the app validates shape only — round 1 PR-HIGH-001 — and the practitioner's after-the-fact review covers meaning); the learner dry-runs the proposed cue set on the source utterance and, when an earlier section's cue still wins first-match routing, NOTES it on the status line — never a silent cue deletion (PR-MED-011); an accepted candidate is queued on the screen and written only when the note is SAVED, as an atomic replace of the user `section_cues.json` (created from the shipped default if absent, preserving D6) plus the dated sidecar; an add undone before Save, a removal, a move's remove leg and a cancelled or abandoned review teach nothing; everything learned is listed on the Practitioner tab (all of it by section, the twenty most recent with their date) with one-click delete. Alternatives considered: propose-then-approve (the v1 design; declined 2026-09-16 as friction); silent mining (excluded); learning from patient lines (excluded).
- **D10 — First run asks, never blocks**; the Practitioner tab is selected at startup when no profile exists and a banner explains; every other screen works as today.
- **D11 — The model candidate is fetched by the practitioner and then chosen by a `[decision]` task (D-P1)** after a smoke that reports same-speaker vs different-speaker cosine similarity on the practitioner's own short recordings; the executor pins URL, size and SHA-256 from the practitioner's report, exactly as silero is pinned.
- **D13 — Label scheme with a profile applied:** `speaker_1` is the practitioner (the matched cluster, or the highest-similarity cluster on zero matches — in both cases among clusters holding at least one segment with transcribed text, PR-MED-008), and — as amended 2026-09-16 (Task 2.6, practitioner-chosen option (a)) — the WHOLE non-practitioner remainder is `speaker_2` until D-S1 estimates the speaker count (the original 2-means split of the remainder into `speaker_2` / `speaker_3` by first appearance is retired: `_cluster_embeddings` is degenerate only for byte-identical features, so it always split one other voice in two; `speaker_3` stays defined for D-S1's return). The no-profile path keeps "first segment is `speaker_1`". Consumers already tolerate N labels: `speaker_role` scores any number of clusters, the radios are one per label, `format_transcript_text` prints the label, the harness's `cluster_metrics` is label-name independent.
- **D14 — Review edits (remove / move a routed line) subtract only.** `NoteScreen` keeps a set of removed assertion ids and a list of manual additions; `_refinalise` builds the working draft as a copy of the generated draft with removed assertions filtered out and additions appended to their sections, then calls `finalise_note` as today, so every check (reconstruction, contradiction, provenance, omission) runs on the edited note; every edit — add, remove, move, undo — goes through the existing content-change helper (`_after_resolution_change`: acknowledgements cleared, note un-saved, re-finalised), so a stale acknowledgement can never survive a changed note (round 1 PR-MED-010); an omission warning raised by a removal is acknowledgeable (review severity), never a block; removals and additions are reversible until Save ("Undo"); after Save they are fixed like proposal decisions. Manual additions use an assertion-id scheme distinct from the provider's (`m<segment>` vs `x<segment>`) and refuse an utterance already present in the note. Alternatives considered: extending `finalise_note`'s signature with removal ids (rejected — the screen-held draft is the one input the pipeline already takes); free-text editing (rejected — Check 1).
- **D15 — Enrolment is a controller-owned activity.** `SessionController.begin_enrolment()` / `end_enrolment()` (the generation-lease SHAPE, but NOT state-agnostic): `begin_enrolment` REFUSES while a session is in an active state (recording / paused / transcribing) or a benchmark worker runs — `SessionActivityError` — and `start`/`resume` refuse while enrolling; `begin_enrolment` stops the microphone screen's monitor stream and the monitor poll (`_ensure_monitor`) is suppressed while the activity is held, resuming on release; the benchmark refuses while enrolling; `closeEvent` refuses while the activity is held; Re-record/Delete are disabled meanwhile; the Practitioner tab holds the activity from capture start through embedding, `save_profile` and its own result handler (success or failure) and releases it there on every path; tests cover BOTH start orders (enrolment then Start; Start then enrolment) and a monitor poll tick during enrolment (round 1 PR-MED-004, completed at round 2). Alternatives considered: a one-time monitor stop (rejected — the poll restarts the stream).
- **D16 — Two embedders behind one protocol.** `OnnxSpeakerEmbedder` (the model) and `SpectralSpeakerEmbedder` (the existing CMN'd mel embedding wrapped; `model_id = "spectral-cmn-v1"`, no file, always available) both satisfy `SpeakerEmbedder`; the profile records which produced it and is usable only with the same `model_id`; D-P1 picks which one SHIPS for enrolment and the other stays for tests and the harness; with the ONNX embedder chosen and its model absent at runtime, D2's visible fallback (no attribution) applies — spectral is never substituted silently (round 1 PR-MED-005). Availability and the factories key on the SELECTED embedder: `SHIPPED_SPEAKER_EMBEDDER` (D-P1's recorded choice, a constant in `speaker_embedding.py`) decides which class `build_transcriber`, `build_recovery_runner` and the Practitioner tab construct; `speaker_embedder_available()` is always true for spectral and true for onnx iff the pinned file is present; the enrolment UI's disabled state applies only when the SELECTED embedder is unavailable (completed at round 2).
- **D12 — Front-end in numpy, model-specific, verified against the model card** (Kaldi-style 80-bin log-mel fbank, 25 ms / 10 ms, Hamming/Povey window, per-utterance mean subtraction, 16 kHz); the D-P1 smoke is a separation / sanity check that can EXPOSE an incompatible export (a poor same-vs-other gap) but does not PROVE front-end compatibility — some mismatches (a uniform sample scale, which CMN cancels; a different window or frame geometry) only degrade the gap, and only a wrong bin count is refused outright — so the export's preprocessing recipe is checked against its model card independently and recorded on Task 0.4 beside the matrix, which is why the smoke AND the model-card check precede the build (round 6 PR-LOW-021).

## Schema / Data Changes
- `TranscriptDocument` (`transcription.py`): three new OPTIONAL fields with `None` defaults — `enrolled_speaker: str | None` (must equal one of the segments' labels when set; validator), `enrolment_similarity: float | None` (raw cosine in [-1, 1], finite — the mean over the matched cluster; PR-MED-007), `speaker_model_id: str | None`. `schema_version` stays `1` (additive, defaulted; old `transcript.enc` artefacts read unchanged; an artefact written with the fields is read by this version only — the recovery path never runs an older build against a newer store on this single-user machine).
- `NoteConfig` (`note_config.py`): new field `section_cues: Mapping[NoteSectionKey, tuple[_TriggerText, ...]]` (built 2026-09-16 as the mapping form, default empty; `normalised_cues()` derives the provider shape) — frozen, part of `to_bytes()`; `config_digest` changes for every config the moment the field exists (expected; the digest identifies content). No persisted artefact embeds a `NoteConfig`; the Task 4.4 inventory confirmed NO test pins a literal digest (Key Findings), so nothing was updated.
- New config file `section_cues.json` (`schema_version: 1`, `section_cues: {<canonical key>: [phrase, ...]}`) shipped as `config_defaults/section_cues.json`.
- New profile store `%LOCALAPPDATA%\ClinikoScribe\profile\` (`key.dpapi`, `voice.enc`) — Design Decision D5.
- New model cache entry `%LOCALAPPDATA%\ClinikoScribe\models\speaker-embedding\<model>.onnx`.

## Config / Environment / Deployment Impact
- No environment variables. No CI changes beyond the model being absent on CI (tests skip-if-absent, the whisper pattern).
- `scripts/setup-models.py` gains `speaker-embedding` in the `--only` set (URL + SHA-256 + size pin), ~26 MB; `AGENTS.md` Local Run Steps step 3 gains the size and the note that enrolment needs it. User-run only.
- **Consent text v1 — RATIFIED 2026-09-05 as written (Task 0.2); shown on the Practitioner tab and at first run; the version string is stored in the profile's consent record:**
  > "This app can learn your voice and your phrasing to improve your notes. If you agree, it stores on this computer: a numeric fingerprint of your voice (never a recording), encrypted; and, if you also turn on phrase learning, the short phrases you approve during review, kept as plain text in your own config file until you delete them. The app cannot tell whether a phrase names a patient — only you can — so it shows you every phrase before saving it, refuses names and numbers, and asks you to confirm it contains no patient information; keep phrases general. Nothing else about any patient is stored beyond their session, and nothing leaves this computer. You can re-record your voice, delete it, or delete any learned phrase at any time from this tab. Version consent-v1."
  (Round 1 PR-MED-002 / PR-HIGH-001: the voice profile is encrypted; approved phrases are PLAIN TEXT in `section_cues.json` with the other clinician config, retained until deleted; the no-patient-information judgement is the practitioner's — the loader validates shape only, `note_config.py:17-21`.)
  A second checkbox, off by default: "Also learn my phrasing from lines I add during review (asks each time)". Ratified text replaces the draft verbatim in `ui/models.py` `CONSENT_TEXT_V1`; changing the text later means a new version and re-consent at next start.
- **Consent text v2 — RATIFIED 2026-09-16 by the practitioner (scoped `/review-plan` at the Phase 4/5 boundary, after the auto-learn decision); ships as `ui/models.py` `CONSENT_TEXT_V2` with `CONSENT_TEXT_VERSION = "consent-v2"` (Task 5.0); v1 stays in the file as history for records that carry it:**
  > "This app can learn your voice and your phrasing to improve your notes. If you agree, it stores on this computer: a numeric fingerprint of your voice (never a recording), encrypted; and, if you also turn on phrase learning, short phrases taken from lines *you* add or move while reviewing a note, saved automatically when you save the note and kept as plain text in your own config file until you delete them. Only your own lines are ever used — never a patient's. The app cannot tell whether a phrase names a patient, so it refuses phrases containing names, numbers, dates or medication names, and shows you everything it has learned on this tab so you can delete any of it. Nothing else about any patient is stored beyond their session, and nothing leaves this computer. You can re-record your voice, delete it, or delete any learned phrase at any time from this tab. Version consent-v2."
  The second checkbox's label becomes "Also learn my phrasing from lines I add or move during review (saved when I save the note)". Re-consent: a profile whose consent record carries `consent-v1` is READABLE but NOT CURRENT — the tab leaves the consent box unticked and editable, Record needs a fresh tick, and learning stays OFF until the practitioner re-enrols or re-consents (Task 5.0's re-consent without re-record: re-save the same vector under the existing key with a v2 consent record).
- Release risk: a practitioner who never enrols sees no change; a practitioner who enrols on one microphone and records on another may see wrong auto-confirms until re-enrolment (D4 residual, visible).

## Critical Constraints
- **No cloud, no network at runtime** (PLAN.md): the offline kill-switches are asserted BEFORE `onnxruntime` is imported in the new embedder; the model is fetched only by `scripts/setup-models.py`, run by the practitioner.
- **No audio is ever persisted for enrolment**; the profile holds a numeric vector only; PCM buffers are dropped when embedding completes (pinned by test with a mock backend).
- **The profile is encrypted at rest under a DPAPI-wrapped key; deletion is key deletion**; no log record, status line, exception message or report ever carries the vector or the consent record's contents (the log tripwire scans every formatter field by construction; add the profile fields to its adversarial fixtures).
- **The windowed plaintext bound in `transcribe_session` is unchanged**: the speaker embedding is computed per segment inside the same window the spectral one is; no whole-session PCM is held.
- **Cluster labels stay opaque and the confirmed role stays a UI selection** (D1); no code path passes a pipeline field straight into `compose_draft`'s `clinician_speaker`.
- **Auto-confirm as ratified (D4)**: always shown, always changeable with one click, never hidden behind a setting the practitioner did not choose.
- **Cue file: whole-file replacement, fail-closed loader, digest-bound** (D6, D7).
- **Learning: practitioner utterances only, opt-in-gated, refusal-filtered, written on Save, visible after the fact** (D9 as amended 2026-09-16): a phrase is learned only from an utterance attributed to the confirmed clinician that the practitioner added or moved during review; a candidate whose SOURCE tokens (original case, before normalisation) contain a name-like, numeric, date-shaped or medication-shaped token is REFUSED (the enforcing control — tested as a class with an adversarial fixture); the phrase is appended to the user cue file only when the note is Saved; every learned phrase is listed on the Practitioner tab by section (the twenty most recent with their date) with a one-click delete; consent text v2 says the app refuses those token classes and that the practitioner reviews what was learned (round 1 PR-HIGH-001 stands: the app cannot classify meaning — the filter plus after-the-fact review are the controls).
- **Review edits subtract or re-route WHOLE transcript lines only** (D14): any speaker's line may be added to, moved to or removed from a section the ownership rule admits — a clinician-owned section takes only the confirmed clinician's non-question lines, the router's own rule in one implementation (`note.admissible_sections`); no free-text editing, no new content; reversible until Save. LEARNING, unlike editing, is clinician-only (the Learning constraint above; peer round 36 PR-LOW-038 separated the two).
- **Docstrings and security docs claim only what the structure enforces**, naming residues (the project's recurring review class).
- **Compatibility**: old `transcript.enc` artefacts read unchanged; a config directory without `section_cues.json` loads the shipped default; a machine without the speaker model behaves exactly as today.

## Validation / Verification
Baselines pinned at planning (read-only dry-run, 2026-09-05, `main` `8527ce9`):
- Desktop suite: `pytest --collect-only -q` → **1368 collected**; full run 1368 passed (76 s on this host); `ruff check .` clean; `mypy` clean over **30** source files.
- `note.py` `_RAW_SECTION_CUES`: **17** sections, **98** phrases (the shipped `section_cues.json` must carry exactly these until the practitioner edits a copy).
- `config_defaults/`: 3 files → 4 after Phase 4.
- `scripts/setup-models.py --only` valid set: `{"silero-vad", *WHISPER_CANDIDATES}` → plus `speaker-embedding` after Task 0.3.
- `note_screen.clear()` call sites in `main_window.py`: 4; `speaker_role(` call sites outside its definition: 3 (`speaker_eval.py`, `ui/transcript.py`, `note.py` guard text); `provider_factory` mentions in `ui/models.py`: 2.
- CI: three jobs green on `main` (last run 33934875082-series; extension on Node 24).

Per-phase checks:
- **Phase 0**: the Phase 3A plan and `AGENTS.md` carry the pause pointer; the consent text is ratified (practitioner); `setup-models.py --only speaker-embedding` fetches the candidate for the practitioner and prints size + SHA-256; the smoke script prints same-speaker vs different-speaker cosine on two short practitioner recordings and one other voice; D-P1 recorded.
- **Phase 1**: unit tests — embedder load contract (absent → `SpeakerModelError`, UNC refused, offline asserted before import), front-end shape (80 × frames) and determinism, profile round-trip, key-first deletion, unreadable/truncated blob → typed error, model-SHA mismatch → absent, no PCM persisted (mock backend; the enrolment function returns and the only artefacts on disk are `key.dpapi` + `voice.enc`), log-tripwire fixtures extended.
- **Phase 2**: attribution unit tests with synthetic vectors (matched cluster + whole remainder `speaker_2` (Task 2.6); all-matched; NONE-matched → ordinary 2-means with the higher-similarity cluster as `speaker_1`; single segment; two labels rendered with a profile applied); BOTH entry points (`build_transcriber` and `build_recovery_runner`) pass the embedder + profile (pinned with fakes); pipeline tests with `MockSpeakerEmbedder` injected (document fields set; the plaintext bound test still passes; degenerate policy unchanged); transcript round-trip with the new fields; the `enrolled_speaker` validator refuses a label absent from the segments; offscreen UI tests — auto-confirm pre-check + confirmation line, "change" reverts, Generate still gated on the template profile (enrolment alone leaves it disabled — PR-MED-009), manual path when the field is `None`, the lying-field defence; harness tests for the third condition.
- **Phase 3**: offscreen UI tests for the Practitioner tab (consent gate, enrol with a mock backend, status, re-enrol, delete), first-run tab selection; docs re-read against the code (the round-40/41 class).
- **Phase 4**: loader properties for the fourth file (absent → default; malformed → loud; unknown key, blank, duplicate refused; digest changes with the file and with a one-phrase edit); provider routes by config cues (a user file that removes an advice cue stops that routing); `DEFAULT_SECTION_CUES` equals the shipped file; the gate-run cue file loads.
- **Phase 5**: the utterance chooser lists only eligible utterances and the section chooser only allowed sections (clinician-owned → the confirmed clinician's non-questions); the added assertion reconstructs byte-identically (Check 1) and carries the manual id scheme; an utterance already in the note is refused; Remove filters the assertion out of the working draft and any omission warning is acknowledgeable, never blocking; Undo restores; Move = remove + add under the ownership rules; edits are frozen after Save; the learn prompt fires after an add or a move of a practitioner utterance only and shows the exact line; approve writes atomically and the loader reads it back; decline writes nothing; opt-in off → no prompt; Practitioner tab lists and deletes.
- **Phase 6 (practitioner)**: the recording set; `measure-speakers.py --enrolment` numbers recorded in this plan and the Phase 3A plan (Task 2.3); the gate run resumes per rubric v1 with the R3 clarification of Task 5.5 (removed lines count in the numerator).
- **Gates**: `ruff check . && mypy && pytest` in `desktop/` green at every phase boundary; CI three jobs green; `/review-loop` + cross-family codex `/peer-loop` at each phase boundary (the loop's standing cadence); the Hardening stage before Phase 6.

## Deferred / Out of Scope
See `Planning Extraction Summary` → `Deferred — Actionable Later`, `Excluded — Revisit Only If Needed`, and `Accepted Assumptions — Revalidate Later` (state-once; nothing is restated here). The Task 9.1 shipping-gate RUN, Task 2.3 and D-S1 remain in `plan-phase3a-note-pipeline.md`; this plan only pauses the run (Task 0.1) and feeds it the single recording set (Task 6.1).

## Current State / Handoff Note
- **PHASE 5 ARCHITECT, RE-SMOKE LEG (2026-09-17 12:55, run=stage-5, leg stage-5-arch-f8e-resmoke, Claude Code `claude-fable-5-1`; no code, test or doc changed; no delegation) — since leg f8d: the composer's suites GREEN (ruff clean, mypy 34 files, pytest 1871), codex round 41 (`gpt-6-astra`, high) over the f8c/f8d hunks returned 0 findings (pass stage-5.p2 CONVERGED), and the practitioner's RE-SMOKE (12:40–12:44, the Explorer-launched app on the real profile and config roots) PASSED on all six steps — screenshots: consent v2 ticked with the opt-in, Confirm consent greyed (nothing to confirm); after one add + one move the learning line read "2 phrases queued - press Save note on this tab …"; after Save note "Learned 2: 'do a bit of' for Presenting complaint; 'alright so you've come' for Presenting complaint. Review learned phrases on the Practitioner tab."; the Practitioner tab then listed both under Recently learned (2026-09-17) and under All learned phrases. The practitioner's ONE new item, verbatim: "It worked this time. I realise that it didnt work when i didn't add any extra lines. So it doesn't save if the line doesn't get added from the drop down box and it's automatically added." DEFERRAL CONFIRMATION GATE (user-smoke variant), one item: `Item 1 of 1: lines the generator placed automatically are not learned — only lines added or moved by hand are (Tasks 5.2/5.3 re-smoke, 2026-09-17). Recommended: No defect (by design) — D9 as amended learns only from the practitioner's own ADD or MOVE; an automatically placed line was routed by a cue the practitioner already has, so learning its opening words would add a redundant or competing cue (the dry-run note exists for exactly that case); the rule is already stated at three practitioner-facing sites — the Note tab's learning line ("lines you add or move are learned when you press Save note on this tab"), the opt-in label ("lines I add or move during review") and consent text v2 ("short phrases taken from lines you add or move while reviewing a note"). App impact if not changed: none — the practitioner now describes the design correctly; a note with no hand edit learns nothing and its learning line says so. Production-impact flag: no.` No task appended; recorded on Task 5.2. PHASE 5 STATE: Tasks 5.0–5.4 🟩 and smoke-passed; Task 5.5 🟨 — STILL AWAITING the practitioner's R3 initials on `docs/testing/shipping-gate.md` (not given at the re-smoke); Task 5.6 🟥 `[pending-hardening]` (the multi-surface exit message; the composer's scoped `/review-plan` before build — a candidate for the hardening stage). STANDING PRACTITIONER ITEMS (open, not blockers): (a) the R3 initials; (b) the path-B decision — the name check refuses any line whose first capitalised word is outside the transcript's common-opener set ("Your home exercise …", "Keep …", "Ice …" never teach; "The …", "So …", "Alright …" pass — the re-smoke's two phrases both opened with common openers): keep it (i), a learner-local exemption list of clinical/imperative openers at the real first word (ii, the architect's recommendation, scoped `/review-plan` before build), or drop the opener and learn from the next words (iii); (c) the shared recording set (Task 6.1) and the Task 2.3 retention decision. Ready for the composer: `smoke-pass` for Phase 5, the phase-close history gate (41 + 41 at this leg's check), the notice flush, `/document` (AGENTS.md Current Status: Phase 5 closed with consent v2, review edits, auto-learn on Save, the two-file learner and its residues; Last Session; the lessons candidate below) and the Phase 5 commit — then the hardening stage H1–H4 (Task 5.6's hardening rides H1). Lessons candidate for `docs/lessons.md` (the composer's call at `/document`): a queued-outcome message must name the control that commits it — "when you save the note" read as "done", and every other review exit drops the queue by design; the first smoke of any feature whose outcome is deferred to a later click should include that click in the script. Files changed this leg: this plan only. No commit made by the architect. Last plan sync: 2026-09-17 12:55 (this bullet).**
- **PHASE 5 ARCHITECT, SUITES + CONFIRMATION LEG (2026-09-17 06:40, run=stage-5, leg stage-5-arch-f8d-suites, Claude Code `claude-fable-5-1`; no delegation) — the composer's suites over leg f8c: ruff clean, mypy 34 files, full suite 1 failed / 1870 passed — the one failure was `test_the_learning_line_names_save_note_while_phrases_are_queued`'s tooltip assertion (`"Save note" in toolTip()`), which pinned a phrase the tooltip never carried; the tooltip text ("… Phrases queued for learning are written here and nowhere else.") is the intended design — it sits on the button labelled "Save note" and describes what that button does with the queue — so the fix is TEST-SIDE ONLY: the assertion now pins `"queued for learning are written here"`. No production code changed this leg. `/review-loop` ROUND 40 (2 of cap 3, confirmation): 0 CRIT / 0 HIGH / 0 MED / 0 LOW — the round-39 hunks re-read, no affected caller, the missed-issue pass found nothing; LOOP CONVERGED at round 2 of 3; Review History integrity: OK (40 + 40). NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_ui_screens.py tests/test_ui_models.py`, then the full suite in `desktop/` — expected: ruff clean, mypy 34 files, pytest 1871 passed (the one failure now passes; nothing added or removed). On green: `phase-complete` for the codex confirmation round (pass stage-5.p2) over the f8c delta (`ui/models.py` learning constants + helper, `ui/note.py` `_render_learning_line` / `_consider_learning` / `_write_learned_phrases` / the Save tooltip, `test_ui_screens.py`, `docs/design-system.md`, Task 5.6) — lenses: that no gate or write path moved (messaging only), the no-draft render branch, the early return before the Save-time read; then the practitioner's RE-SMOKE with the six steps in the previous bullet (unchanged) and the standing path-B decision (unchanged). Files changed this leg: `desktop/tests/test_ui_screens.py` (one assertion), this plan. No commit made by the architect. Last plan sync: 2026-09-17 06:40 (this bullet).**
- **PHASE 5 ARCHITECT, SMOKE-FEEDBACK FIX LEG (2026-09-17 06:05, run=stage-5, leg stage-5-arch-f8c-answers, Claude Code `claude-fable-5-1`; no delegation — below the bar; the staging file stays empty) — the practitioner's answers (verbatim: 1 "Yes"; 2 "I can't remember, but it had the phrases there and then told me to go to another screen and press save"; 3 "I don't remember but I ran 2 consults and had a few"; 4 "I'm not at the PC right now. But it had no learned phrases yet.") settle the assessment: consent was confirmed with the opt-in (path A out), the Learned phrases box was visible (path D out), and answer 2 matches the queued-phrase status ("Will learn '…' … when you save the note") being read as the OUTCOME, after which the review was left by an exit that writes nothing — Cancel review and regenerate, Delete note and complete without one, or Complete from the Transcript screen (blocked until Save or Delete) — because the tab never said that Save note ON THIS TAB is the one control that learns (a mock session in the gate procedure ends in Cancel + Discard, exactly the exit that teaches nothing). Path B (a capitalised opener refused as a name) is NOT evidenced (the phrases were queued) and stays a standing decision for the practitioner (below). DEFERRAL CONFIRMATION GATE (user-smoke variant), one item: `Item 1 of 1: learned phrases not showing on the Practitioner tab after Save (Tasks 5.2/5.3 live smoke, 2026-09-17). Recommended: Fix now (two single-site messaging gaps) + Include in plan (one multi-surface gap → Task 5.6). App impact if not fixed now: the feature's outcome is invisible unless the practitioner already knows Save note is the step. Production-impact flag: no.` APPLIED (Fix now): `ui/models.py` — `LEARNING_ON_LINE` now reads "… learned when you press Save note on this tab", NEW `learning_queued_line(count)` ("Phrase learning is on: N phrase(s) queued - press Save note on this tab to learn them (Cancel, Delete and Complete learn nothing).") and `LEARNING_NOT_ATTRIBUTED_NOTE`; `ui/note.py` — `_render_learning_line` (the learning line follows the queue: the off-reason / the queued count naming Save note / the on-line; blank on a cleared tab; rendered from `_refresh_learning_status` and `_update_controls`), the add/move status says "when you press Save note on this tab", a line not attributed to the practitioner now SAYS "Not learned: this line is not attributed to you - only your own lines are learned." instead of silence, Save with edits that queued nothing reports "Nothing learned from this note: …" (the off-reason, or "no added or moved line was one of yours that passed the checks above"), the Save button tooltip says queued phrases are written there and nowhere else; `docs/design-system.md` the status cue; tests — one retarget (the label after an add is the queued line), the not-attributed pin, `test_the_learning_line_names_save_note_while_phrases_are_queued`, `test_save_after_edits_that_queued_nothing_says_why` (three branches). INCLUDED IN PLAN: Task 5.6 `[pending-hardening]` — the Transcript screen's status after Cancel / Delete-and-complete says "N queued phrases were not learned", and the `closeEvent` refusal mentions queued phrases (multi-surface; the composer's scoped `/review-plan` before build). `/review-loop` ROUND 39 (1 of cap 3) DONE over this leg's diff: 0 CRIT / 0 HIGH / 0 MED / 2 LOW (a stale learning line on a cleared tab; a profile read on every Save), both applied; Review History integrity: OK (39 + 39). NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_ui_screens.py tests/test_ui_models.py`, then the full suite in `desktop/` — baseline ruff clean / mypy 34 files / pytest 1869; expected: ruff clean, mypy 34 files, pytest 1871 passed (+2 in `TestNoteScreenEdits`; the retarget and the added assert change no count). Risks the composer should watch: (1) `test_save_after_edits_that_queued_nothing_says_why`'s third branch sets the edit status to "" before Save and expects it unchanged — the early return before the status read is what makes that hold; (2) the retargeted `test_the_learning_status_is_re_read_at_each_add` now expects `learning_queued_line(1)` after the add. On green: round 40 (the confirmation round with the regression check over the round-39 hunks), then `phase-complete` for the codex confirmation (pass stage-5.p2), then the practitioner's RE-SMOKE — EXACT STEPS for the practitioner, in order: (1) Practitioner tab: confirm the consent box is ticked and "Also learn my phrasing from lines I add or move during review (saved when I save the note)" is ticked; if either is not, tick both and press "Confirm consent" — expect "Consent saved - your voice profile is unchanged."; the "Learned phrases" box at the bottom reads "No learned phrases yet." (2) Record a short mock consultation (you speaking the clinician part), Finish, then on the Transcript screen check the line "Clinician: confirmed from your voice profile", choose the template, Generate. (3) On the Note tab, under "Edit the note": pick one of YOUR lines in the "Line:" chooser (the label starts with the speaker the profile confirmed as you), pick a section, press "Add line to section" — READ the two lines under the chooser: the status should say "Added to … Will learn '<a few words>' for <section> when you press Save note on this tab." and the line above it "Phrase learning is on: 1 phrase queued - press Save note on this tab …". If instead it says "Not learned: …", tell us the exact words. (4) Confirm or decline every proposed line and acknowledge every review warning, then press "Save note" ON THE NOTE TAB — the status should now start "Learned 1: '…' for …. Review learned phrases on the Practitioner tab." (5) Go to the Practitioner tab: "Recently learned" should list the phrase with today's date and its section, and "All learned phrases" the same phrase; press "Delete selected" once to see it go. (6) Then Complete on the Transcript screen. Report the exact status wording from steps 3 and 4 if anything differs. STANDING DECISION FOR THE PRACTITIONER (path B, not a blocker): the name check refuses any line whose first word is capitalised and not a common opener ("Your home exercise …", "Keep …", "Try …", "Ice …" are refused as name-like; "The …", "So …", "I …", "We'll …" pass) — options: (i) keep it (safe, narrow; many instruction lines never teach), (ii) a learner-local list of common clinical/imperative openers exempted at the real first word only (an exemption admits listed forms only, so a new form still fails toward refusal), (iii) drop the opener and learn from the next words — the architect recommends (ii), hardened by a scoped `/review-plan` before build. Files changed this leg: `desktop/src/scribe_desktop/ui/models.py`, `ui/note.py`, `desktop/tests/test_ui_screens.py`, `docs/design-system.md`, this plan. No commit made by the architect. Last plan sync: 2026-09-17 06:05 (this bullet).**
- **PHASE 5 ARCHITECT, SMOKE-FEEDBACK LEG (2026-09-17 05:45, run=stage-5, leg stage-5-arch-f8b-smoke-feedback, Claude Code `claude-fable-5-1`; no code, test or doc changed; no delegation — the staging file stays empty) — the practitioner's live smoke returned ONE item, verbatim: "Learned phrases are not showing up after saving in the practioner profile." ASSESSMENT (from the code paths; the agent shell cannot see the real `%LOCALAPPDATA%`): the write and re-read path is sound as built — `append_user_cues` writes the user `section_cues.json` + `section_cues.learned.json` under `default_config_root()` on Save and `learned_phrases_changed` → `refresh_learned_phrases` re-reads the same root (pinned end to end in `TestNoteWiring` and `TestNoteScreenEdits`) — so the symptom points at one of FOUR paths, each visible on screen and each distinguishable only by what the practitioner saw: (A) learning was OFF — the real profile carried `consent-v1`, so learning stays off until "Confirm consent" with the opt-in ticked on the Practitioner tab; the Note tab's learning line then reads "Phrase learning is off until you confirm the updated consent text on the Practitioner tab." (or "… turn it on on the Practitioner tab.") and every add/move appends that hint; (B) the refusal filter refused the line as a NAME because its first word was a capitalised opener outside the transcript's common-starter set ("Your home exercise …", "Keep …", "On examination …" — Whisper capitalises every segment's first word; the build leg's watch item 1) — the status line reads "Not learned: contains a name/number/date/medication (name)."; (C) the line added or moved was NOT attributed to the practitioner (a patient line, or a mis-attributed one) — the learner ignores it SILENTLY by design (the status says only "Added to …"), which is a UX gap in its own right; (D) the "Learned phrases" box sits at the bottom of a tab that has no scroll area — on a short window the two lists may be squeezed below the fold. DEFERRAL CONFIRMATION GATE (user-smoke variant, one item): `Item 1 of 1: learned phrases not showing on the Practitioner tab after Save (Task 5.2/5.3 live smoke, 2026-09-17, verbatim ask above). Recommended: Fix now — AFTER clarification (must-pause): the cause is one of A–D and only the practitioner's observation separates them; a guess-fix would risk the enforcing control. App impact if not fixed now: the feature's headline outcome is invisible; no data risk (nothing wrong is written on any path). Production-impact flag: no.` Pre-decided routes once the answers arrive — (A) no defect; Fix now a Save-time line "Nothing learned from this note: phrase learning is off — <hint>" so the outcome is never silent (one site, `ui/note.py` `_write_learned_phrases`); (B) a PRACTITIONER DECISION on the enforcing control (must-pause either way): the pinned name heuristic refuses every capitalised non-starter opener; the options are (i) keep the rule (safe, narrow — many instruction lines never teach), (ii) a learner-local opener EXEMPTION list at the real first word only (common clinical/imperative openers: keep, try, avoid, continue, rest, ice, use, start, stop, your, on, for, with, at, in, before, after — an exemption ADMITS listed forms only, so a new form still fails toward refusal, and a starter-homograph name in the list is the same admission the transcript already makes), or (iii) drop the opener from the candidate and learn from the next 2–4 tokens — the architect recommends (ii), scoped-`/review-plan`-hardened before build; (C) Fix now a status note "Not learned: this line is not attributed to you — only your own lines are learned." (one site, `_consider_learning`); (D) Fix now a `QScrollArea` around the Practitioner tab's content (one site, `ui/practitioner.py`). QUESTIONS FOR THE PRACTITIONER (the composer relays verbatim): (1) On the Practitioner tab before the consultation, did you tick the consent box AND "Also learn my phrasing from lines I add or move during review …", then press "Confirm consent" — and did the line under the record controls then say "Consent saved - your voice profile is unchanged."? (2) On the Note tab, what did the text under "Edit the note" say right after you added or moved the line — "Will learn '…' for …", "Not learned: contains a name/number/date/medication (…)", "Phrase learning is off …", or only "Added to …" / "Moved to …"? And after Save — did it start "Learned 1: …"? (3) Which line did you add or move — one attributed to you (speaker_1 with the profile applied), and what was its first word? (4) On the Practitioner tab, can you see the "Learned phrases" box at the bottom (two lists and "No learned phrases yet.") — is it cut off by the window height? No code, test or doc changed this leg; suites unchanged (ruff clean, mypy 34 files, pytest 1869). Still carried: the R3 initials on `docs/testing/shipping-gate.md:55`; Task 4.5 closed on the shipped defaults. Last plan sync: 2026-09-17 05:45 (this bullet).**
- **PHASE 5 ARCHITECT, PEER ROUND 37 FIX LEG (2026-09-16 22:55, run=stage-5, legs f6-verify-r37 → f7-fix-r37, Claude Code `claude-fable-5-1`; no delegation — below the bar; `.cursor/loops/stage-5-delegation.stage-5-arch-f7-fix-r37.log` stays empty) — codex confirmation round 37 CONFIRMED all eight round-36 fixes and returned one MED, PR-MED-025 (a raw temp-file cleanup `OSError` from the shared atomic writer's unguarded `finally` bypassed the config write boundary — the committed-cue outcome, the tab's retryable branch and, in `save()`, the post-commit refresh); LEG 1 verified it MED at its label, the composer routed it to fixing, LEG 2 APPLIED it: `note_config.py` `_write_config_file` folds any raw `OSError` escaping `atomic_write_bytes` into `NoteConfigWriteError` (the session-store writer untouched), `ui/practitioner.py` `on_confirm_consent` catches `OSError` too (the in-phase sibling behind `save_profile`); six pins through the REAL writer with a directory planted at the temp path (append: committed outcome + the cue file loads; the cue-file double fault typed with nothing written; delete: typed failure then a successful retry; the Note tab's `save()` post-commit guarantee — returns, controls updated, state emitted, "Learned 1" + the date-record note, the refresh signal; the Practitioner tab keeps the row and the retry empties both lists; the Windows-only consent-slot sibling). Round 37 closed (1 of 1 Applied); the round-37 Review History line updated in place; the LEG-1 tuple carries `decision=Applied`; Fix-delta self-check PASS. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_note_config.py tests/test_ui_screens.py`, then the full suite in `desktop/` — baseline ruff clean / mypy 34 files / pytest 1863; expected: ruff clean, mypy 34 files, pytest 1869 passed = 1863 + 6 (`test_note_config.py` +3, `test_ui_screens.py` +3, one of them Windows-only — this host runs it); nothing removed or retargeted. Risks the composer should watch: (1) the planted-directory trigger relies on `Path.open("wb")` and `Path.unlink` both raising an `OSError` subclass on a DIRECTORY (Windows: `PermissionError` for both; POSIX: `IsADirectoryError`) — if a host maps either to a non-`OSError`, the pins would show it; (2) the consent-slot pin needs DPAPI (`windows_only`) and a stale profile (`_make_consent_stale`); (3) the out-of-phase residue named at LEG 1 — the shared writer's `finally` masking its primary error — is a follow-up candidate for the composer's Deferred list, not touched here. Then codex confirmation round 38 (the architect is resumed to verify). Files changed this leg: `desktop/src/scribe_desktop/note_config.py`, `ui/practitioner.py`, `desktop/tests/test_note_config.py`, `test_ui_screens.py`, this plan. No commit made by the architect. Last plan sync: 2026-09-16 22:55 (this bullet).**
- **PHASE 5 ARCHITECT, PEER ROUND 36 FIX LEG (2026-09-16 18:05, run=stage-5, legs f4-verify-r36 → f5-fix-r36, Claude Code `claude-fable-5-1`; no delegation — the eight new tests were below the bar, the staging file `.cursor/loops/stage-5-delegation.stage-5-arch-f5-fix-r36.log` stays empty) — codex `gpt-6-astra` peer round 36 returned 0 CRIT / 1 HIGH / 2 MED / 5 LOW over the Phase 5 diff; LEG 1 verified all eight (PR-HIGH-008 → MED with evidence: a real hole, but only capitalised starter-homograph names after an opening filler, the pinned heuristic admits the same word at a true start, the consequence one deletable plaintext phrase), the composer routed all eight to fixing under `gates=executor`, and LEG 2 APPLIED them one at a time (see the round's `/fix notes`): `LearningCandidate.first_in_segment` carries the first chosen word's REAL segment position into the filter; `append_user_cues` RETURNS a sidecar-only write failure (`AppendedCues.sidecar_error`) after the cue file is on disk and the Note tab reports "learned, without a date" and fires `learned_phrases_changed`; `delete_user_cue` cleans the cue file and the sidecar independently, finishes a partial deletion on retry and prunes orphans; D9 in `## Design Decisions` rewritten to the amended design; surface 10's "never a whole utterance" → the enforced four-token prefix bound; the "every phrase with its date" overclaim narrowed at surface 1 and the plan's Learning constraint; the Review-edits constraint (+ the Goal and Flow 5 siblings) now states section-based admission with learning clinician-only; the gate page's example (1) relabelled and a genuine proposal-only example (1/3) added; `AGENTS.md` Current Status's stale "propose-then-approve" summary swept; CHANGELOG sentence; retention row. Round 36 closed (8 of 8 Applied); the round-36 Review History line updated in place; the LEG-1 tuple lines carry `decision=Applied`; Fix-delta self-check PASS. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_note_config.py tests/test_ui_screens.py tests/test_ui_models.py`, then the full suite in `desktop/` — baseline ruff clean / mypy 34 files / pytest 1854; expected: ruff clean, mypy 34 files, pytest 1863 passed = 1854 + 9 (`test_note_config.py` +7: two in `TestProposeLearningPhrase`, two in `TestAppendUserCues`, three in `TestDeleteUserCue`; `test_ui_screens.py` +2 in `TestNoteScreenEdits`); nothing removed or retargeted (the existing `test_an_unknown_phrase_returns_false_and_writes_nothing` still holds — no orphan, nothing written). Risks the composer should watch: (1) mypy on `AppendedCues` (a `NamedTuple` field with a default) and on `delete_user_cue`'s `sidecar_kept.keys() != sidecar.keys()`; (2) the selective `atomic_write_bytes` monkeypatch in both test files (`_fail_sidecar_writes` / the UI test's `selective`) keys on `path.name == LEARNED_SIDECAR_FILENAME`; (3) `test_a_name_after_a_leading_filler_is_refused_by_the_learner` assumes "Um, Will needs the exercises" and "Will needs the exercises" are unrouted by the shipped cues (no cue among "will", "needs", "exercises" — "home exercise" needs the token `exercise`) and that `normalise_token("Um,")` is the filler `um`; (4) the UI status pin looks for the literal `(name)` and "date record could not be written". Then codex confirmation round 37 (the architect is resumed to verify). Files changed this leg: `desktop/src/scribe_desktop/note_config.py`, `ui/note.py`, `ui/practitioner.py` (comment), `desktop/tests/test_note_config.py`, `test_ui_screens.py`, `docs/security/threat-model.md`, `docs/security/retention-schedule.md`, `docs/testing/shipping-gate.md`, `AGENTS.md`, `CHANGELOG.md`, this plan. No commit made by the architect. Last plan sync: 2026-09-16 18:05 (this bullet).**
- **PHASE 5 ARCHITECT, CONVERGENCE (2026-09-16 17:30, run=stage-5, leg stage-5-arch-f3-confirm, Claude Code `claude-fable-5-1`; no delegation this leg — `.cursor/loops/stage-5-delegation.stage-5-arch-f3-confirm.log` stays empty) — round-34 fixes composer-verified GREEN (17:25: ruff clean, mypy 34 files, `test_ui_screens.py` 182, full suite 1854); `/review-loop` ROUND 35 (2 of cap 3, confirmation): 0 CRIT / 0 HIGH / 0 MED / 0 LOW — full re-review of the Phase 5 surface, the Post-Fix Regression Check over the eleven round-34 hunks found no affected caller (no signature, return or shape changed; `_read_learning_status` kept for the wiring tests), the missed-issue pass found nothing; Review History integrity: OK (the round-35 line parses; history checker 35 + 35); no gate fired; no code changed this round, so no suite is owed. LOOP CONVERGED at round 2 of 3. PHASE 5's AGENT TASKS COMPLETE on the architect's side: Tasks 5.0–5.4 🟩, Task 5.5 🟨 pending ONLY the practitioner's initials on the R3 clarification; progress 80% (32 of 40). Immediate next action: (1) the COMPOSER-seat cross-family codex peer pass (`gpt-6-astra`, xhigh for the first round) over the Phase 5 diff — `git diff 7696b28`, every file tracked (20 files, 4290 insertions): the surface is `note.py` (the ownership block `spoken_by_confirmed_clinician` / `section_admits_utterance` / `admissible_sections` / `first_matching_section`, the id schemes, `whole_utterance_assertion`, the slimmed `_route`), `note_config.py` (the "Consented phrase learning" block: the filter, the proposer, `append_user_cues` / `load_learned_phrases` / `delete_user_cue`, the sidecar models, `NoteConfigWriteError`), `ui/models.py` (consent v2 constants, `consent_is_current`, `learning_status`, `working_draft`, `eligible_utterances`, `editable_lines`, the omission copy, the stale-consent report line), `ui/note.py` (the "Edit the note" group, add/remove/move/undo, `_consider_learning`, the Save-time write), `ui/practitioner.py` (consent v2, the stale notice, Confirm consent, the two learned lists), `ui/main_window.py`, `practitioner_profile.py` (docstring), the four test modules, the three security docs, `docs/design-system.md`, `docs/testing/shipping-gate.md` (the R3 clarification + the two-voice mock), `AGENTS.md`, `CHANGELOG.md`, and `.cursor/plans/plan-phase3a-note-pipeline.md` (the Task 9.1 header's R3 sentence); the architect is resumed per round to verify and fix; peer round numbering continues at 36. Lenses for the cross-family reader: (a) the five bounding call sites threat-model surface 1 names for what can be WRITTEN into the cue file — is there any path by which a line not attributed to the confirmed clinician, or a candidate the filter would refuse, reaches `append_user_cues`, and is the write reachable anywhere but `save()` after `on_save` returned (`_consider_learning`'s ownership-first order, `_write_learned_phrases`, the queue's clearing on undo / cancel / abandon / a new transcript); (b) the docs-accuracy class (rounds 40/41) over surface 1's honest limit, surface 2's review-edit paragraph, surface 9's re-consent clause, surface 10, flow 13 and the learned-phrases retention row — every sentence against its call site, including the four named residues of the filter and the two-file write; (c) the ownership extraction — is `first_matching_section` byte-for-byte `_route`'s old rule, and can the chooser or a Move admit a section the router would have refused (`admissible_sections` + Check 3's coordinate-derived `role_unconfirmed`); (d) the consent-version gate — every place a v1 record could still pre-tick, enable Record or enable learning (`refresh_profile_state`, `_update_controls`, `learning_status`, `consent_is_current`). (2) The practitioner's live smoke of this UI-bearing phase is the MUST-PAUSE after the peer pass (fresh start on their real v1 profile → the stale notice → tick + Confirm consent with the opt-in → a mock consultation → Generate → add / remove / move a line → a phrase learned on Save → the Practitioner tab's lists and Delete), together with the R3 initials for Task 5.5. (3) Then the hardening stage (H1–H4) under this run, then the practitioner's Phase 6. Watch items carried from the build leg: the capitalised-opener refusal narrows what learning picks up (now a named residue at three sites; a scoped amendment is the hardening if the smoke shows it bites); a candidate takes up to four leading tokens; the consent text's markdown emphasis ships as the plain word. Files changed this leg: this plan only. No commit made by the architect. Last plan sync: 2026-09-16 17:30 (this bullet).**
- **PHASE 5 ARCHITECT, REVIEW LEG (2026-09-16 17:40, run=stage-5, leg stage-5-arch-f2-review, Claude Code `claude-fable-5-1`; no delegation this leg — three LOWs below the bar; `.cursor/loops/stage-5-delegation.stage-5-arch-f2-review.log` stays empty) — Tasks 5.0–5.4 🟩 (composer-verified GREEN 17:16: ruff clean, mypy 34 files, the four test modules 766 passed, full suite 1852); Task 5.5 stays 🟨 until the practitioner initials the R3 clarification (the blank line is on the page); progress 67% → 80% (32 of 40). `/review-loop` ROUND 34 (1 of cap 3) DONE: 0 CRIT / 0 HIGH / 0 MED / 3 LOW, all Fix-now and APPLIED by the architect one at a time (LOW-001 — the capitalised-opener refusal named as a residue in threat-model surface 1, the `note_config.py` block comment and the filter's docstring; LOW-002 — the utterance/section choosers keep their selection across a re-finalise; LOW-003 — the learning gate is re-read at every add or move, not only at review start and Save, so a mid-review opt-in applies; +2 pins in `test_ui_screens.py::TestNoteScreenEdits`). Review History integrity: OK (the round-34 line parses; history checker 34 + 34); Fix-delta self-check PASS; no gate fired (headless auto-disposable). Code changed, so handing off `reason=composer-run`: NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_ui_screens.py`, then the full suite in `desktop/` — baseline ruff clean / mypy 34 files / pytest 1852; expected: ruff clean, mypy 34 files, `test_ui_screens.py` +2, full suite 1854 passed (nothing removed or retargeted). Risks the composer should watch: (1) `test_the_chooser_keeps_its_selection_across_a_refinalise` assumes the default edit fixture offers ≥ 2 eligible lines and the first offered line ≥ 2 sections — segments 3, 4 and 5 of `_EDIT_TURNS` are unrouted (the "ice pack" autofill trigger is not a cue) and segment 3 is a clinician statement, so 17 sections; (2) `_refresh_learning_status` runs one profile read per add/move on the GUI thread (a DPAPI unwrap + one decrypt, the same read `begin_review` makes) — negligible, but the smoke can confirm. On green the architect runs round 35 (the confirmation round with the Post-Fix Regression Check over the eleven round-34 hunks); at convergence it ends `phase-complete` for the composer-seat codex pass. Files changed this leg: `desktop/src/scribe_desktop/ui/note.py`, `note_config.py` (comment + docstring), `desktop/tests/test_ui_screens.py`, `docs/security/threat-model.md`, `docs/security/data-flow-map.md`, this plan. No commit made by the architect. Last plan sync: 2026-09-16 17:40 (this bullet).**
- **PHASE 5 ARCHITECT, BUILD LEG (2026-09-16 17:25, run=stage-5, leg stage-5-arch-f1, Claude Code `claude-fable-5-1`; ARCHITECT MODE — TWO delegations to the `loop-delegate` (`claude-opus-5` by agent-definition pin; the Agent results again exposed no `modelUsage` field): the learner + ownership tests (`test_note_config.py`, `test_note.py`) and the UI tests (`test_ui_screens.py`, `test_ui_models.py`), both verified line by line against their specs, records in `.cursor/loops/stage-5-delegation.stage-5-arch-f1.log`) — Tasks 5.0, 5.1, 5.1b, 5.2, 5.3, 5.4 and 5.5 BUILT and marked 🟨 (file-by-file records on each task); NOT yet verified (verify=composer); handing off `reason=composer-run`; NO open gate (no CRIT/HIGH, no Defer/Accept, nothing production-impacting — no dependency, env var, migration or deploy config; the sidecar file and the consent-version bump are the tasks' own deliverables).** Files changed this leg: `desktop/src/scribe_desktop/note.py` (the ownership rule extracted + the id schemes + `whole_utterance_assertion`), `note_config.py` (the learner block), `practitioner_profile.py` (a docstring), `ui/models.py` (consent v2, learning status, the review-edit view models, the omission copy), `ui/note.py` (rewritten in full around the existing behaviour: the "Edit the note" group, the working draft, the learning queue, the Save-time write), `ui/practitioner.py` (rewritten in full around the existing behaviour: consent v2, the stale notice, Confirm consent, the two learned lists), `ui/main_window.py` (`config_root=`, the learning provider, the signal wiring); tests `desktop/tests/test_note.py`, `test_note_config.py`, `test_ui_screens.py`, `test_ui_models.py` (delegates; two architect edits in the first pair, logged); docs `docs/security/threat-model.md`, `data-flow-map.md`, `retention-schedule.md`, `docs/design-system.md`, `docs/testing/shipping-gate.md`, `AGENTS.md`, `CHANGELOG.md`, `.cursor/plans/plan-phase3a-note-pipeline.md` (the Task 9.1 header's R3 sentence), this plan. Verified by the architect itself: every production hunk re-read (the `git diff` of all seven source files), no line over 100 characters in any touched Python file (grep; the one 103-character line in `test_note.py:450` is pre-existing), ruff-isort order of every import block checked by eye against the case-insensitive type-grouped rule, every doc claim traced to its call site before writing, every delegate test checked against the production code AND the packaged cue file's routing of the fixture lines. NOT run (no grant): ruff, mypy, pytest. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_note.py tests/test_note_config.py tests/test_ui_models.py tests/test_ui_screens.py`, then the full suite in `desktop/` — baseline ruff clean / mypy 34 files / pytest 1705; expected: ruff clean, mypy 34 files (no new module), pytest ≈ 1852 passed = 1705 + 73 (`test_note_config.py`: `TestRefusalFilter` 37-case fixture + 2, `TestProposeLearningPhrase` 8, `TestAppendUserCues` 15, `TestLoadLearnedPhrases` 6, `TestDeleteUserCue` 5) + 27 (`test_note.py::TestOwnershipRule`, 17 parametrised) + 34 (`test_ui_screens.py`: `TestPractitionerScreen` +11 — 6 Windows-only, all run here — `TestNoteScreenEdits` 21, `TestNoteWiring` +2) + 13 (`test_ui_models.py`: `TestLearningStatus` 6, `TestReviewEditModels` 7); three retargets (`test_constructs_with_consent_text_verbatim_and_nothing_enabled`, `test_consent_text_is_versioned`, `test_profile_line_enrolled`), no item removed — a `--collect-only -q` gives the exact figure if the ±1 on the fixture count matters. Risks the composer should watch: (1) mypy strict on `ui/note.py` — the `lambda _=False, aid=…, combo=target: self.move_line(aid, combo.currentData())` default-bound lambdas (B023-safe) and `combo.currentData()`'s `Any`; on `ui/practitioner.py` — `profile.model_copy(update=…)` typed `Self`, the `and`-chain narrowing of `profile` in `_update_controls`; on `note_config.py` — `RefusalClass` as a module-level `Literal` alias used in a return annotation; (2) ruff `E3xx` blank-line rules if enabled on this ruff version (checked by eye); (3) `test_ui_screens.py::TestNoteScreenEdits::test_removing_a_line_raises_an_acknowledgeable_omission_never_a_block` assumes NO `high_risk_omission` exists before the removal — every other clinician line in `_EDIT_TURNS` is either routed (covered) or carries no high-risk token ("Please" is a common starter); if the shipped cues change, the fixture comment says which lines must stay unrouted; (4) the DPAPI tests (`_enrol`, `_make_consent_stale`, `save_profile`) run only on Windows — this host; (5) `test_a_config_root_that_is_a_file_cannot_be_written` (architect-added) accepts ANY `NoteConfigError` because the read of `<file>/section_cues.json` maps to `FileNotFoundError` or `NotADirectoryError` depending on the host; (6) the practitioner's REAL profile carries `consent-v1`: after this lands the app's Practitioner tab shows the stale notice and asks for a fresh tick + Confirm consent (by design, Task 5.0) — say so at the smoke. On green: mark 5.0–5.5 🟩 and progress 67% → 85% (34 of 40 tasks, the plan now counting 5.0), then `/review-loop` from round 34 (max existing round 33) over the Phase 5 diff (cap 3), then end `phase-complete` for the composer-seat codex pass — point the peer at the five bounding call sites named in threat-model surface 1 and at flow 13 (the docs-accuracy class), at `_consider_learning`'s ownership-first order, at the two-file write's named residue, and at the R3 clarification (a rubric-v1 clarification, no number changed). DESIGN READINGS the composer should relay to the practitioner (recorded on the tasks, none a blocker): (a) the ratified consent text's markdown emphasis on *you* ships as the plain word (the label is PlainText); (b) the refusal filter honours the pinned name heuristic at the utterance's first word, so a capitalised opener outside the transcript's common-starter set ("On examination…", "Your home…") is refused as a name and such lines never teach — fail-closed and spec-conformant, but it narrows what learning can pick up; a scoped amendment (a learner-local opener exemption list, or dropping the opener from the candidate) is the hardening if the smoke shows it bites; (c) a candidate takes up to FOUR leading tokens (the specific end of the 2–4 range). STILL THE PRACTITIONER'S: the initials on the R3 clarification in `docs/testing/shipping-gate.md` (Task 5.5) and the live smoke of this UI-bearing phase (add / remove / move a line, a phrase learned on Save, the Practitioner tab's lists and Delete, the stale-consent notice and Confirm consent on their real v1 profile) — the composer's MUST-PAUSE after the peer pass. Last plan sync: 2026-09-16 17:25 (this bullet).**
- **PHASE 4 ARCHITECT, PEER ROUND 32 FIX LEG (2026-09-16 16:08, run=stage-4, legs e4-verify-r32 → e5-fix-r32, Claude Code `claude-fable-5-1`; no delegation — both fixes below the bar; the leg's staging file stays empty) — codex `gpt-6-astra` peer round 32 (xhigh) returned 0 CRIT / 1 HIGH / 0 MED / 1 LOW over the Phase 4 diff; LEG 1 verified both (PR-HIGH-007 → MED with evidence: an emptied-but-valid-JSON packaged default is the only path, the consequence a visible empty draft; PR-LOW-034 confirmed docs-only LOW), the composer routed both to fixing under `gates=executor`, and LEG 2 APPLIED them one at a time (see the round's `/fix notes`): `note.py`'s reader is split into `_parse_shipped_section_cues(payload)` — shape AND completeness (every canonical key present, every list non-empty; two new "incomplete (broken install)" refusals) — and `_load_shipped_section_cues()` (read + decode + parse); +10 test items in `test_note.py::TestShippedSectionCues` (an 8-way parametrised refusal test, a complete-payload accept test that is deliberately NOT a count pin, and the packaged-file-parses-to-the-constant pin); `SectionCuesFile` and the sparse-user-file loader tests untouched (D6 preserved); `CHANGELOG.md:8` and the Task 4.1 record narrowed to the enforced rule; `note_config.py:6` now "validated, attribute-frozen" with the residue pointer, and the `section_cues` comment's closing clause replaced by the exact bound (sibling grep clean). Round 32 closed (2 of 2 Applied); the round-32 Review History line updated in place; the LEG-1 tuple lines carry `decision=Applied`; Fix-delta self-check PASS. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_note.py tests/test_note_config.py`, then the full suite in `desktop/` — baseline ruff clean / mypy 34 files / pytest 1695; expected: ruff clean, mypy 34 files, pytest 1705 passed (+8 parametrised cases + 2 in `test_note.py`; `test_note_config.py` unchanged in count; nothing removed or retargeted); then the codex confirmation round 33 (the architect is resumed to verify). Risks the composer should watch: (1) mypy on `_parse_shipped_section_cues(payload: object)` — the `isinstance(payload, dict)` narrowing feeds `.get`, and `", ".join(missing)` joins `Literal` keys; (2) the parametrised ids contain an underscore (`no-section_cues-object`) — legal for pytest; (3) the import-time guard runs on every `scribe_desktop.note` import, so a shipped file that failed it would break collection loudly — that is the point, and the packaged file is complete (17 keys, all non-empty, pinned). Files changed this leg: `desktop/src/scribe_desktop/note.py`, `note_config.py` (comments/docstring), `desktop/tests/test_note.py`, `CHANGELOG.md`, this plan. No commit made by the architect. Last plan sync: 2026-09-16 16:08 (this bullet).**
- **PHASE 4 ARCHITECT, CONVERGENCE (2026-09-16 15:53, run=stage-4, leg stage-4-arch-e3-confirm, Claude Code `claude-fable-5-1`; no delegation this leg — `.cursor/loops/stage-4-delegation.stage-4-arch-e3-confirm.log` stays empty) — Task 4.5 🟩 (composer-verified 15:52: `cmp` identical, the README smoke exact, ruff clean, mypy 34 files, `test_note_config.py` 158, full suite 1695); Tasks 4.1–4.5 all 🟩, progress 67% (26 of 39). `/review-loop` ROUND 31 (2 of cap 3, confirmation): 0 CRIT / 0 HIGH / 0 MED / 0 LOW — full re-review of every changed file, the Post-Fix Regression Check over the six round-30 hunks (four docs, two comments; no signature, default or validator changed) found no affected caller, the missed-issue pass found nothing; Review History integrity: OK (the round-31 line parses; history checker 31 + 31); no gate fired; no code changed this round, so no suite is owed. LOOP CONVERGED at round 2 of 3. PHASE 4's AGENT TASKS COMPLETE on the architect's side. Immediate next action: (1) the COMPOSER-seat cross-family codex peer pass (`gpt-6-astra`, xhigh for the first round) over the Phase 4 diff — `git diff 055d0aa` PLUS the two UNTRACKED files the peer must be told to read (`desktop/src/scribe_desktop/config_defaults/section_cues.json`, `docs/testing/shipping-gate-config/section_cues.json` — byte-identical); the surface is `note.py` (the cue block `:722-800`, the `__all__` entry), `note_config.py` (the docstring paragraph, `CONFIG_FILENAMES`, `_check_section_cues`, `SectionCuesFile`, `NoteConfig.section_cues` + `normalised_cues`, `load_note_config`), `note_check.py` (docstring), `ui/models.py` (`_extractive_provider_from_config`, `build_note_generator`), the three test modules (`test_note.py::TestShippedSectionCues`, `test_note_config.py::TestSectionCuesFile` / `TestSectionCuesLoader` / the packaging pair, `test_ui_models.py::TestProviderFromConfig`), the three security docs, `docs/testing/shipping-gate.md`, the gate-config README, `AGENTS.md:84`, `CHANGELOG.md`; the architect is resumed per round to verify and fix; peer round numbering continues at 32. Lenses for the cross-family reader: (a) D6/D7 held by structure, not prose — is there any path by which the provider the app runs routes by cues the draft's `config_digest` does not bind (the worker builds from the loaded config; `NoteConfig`'s empty default is only reachable by direct construction; the frozen-dict residue named at `note_config.py:788-798`); (b) the docs-accuracy class (rounds 40/41) over the routing-not-content claims in `threat-model.md` surface 1, `data-flow-map.md` flow 11 and the retention row — every sentence against `ExtractiveNoteProvider._route` / `generate_sections`, the read-only loader and `check_note`; (c) the two-call-site validator and the loader's error naming — a malformed user cue file must name `section_cues.json`, never "resolved note config"; (d) the MINIMAL import-time read in `note.py` versus the content rules that live only in `SectionCuesFile` (the shipped file is pinned against the model by test — is the gap closed for every shape the shipped file could take?). (2) The composer's commit after the peer pass (no smoke is owed: Phase 4 has no UI). (3) Then the scoped `/review-plan` at the Phase 4/5 boundary the composer named (the practitioner's auto-learn-review-later decision amends D9 / consent v2 BEFORE Phase 5 starts — not this leg's work; nothing in Phase 5 or the consent text was touched). Files changed this leg: this plan only. No commit made by the architect. Last plan sync: 2026-09-16 15:53 (this bullet).**
- **PHASE 4 ARCHITECT, REVIEW LEG (2026-09-16 15:48, run=stage-4, leg stage-4-arch-e2-review, Claude Code `claude-fable-5-1`; no delegation this leg — the leg's staging file `.cursor/loops/stage-4-delegation.stage-4-arch-e2-review.log` stays empty) — Tasks 4.1–4.4 🟩 (composer-verified GREEN 15:43: ruff clean, mypy 34 files, the three test modules 463 passed, full suite 1695); Task 4.5 BUILT and 🟨 on the practitioner's relayed decision "use the defaults" (15:20): `docs/testing/shipping-gate-config/section_cues.json` is a byte-identical copy of the shipped file (untracked), the README's paragraph states the decision, install step 2 copies it unconditionally and the loader smoke prints the cue counts (expected `1 rules, 3 prefills, 17 cue sections, 98 cue phrases`); progress 54% → 67% (26 of 39). `/review-loop` ROUND 30 (1 of cap 3) DONE: 0 CRIT / 0 HIGH / 0 MED / 2 LOW, both Fix-now and APPLIED by the architect one at a time (LOW-001 — three docs sites still called the gate-run cue file "owed once the practitioner supplies phrases" after the decision; `shipping-gate.md`, `CHANGELOG.md` (entry now Tasks 4.1–4.5), `AGENTS.md:84`; LOW-002 — the frozen models' plain-dict `section_cues` residue named and bounded in the `NoteConfig` field comment and the `SectionCuesFile` docstring; comment-only). Review History integrity: OK (the round-30 line parses); Fix-delta self-check PASS; no gate fired (headless auto-disposable). Code touched (a comment hunk in `note_config.py`), so handing off `reason=composer-run`: NEEDS THE COMPOSER: (1) the byte-identity check of the Task 4.5 copy against `desktop/src/scribe_desktop/config_defaults/section_cues.json` (`fc.exe /b` or a hash) and the README smoke from a NORMAL terminal at the repo root; (2) `ruff check .`, `pytest tests/test_note_config.py` in `desktop/`, then the full suite — expected ruff clean, mypy 34 files unchanged, `test_note_config.py` unchanged in count, full suite 1695 (nothing added or removed this leg). On green: mark 4.5 🟩, then the architect runs round 31 (the confirmation round with the Post-Fix Regression Check over the six round-30 hunks); at convergence it ends `phase-complete` for the composer-seat codex pass over `git diff 055d0aa` PLUS the two untracked files (`desktop/src/scribe_desktop/config_defaults/section_cues.json`, `docs/testing/shipping-gate-config/section_cues.json`). Files changed this leg: `docs/testing/shipping-gate-config/section_cues.json` (new), `docs/testing/shipping-gate-config/README.md`, `docs/testing/shipping-gate.md`, `CHANGELOG.md`, `AGENTS.md`, `desktop/src/scribe_desktop/note_config.py` (comments), this plan. No commit made by the architect. Last plan sync: 2026-09-16 15:48 (this bullet).**
- **PHASE 4 ARCHITECT, BUILD LEG (2026-09-16 15:37, run=stage-4, leg stage-4-arch-e1, Claude Code `claude-fable-5-1`; ARCHITECT MODE — ONE delegation to the `loop-delegate` (the three test modules against the architect's spec; the Agent result again exposed NO `modelUsage` field, so the resolved tier rests on the agent-definition pin `claude-opus-5` and the run's preflight probe); records in `.cursor/loops/stage-4-delegation.stage-4-arch-e1.log`) — Tasks 4.1, 4.2, 4.3 and 4.4 BUILT and marked 🟨 (file-by-file records on the tasks); Task 4.5 stays 🟥 (needs the PRACTITIONER's phrases — not built, as instructed); NOT yet verified (verify=composer); handing off `reason=composer-run`; NO open gate (no CRIT/HIGH, no Defer/Accept, nothing production-impacting — no dependency, env var, migration or deploy config; the new packaged JSON is Task 4.1's named deliverable; one docs-only LOW class fix in `threat-model.md` auto-disposed and recorded on Task 4.4).** Files changed this leg: `desktop/src/scribe_desktop/config_defaults/section_cues.json` (NEW, untracked — a peer reading `git diff` must be told), `desktop/src/scribe_desktop/note.py`, `note_config.py`, `note_check.py`, `ui/models.py`, `desktop/tests/test_note.py`, `test_note_config.py`, `test_ui_models.py` (the three test files by the delegate, verified line by line against the spec: six readings surfaced by the delegate, all accepted — two `F401`-avoiding import omissions, a 3-line shared bytes helper, isort placement of `_canonical_config`, section banners, an exact-list assertion the architect re-checked against all 98 phrases; no silent patch, no miss), `docs/security/data-flow-map.md`, `docs/security/retention-schedule.md`, `docs/security/threat-model.md`, `docs/testing/shipping-gate.md`, `docs/testing/shipping-gate-config/README.md`, `CHANGELOG.md`, this plan. Verified by the architect itself: every phrase of the new JSON re-read against the deleted literal; every hunk re-read; no line over 100 characters in the four touched source files and none added to the three test files (grep; the one 103-character line in `test_note.py:441` is pre-existing); every doc claim traced to its call site (cues route, never author; the digest binds them; Phase 5 not built). NOT run (no grant): ruff, mypy, pytest. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_note.py tests/test_note_config.py tests/test_ui_models.py`, then the full suite in `desktop/` — baseline ruff clean / mypy 34 files / pytest 1666; expected: ruff clean, mypy 34 files (no new module), pytest 1695 passed = 1666 + 3 (`TestShippedSectionCues`) + 12 (`TestSectionCuesFile`) + 9 (`TestSectionCuesLoader`) + 5 (`TestProviderFromConfig`); no item removed or retargeted — the fourteen digest equality pins compare objects. Risks the composer should watch: (1) mypy strict on `note_config.py` — the `Mapping[NoteSectionKey, tuple[_TriggerText, ...]] = {}` field defaults on both models and `_check_section_cues`'s `Mapping[NoteSectionKey, tuple[str, ...]]` parameter receiving them; on `note.py` — `_load_shipped_section_cues` narrows `json.loads`'s `Any` with `isinstance` (a mypy complaint there is a typing nit, not a behaviour defect); (2) `NoteConfig` is a frozen pydantic model now holding a dict field — hashing one would raise, and the grep found no `hash(` / set / cache use of a config anywhere in `src` or `tests`; (3) `test_the_shipped_cues_route_through_the_generator` runs `compose_draft` with the shipped `template-a` profile over a one-utterance transcript written under a real `SessionCrypto` — if `compose_draft` needs anything the Phase 6 pipeline tests do not (they use the same `write_transcript` idiom), it surfaces here first; (4) every existing `NoteConfig(...)` construction in the suite now also runs `_check_section_cues` over an empty mapping (a no-op) and every `config_digest` value changes once — expected, no test pins a literal. On green: mark 4.1–4.4 🟩 and progress 54% → 64% (25 of 39), then `/review-loop` from round 30 (max existing round 29) over the Phase 4 diff (cap 3), then end `phase-complete` for the composer-seat codex pass — point the peer at the untracked JSON, at the routing-not-content claims in the three security docs (the round-40/41 class) and at the two-call-site validator. Task 4.5 (the gate-run cue file) waits for the practitioner's phrases — the composer has asked. Placement note: newest-first at the top of this section, the file's standing order. Last plan sync: 2026-09-16 15:37 (this bullet).**
- **PHASE 3 ARCHITECT, PEER ROUND 27 FIX LEG (2026-09-16 14:10, run=stage-3, legs d4-verify-r27 → d5-fix-r27, Claude Code `claude-fable-5-1`; one delegation — the PR-REG-005 test plumbing — to the `loop-delegate`, records in `.cursor/loops/stage-3-delegation.stage-3-arch-d5-fix-r27.log`; the Agent result again exposed no `modelUsage` field) — codex `gpt-6-astra` peer round 27 returned 0 CRIT / 1 HIGH / 2 MED / 4 LOW over the Phase 3 diff; LEG 1 verified all seven (PR-HIGH-006 → MED: a same-login store fault is the only path; PR-MED-022 → LOW: the shipped Start path has the identical tick handoff), the composer routed all seven to fixing under `gates=executor`, and LEG 2 APPLIED them one at a time (see the round's `/fix notes`): the consent box is pre-ticked only from a READABLE profile's record and a tick from a record that becomes unreadable in-session is withdrawn (+2 tests, 1 retargeted); the learning opt-in stays editable and applies at the next re-record (+1 test, 1 retargeted); the idle monitor is handed over synchronously through a new `on_capture_start` seam wired to `stop_monitor` (+3 tests); the speaker report line claims "installed" not "ready" (2 tests retargeted); the CHANGELOG and the `closeEvent` comment state the real Stop/save boundary; surface 9 states both outcomes of the delete/read race; `MicrophoneScreen(profile_root=)` keeps every test off the default store (12 + 1 sites by the delegate, +1 pin test). Fix-delta self-check: flagged-and-fixed in-leg (the in-session readable→unreadable consent transition). Round 27 closed (7 of 7 Applied); the round-27 Review History line updated in place. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_ui_screens.py tests/test_ui_models.py tests/test_status_and_app.py`, then the full suite in `desktop/` — baseline ruff clean / mypy 34 files / pytest 1660; expected: ruff clean, mypy 34 files, pytest 1666 passed (+2 Windows-only in `TestPractitionerScreen`, +2 hook tests there, +2 in `TestMainWindow`; no item removed); then the codex confirmation round 28 (the architect is resumed to verify). Risks the composer should watch: (1) `test_capture_start_hook_runs_before_the_capture_on_the_gui_thread` compares `threading.main_thread().ident` with `get_ident()` — the GUI thread is the main thread under pytest; (2) `test_profile_reads_never_touch_the_default_root` monkeypatches `practitioner_profile.default_profile_root` — any construction-time read through the default root anywhere in `MainWindow` would surface here as an `AssertionError` (that is the point; the TranscriptScreen's readiness provider is only consulted when a document is shown); (3) `test_re_record_changes_the_learning_opt_in` relies on `refresh_profile_state` re-reading the stored choice after a failed re-record. Files changed this leg: `desktop/src/scribe_desktop/ui/practitioner.py`, `ui/microphone.py`, `ui/main_window.py`, `ui/models.py`, `desktop/tests/test_ui_screens.py`, `test_ui_models.py`, `test_status_and_app.py`, `docs/security/threat-model.md`, `docs/design-system.md`, `CHANGELOG.md`, this plan. No commit made by the architect. Lint leg d6 (14:15): the composer's suites over these fixes were green (mypy 34 files, the three UI modules 214 passed, full suite 1666) except ONE ruff E501 in the `ui/practitioner.py` module docstring (line 17, 103 characters, the PR-MED-022 handoff sentence) — re-wrapped, no wording change; expected on re-run: ruff clean, `test_ui_screens.py` unchanged. Peer round 28 (codex confirmation, legs d7 → d8, 14:22–14:25): all seven round-27 fixes CONFIRMED; two new docs-only LOWs verified and APPLIED as classes — PR-LOW-032 (the Stop/save residue starts at the final post-embedding check, not at the save: threat-model surface 5, the CHANGELOG clause, the `raise_if_stopped` comment) and PR-LOW-033 (the consultation Start path's overlap lasts until the next monitor poll, a 100 ms cadence rather than a bound). Round 28 closed (2 of 2 Applied); the comment hunk in `ui/practitioner.py` means ruff + `test_ui_screens.py` are owed (expected: ruff clean, 146 passed), then codex round 29 (the confirmation round — a clean or docs-only round would be the tail signature). Last plan sync: 2026-09-16 14:25 (this bullet).**
- **PHASE 3 ARCHITECT, CONVERGENCE (2026-09-16 13:09, run=stage-3, leg stage-3-arch-d3-confirm, Claude Code `claude-fable-5-1`) — round-25 fixes composer-verified GREEN (13:08: ruff clean, mypy 34 files, `test_ui_screens.py` 140 passed, full suite 1660); `/review-loop` ROUND 26 (2 of cap 3, confirmation): 0 CRIT / 0 HIGH / 0 MED / 0 LOW — full re-review of every changed file, the Post-Fix Regression Check over the two round-25 hunks found no affected caller, the missed-issue pass found nothing; Review History integrity: OK (the round-26 line parses; history checker 26 + 26); no gate fired; no code changed this round, so no suite is owed; no delegation (the leg's staging file stays empty). LOOP CONVERGED at round 2 of 3. PHASE 3's AGENT TASKS COMPLETE on the architect's side: Tasks 3.1–3.3 🟩 (progress 51%). Immediate next action: (1) the COMPOSER-seat cross-family codex peer pass (`gpt-6-astra`, xhigh for the first round) over the Phase 3 diff — `git diff 1e93a6f` plus the UNTRACKED `desktop/src/scribe_desktop/ui/practitioner.py` (the peer must be told to read it: an untracked file is absent from `git diff`); the surface is `ui/practitioner.py` (new), `ui/models.py`, `ui/main_window.py`, `desktop/tests/test_ui_screens.py`, `desktop/tests/test_ui_models.py`, the three security docs, `docs/design-system.md`, `AGENTS.md`, `CHANGELOG.md`; the architect is resumed per round to verify and fix; peer round numbering continues at 27; point the peer at the docs-accuracy class (every threat-model / retention / data-flow claim against its call site — the round-40/41 lesson) and at the D15 sequence in `on_record` / the two handlers; (2) the composer's commit is REFUSED without Task 3.4's `smoke-pass` record — the practitioner's live smoke (fresh start → banner → consent → enrol → a mock consultation → auto-confirmed role shown → Generate) is the MUST-PAUSE after the peer pass; the practitioner needs the speaker model promoted (Task 0.6, done) and a microphone; (3) then Phase 4 under this run. Watch item carried from Task 3.2: the microphone screen's 5 s report timer now performs one DPAPI profile read per tick — cache the readiness if the smoke shows sluggishness. Files changed this leg: this plan only. No commit made by the architect. Last plan sync: 2026-09-16 13:09 (this bullet).**
- **PHASE 3 ARCHITECT, REVIEW LEG (2026-09-16 13:05, run=stage-3, leg stage-3-arch-d2-review, Claude Code `claude-fable-5-1`) — Tasks 3.1–3.3 🟩 (composer-verified GREEN 13:01: ruff clean, mypy 34 files, the two UI modules 194 passed, full suite 1659; progress 51%); `/review-loop` ROUND 25 (1 of cap 3) DONE: 0 CRIT / 0 HIGH / 1 MED / 1 LOW, both Fix-now and APPLIED by the architect (no delegation — below the bar; the leg's delegation staging file stays empty): MED-001 — a Stop pressed after the capture returned was not honoured and the profile was still embedded and SAVED under a "Stopping..." status; fixed with a `raise_if_stopped()` check before the embedding and again before the save (`EnrolmentCancelledError` → the existing "Enrolment stopped - nothing was saved." path), a regression test (`test_stop_after_the_capture_returned_still_saves_nothing`: the `embed` seam blocks, Stop lands mid-embedding, nothing on disk, one `end_enrolment`) and threat-model surface 5 naming where Stop is honoured plus the post-save residue; LOW-001 — the unused `released` attribute on the test's `_FakeCapture` removed. Review History integrity: OK (the round-25 line parses); no gate fired (in scope, not production-impacting — headless auto-disposable); two candidates dropped at verification (recorded in the round). Code changed, so handing off `reason=composer-run`: NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_ui_screens.py`, then the full suite in `desktop/` — expected ruff clean, mypy 34 files, pytest 1660 passed (1659 + 1; the new test is not Windows-only). On green the architect runs round 26 (the confirmation round with the Post-Fix Regression Check over the two round-25 hunks); at convergence it updates this note and ends `phase-complete` for the composer-seat codex pass, then the practitioner's Task 3.4 smoke (MUST-PAUSE). Files changed this leg: `desktop/src/scribe_desktop/ui/practitioner.py`, `desktop/tests/test_ui_screens.py`, `docs/security/threat-model.md`, this plan. No commit made by the architect. Last plan sync: 2026-09-16 13:05 (this bullet).**
- **PHASE 3 ARCHITECT, BUILD LEG (2026-09-16 12:54, run=stage-3, leg stage-3-arch-d1, Claude Code `claude-fable-5-1`; ARCHITECT MODE — ONE delegation to the `loop-delegate` (`claude-opus-5` by agent-definition pin; the Agent tool's result exposed NO `modelUsage` field, so the resolved tier rests on the preflight harness probe h1 — flagged for the composer, not a mismatch); records in `.cursor/loops/stage-3-delegation.stage-3-arch-d1.log`) — Tasks 3.1, 3.2 and 3.3 BUILT and marked 🟨 (records on the tasks); NOT yet verified (verify=composer); handing off `reason=composer-run`; NO open gate (no CRIT/HIGH, no Defer/Accept, nothing production-impacting — no dependency, env var, migration or deploy config; one LOW note recorded on Task 5.3 rather than a new task, because it is inside that task's scope).** Files changed this leg: `desktop/src/scribe_desktop/ui/practitioner.py` (new; delegate), `ui/models.py`, `ui/main_window.py`, `desktop/tests/test_ui_screens.py` (delegate), `desktop/tests/test_ui_models.py` (delegate), `docs/security/threat-model.md`, `docs/security/retention-schedule.md`, `docs/security/data-flow-map.md`, `docs/design-system.md`, `AGENTS.md`, `CHANGELOG.md`, this plan. Verified by the architect itself: the delegate's diff read line by line against the spec — the D15 sequence verbatim; two spec defects the delegate surfaced and resolved with the smallest reading, both accepted (`task.start()` before `_update_controls()` because `is_busy` reads `QThread.isRunning()`; a test-side readiness provider without the shipped-identity pin because the stub embedder's id is not the shipped one); one hardening edit by the architect after verification (an explicit `Callable` annotation on `_readiness_provider`); no line over 100 characters in the five touched Python files (grep); every doc claim traced to its call site. NOT run (no grant): ruff, mypy, pytest. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_ui_screens.py tests/test_ui_models.py`, then the full suite in `desktop/` — baseline ruff clean / mypy 33 files / pytest 1636; expected: ruff clean, mypy 34 files (the new module), pytest 1659 passed = 1636 + 13 (`TestPractitionerScreen`, 6 Windows-only — all run on this host) + 5 (`TestMainWindow`, 1 Windows-only) + 5 (`TestPractitionerReportLines`, 2 Windows-only). Risks the composer should watch: (1) mypy strictness on `ui/practitioner.py` — the `work()` closure's `del pcm`, `embedding_dim=len(vector)` over `Any`, the `Signal(object)` class attribute (the `TaskThread` precedent) and the lambda default for `_readiness_provider`; (2) `test_enrolment_saves_profile_under_the_lease` asserts the busy widget states right after `on_record()` — they are set synchronously after `task.start()` and persist until the QUEUED handler runs, so they do not depend on the worker still running; (3) `test_startup_with_a_profile_stays_on_the_microphone_tab` reads the real speaker-model presence and admits both outcomes; (4) `test_status_and_app.py::test_window_offscreen_smoke` now constructs the Practitioner tab over the DEFAULT profile root with a real controller — on this host the agent-visible `%LOCALAPPDATA%`; (5) the microphone screen's 5 s model-report timer now performs one DPAPI profile read per tick on the GUI thread (a few ms) — if the practitioner's smoke shows sluggishness, cache the readiness. On green: mark 3.1–3.3 🟩 and progress 44% → 51% (20 of 39 tasks), then `/review-loop` from round 25 (max existing round 24) over the Phase 3 diff (cap 3), then end `phase-complete` for the composer-seat codex pass; Task 3.4 (the practitioner's live smoke — fresh start → banner → consent → enrol → a mock consultation → auto-confirmed role → Generate) is the MUST-PAUSE after that. Placement note: this bullet sits at the TOP of the handoff note, the file's standing newest-first order for executor records (the spawn's "below the COMPOSER RUN-STATE bullet" cannot hold in this file's order, where that bullet is last; it is unchanged). Last plan sync: 2026-09-16 12:54 (this bullet).**
- **TASK 2.6 ARCHITECT-EXECUTOR, REVIEW LEG (2026-09-16 09:53, run=stage-2, leg stage-2-exec-c13-review-2.6) — Task 2.6 🟩 (composer-verified GREEN 09:51: ruff clean, mypy 33 files, `tests/test_attribution.py` 51 passed, full suite 1636; progress 44%); `/review-loop` ROUND 22 (1 of cap 3) over the Task 2.6 diff: 0 CRIT / 0 HIGH / 0 MED / 0 LOW — zero actionable findings, LOOP CONVERGED at round 1; Review History integrity: OK; no gate fired; no code changed, so no suite is owed. TASK 2.6 COMPLETE on the executor's side; no `DELEGATION:` record (nothing delegated). Immediate next action: (1) the COMPOSER-seat codex confirmation round over the Task 2.6 diff (`git diff 4caef36` — five files); the executor is resumed to verify any finding; peer round numbering continues at 23; (2) the composer's commit; (3) Phase 3 (Tasks 3.1–3.4, UI-bearing; the live smoke at 3.4 is a MUST-PAUSE). Owed by Phase 3 (unchanged): the benchmark blocker wiring and `closeEvent` refusal (3.1/3.2), the flow-8 pre-promotion sentence (3.3), the microphone screen's speaker-model line (3.2). Files changed this leg: this plan only. No commit made by the executor. Last plan sync: 2026-09-16 09:53 (this bullet).**
- **TASK 2.6 ARCHITECT-EXECUTOR, BUILD LEG (2026-09-16 09:48, run=stage-2, leg stage-2-exec-c12-task-2.6, Claude Code `claude-fable-5-1`; architect mode on, NO delegation — a one-branch change plus test retargeting is below the delegation bar, so no `DELEGATION:` record was written) — Task 2.6 BUILT and marked 🟨 (record on the task); NOT yet verified (verify=composer); handing off `reason=composer-run`; NO open gate.** Built: `attribute_speakers`' matched branch labels the whole remainder `speaker_2` (D13 as amended; the remainder is no longer 2-means-split), with the function, module and constant docstrings updated; four test expectations retargeted across `test_attribution.py` and `test_speaker_eval.py` (one now-unused `SPEAKER_3` import dropped there; the `:399` fixture and the `:578` literal keep their label-name-independence role); `docs/security/data-flow-map.md` flow 7 and the CHANGELOG entry state the rule. Verified by the architect itself: every hunk re-read; no line over 100 characters (grep); `SPEAKER_3` still defined, exported and used by the fixture and a negative assertion; `_cluster_embeddings` still used by the zero-match branch and `label_speakers`. NOT run (no grant): ruff, mypy, pytest. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_attribution.py`, then the full suite — baseline 1636; expected 1636 passed (no item added or removed), mypy 33 files. On green: mark 2.6 🟩 and update the progress line, then `/review-loop` from round 22 (max existing round 21) over the Task 2.6 diff (cap 3), then `phase-complete` for the composer-seat codex confirmation. Files changed this leg: `desktop/src/scribe_desktop/transcription.py`, `desktop/tests/test_attribution.py`, `desktop/tests/test_speaker_eval.py`, `docs/security/data-flow-map.md`, `CHANGELOG.md`, this plan. No commit made by the executor. Last plan sync: 2026-09-16 09:48 (this bullet).**
- **PHASE 2 EXECUTOR, PEER ROUND 20 FIX LEG (2026-09-16 09:22, run=stage-2, legs c10-verify-r20 → c11-fix-r20; the run was paused 08:53 and resumed by a new composer session 09:16) — codex confirmation round 20 confirmed all five round-19 fixes and returned one new MED, PR-REG-004: `clinician_pcm` ran OUTSIDE the leave-one-out pool's guard, and the label parser admits any finite timestamp, so an `end` of `1e305` s overflows the sample index (`int(inf)` → `OverflowError`) and aborted the whole run before any recording was scored — my round-19 note that extraction "cannot raise" was wrong. LEG 1 verified it MED (behavioral, in scope, not production-impacting; the evaluation path compares floats and would never report such a pair itself); the composer routed it to fixing; LEG 2 APPLIED it: `_leave_one_out_pool` now returns `(pool, failures)` — read/parse failures still skip silently (the evaluation reports them once), extraction failures are recorded by WAV name → exception type — and `main` prints each once as `[error] <wav>: enrolled pool preparation failed - <Type>` and counts it towards the non-zero exit; +1 regression test (`test_an_oversized_label_timestamp_does_not_abort_the_leave_one_out_run`). Round 20 closed (1 of 1 Applied); the round-20 Review History line updated in place; Fix-delta self-check PASS. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest tests/test_speaker_eval.py`, then the full suite — baseline 1635; expected 1636 passed, mypy 33 files; then the codex confirmation round 21 (the executor is resumed to verify). Files changed this leg: `desktop/src/scribe_desktop/speaker_eval.py`, `desktop/tests/test_speaker_eval.py`, this plan. No commit made by the executor. Last plan sync: 2026-09-16 09:22 (this bullet).**
- **PHASE 2 EXECUTOR, LINT LEG (2026-09-16 03:46, run=stage-2, leg stage-2-exec-c9-lint) — composer suites after the round-19 fixes: mypy clean over 33 files, pytest 1635 passed (the expected 1636 counted the Windows-only real-loader case twice — 1635 is the right number), ruff 6 errors in `desktop/tests/test_ui_models.py` only: the `windows_only` marker sat above the imports (E402 ×5) and one monkeypatch line ran to 103 characters (E501). Fixed: the marker now follows the imports (the file's original layout) and the line is wrapped through a local; nothing else changed. NEEDS THE COMPOSER: `ruff check .`, `pytest tests/test_ui_models.py`, then the full suite — expected ruff clean, mypy unchanged, pytest 1635 passed; then the codex confirmation round 20. Files changed this leg: `desktop/tests/test_ui_models.py`, this plan. No commit made by the executor. Last plan sync: 2026-09-16 03:46 (this bullet).**
- **PHASE 2 EXECUTOR, PEER ROUND 19 FIX LEG (2026-09-16 03:42, run=stage-2, legs c7-verify-r19 → c8-fix-r19) — codex `gpt-6-astra` peer round 19 returned 0 CRIT / 2 HIGH / 1 MED / 2 LOW over the Phase 2 diff plus a Task 2.6 assessment (option (a) recommended; left to the composer at the phase boundary); LEG 1 verified all five against the code (PR-HIGH-004 and PR-HIGH-005 downgraded to MED with evidence — on every hidden-notice path no note can be produced from that view, and an unusable blob lands on the pre-enrolment manual path, so neither is a Critical-Constraint breach; PR-REG-003 confirmed MED; the two LOWs confirmed docs-only); the composer routed all five to fixing under `gates=fix-biased`; LEG 2 APPLIED them one at a time (see the round's `/fix notes`): the D2 status line moved OUTSIDE the generation group and is rendered by `show_document` on both entry points (recovered view and failed-config view included; every Generate gate untouched; +5 offscreen tests, 3 retargeted); readiness presence is now `load_profile`'s own verdict, never the D10 stat (`profile_present` dropped from `ui/models.py`; +4 tests, 2 reshaped, one Windows-only through the real loader); the leave-one-out pool skips any exception and the per-recording enrolment step is isolated with type-only reporting and error accounting (+2 tests); the one-cluster wording corrected at the five sibling sites; the window-bound wording corrected at four sites including the pre-existing shorthand. No dependency added; no Generate gate, segmentation or buffering changed. Fix-delta self-check PASS. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest -q` in `desktop/` — baseline 1625; expected 1636 passed (+5 `test_ui_screens.py`, +4 `test_ui_models.py` counting the parametrised pair and the Windows-only case, +2 `test_speaker_eval.py`), mypy 33 files; then the codex confirmation round 20 (the executor is resumed to verify). The round-19 Review History line stays the composer's (pending). Files changed this leg: `desktop/src/scribe_desktop/ui/models.py`, `ui/transcript.py`, `speaker_eval.py`, `desktop/tests/test_ui_models.py`, `test_ui_screens.py`, `test_speaker_eval.py`, `docs/security/threat-model.md`, `docs/security/data-flow-map.md`, `docs/testing/speaker-measurement.md`, `CHANGELOG.md`, this plan. No commit made by the executor. Last plan sync: 2026-09-16 03:42 (this bullet).**
- **PHASE 2 EXECUTOR, CONVERGENCE (2026-09-16 02:27, run=stage-2, leg stage-2-exec-c6-review) — round-17 fixes composer-verified GREEN (02:24: ruff clean, mypy 33 files, pytest 1625); `/review-loop` ROUND 18 (2 of cap 3, confirmation): 0 CRIT / 0 HIGH / 0 MED / 0 LOW — full re-review of every changed file, the Post-Fix Regression Check over the four round-17 hunks found no affected caller, the missed-issue pass found nothing; Review History integrity: OK (the round-18 line parses; history checker 18 + 18); no gate fired; no code changed this round, so no suite is owed. LOOP CONVERGED at round 2 of 3. PHASE 2 COMPLETE on the executor's side: Tasks 2.1–2.5 🟩; Task 2.6 (the D13 remainder split — one other voice becomes `speaker_2` + `speaker_3` whenever it has two or more segments; MED, Include in plan, auto-disposed) stays 🟥 pending its scoped `/review-plan`. Immediate next action: (1) the COMPOSER-seat cross-family codex peer pass (`gpt-6-astra`) over the Phase 2 diff — `git diff 58e7bad` plus the new `desktop/tests/test_attribution.py`; the executor is resumed per round to verify and fix; peer round numbering continues at 19; put Task 2.6's question to the peer explicitly (options (a) whole remainder = `speaker_2` until D-S1, (b) keep the split as the documented residue, (c) a single-voice test = D-S1); (2) the composer's commit; (3) the scoped `/review-plan` over Task 2.6 at the phase boundary; (4) Phase 3 (Tasks 3.1–3.4, UI-bearing — the Practitioner tab, with the live smoke at 3.4) under this run. Owed by Phase 3 (unchanged, recorded on Task 3.1): the benchmark blocker wiring, `closeEvent` refusing while enrolling, the flow-8 pre-promotion sentence; and by Task 3.2: the microphone screen's speaker-model line in `model_report_lines` (D2's other surface, deliberately left to 3.2 in Phase 2). Files changed this leg: this plan only. No commit made by the executor. Last plan sync: 2026-09-16 02:27 (this bullet).**
- **PHASE 2 EXECUTOR, REVIEW LEG (2026-09-16 02:22, run=stage-2, leg stage-2-exec-c5-review) — Tasks 2.1–2.5 🟩 (composer-verified GREEN 02:17: ruff clean, mypy 33 files, pytest 1624; progress 41%); Task 2.6 stays 🟥 pending its scoped `/review-plan` (the composer's, at the phase boundary); `/review-loop` ROUND 17 (1 of cap 3) DONE: 0 CRIT / 0 HIGH / 0 MED / 4 LOW, all Fix-now and APPLIED one per file (LOW-001 the pipeline's identity guard now compares `model_sha256` too; LOW-002 `words_have_text` is the single has-text definition, `segment_has_text` delegates and the pipeline calls it; LOW-003 `_clear_auto_confirm()` replaces three copy-pasted resets on the Transcript screen; LOW-004 `EnrolmentInputs` fields are `repr=False` so the harness's inputs never render the vector) with three test pins (+1 parametrised case in `test_attribution.py`, +2 assertions there, +1 assertion in `test_speaker_eval.py`); Review History integrity: OK (the round-17 line parses); Fix-delta self-check PASS; no gate fired (LOW, in-scope, not production-impacting — headless auto-disposable). Code changed, so handing off `reason=composer-run`: NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest -q` in `desktop/` (expected: ruff clean, mypy 33 files, pytest 1625 passed = 1624 + 1 parametrised case). On green the executor runs round 18 (the confirmation round with the Post-Fix Regression Check over the four round-17 hunks); at convergence it runs the history checker and ends `phase-complete` for the composer-seat codex peer pass (point the peer at Task 2.6's question). Loop-log note: this executor has no grant to append a `ROLE: round` line to the run logs — the composer owns that record for round 17. Files changed this leg: `desktop/src/scribe_desktop/transcription.py`, `ui/transcript.py`, `speaker_eval.py`, `desktop/tests/test_attribution.py`, `test_speaker_eval.py`, this plan. No commit made by the executor. Last plan sync: 2026-09-16 02:22 (this bullet).**
- **PHASE 2 EXECUTOR, RESIDUE LEG (2026-09-16 02:15, run=stage-2, leg stage-2-exec-c4-fix) — composer suites after the c3 fixes: mypy clean over 33 files, pytest 1623 passed / 1 FAILED (`test_speaker_eval.py::TestEvaluateRecordingEnrolled::test_a_profile_of_the_other_voice_confirms_the_patient_visibly`, `NameError`) and ruff F821 at the same line: the corrected expectation named `SPEAKER_3`, which the module imported nowhere (leg c3's miss — the pipeline test module imports it, the harness module did not). Fixed by adding `SPEAKER_3` to the module's existing `scribe_desktop.transcription` import block; the constant is used by that one assertion only (grep). Nothing else changed. NEEDS THE COMPOSER: `ruff check .`, `pytest tests/test_speaker_eval.py`, then the full suite — expected ruff clean, mypy unchanged, pytest 1624 passed. On green: mark 2.1–2.5 🟩 (2.6 stays 🟥 pending its scoped review), then `/review-loop` from round 17. Files changed this leg: `desktop/tests/test_speaker_eval.py`, this plan. No commit made by the executor. Last plan sync: 2026-09-16 02:15 (this bullet).**
- **PHASE 2 EXECUTOR, FIX LEG (2026-09-16 02:15, run=stage-2, leg stage-2-exec-c3-fix) — composer suites after the build: ruff clean, mypy 2 errors, pytest 1608 passed / 16 FAILED. Three root causes, each fixed at the root, plus the mypy rename; NO production behaviour changed; handing off `reason=composer-run`; ONE scope-expansion candidate recorded (Task 2.6, MED, Include in plan — auto-disposed under the headless rule, the scoped `/review-plan` owed to the composer).** (1) TEN `test_attribution.py` pipeline tests failed at store creation with "session_id must be exactly 32 lowercase hex chars": the fixture's session-directory names `ag`…`ao` contain non-hex letters (`aa`…`af` passed for that reason alone) — renamed to `b1`…`b9` and `_segment_pcms` now asserts its name is hex. (2) The remainder split (failures 1 and 13): the two unmatched segments of one tone are NOT byte-identical slices (the VAD's frame-aligned padding differs by a few ms), so D13's 2-means over the remainder returns two clusters and labels them `speaker_2` / `speaker_3` — this is what D13 literally prescribes and what `_cluster_embeddings` does on the no-profile path for any two non-identical segments; the CODE follows the plan, the two tests' expectations assumed the degenerate branch and were corrected to `speaker_2, speaker_1, speaker_3` with the reasoning in their docstrings, and `attribute_speakers`' docstring now states the residue plainly. The behaviour question this exposes — one other voice always becomes two labels — is recorded as Task 2.6 (MED, options (a)/(b)/(c), recommended Include in plan; built as (b), D13 as written, no design changed by the executor). (3) The three `TestMainEnrolled` failures ("no speech detected in the enrolment audio"): `main` correctly hands the RUN's VAD to `enrol` (the same VAD the recordings are segmented with), and the tests' inert stub hears nothing — a new `_AmplitudeVad` stub replaces it in that class's `_capture`; the harness is unchanged. (4) Failure 12: `cluster_metrics` maps the ONE predicted cluster to the true role it covers longest (label-agnostic by design — the patient's 3 s of 5 → 0.6), so the expectation of 0.4 was the test's wrong assumption; PR-MED-014 requires only that the all-matched output be SCORED, and the false positive is exposed by the 1/2 cluster count and the WRONG auto-confirm verdict — the test now pins the mapping `speaker_1 -> patient` and 0.6 with that comment. (5) mypy: the aggregate counter in `render_report` reused the per-row `auto` name (`RoleOutcome | None`) — renamed `auto_counts`. Every hunk re-read; no line over 100 characters (grep). NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest -q` in `desktop/` — expected ruff clean, mypy 33 files clean, pytest 1624 passed (1608 + the 16 fixed; collected count unchanged). On green: mark 2.1–2.5 🟩 (2.6 stays 🟥 pending its scoped review), then `/review-loop` from round 17, then `phase-complete` for the composer-seat codex peer pass — the peer should be pointed at Task 2.6's question explicitly. Files changed this leg: `desktop/src/scribe_desktop/speaker_eval.py` (the rename only), `desktop/src/scribe_desktop/transcription.py` (a docstring), `desktop/tests/test_attribution.py`, `desktop/tests/test_speaker_eval.py`, this plan. No dependency added; nothing production-impacting; no commit made by the executor. Last plan sync: 2026-09-16 02:15 (this bullet).**
- **PHASE 2 EXECUTOR, BUILD LEGS (2026-09-16 02:10, run=stage-2, legs stage-2-exec-c1 → c2-resume, Claude Code `claude-fable-5-1` via the spawn wrapper; leg c1 died on the account's session limit at 23:25 after the build and tests were on disk but before any plan record — this resume wrote the records from disk, nothing was re-done) — Tasks 2.1, 2.2, 2.3, 2.4 and 2.5 BUILT and marked 🟨 (file-by-file records under each task); NOT yet verified (verify=composer); handing off `reason=composer-run`; NO open gate.** Built: `desktop/src/scribe_desktop/transcription.py` (the two pipeline parameters, `attribute_speakers`, `enrolled_cluster`, `_cosine`, `segment_has_text`, `SPEAKER_3`, `MIN_ATTRIBUTION_PCM_BYTES`, the three `TranscriptDocument` fields + validators, `recover_session_transcription`'s pass-through), `speaker_embedding.py` (`shipped_embedder_identity`; the protocol docstring states the one-frame minimum), `ui/models.py` (`AttributionReadiness`, `attribution_readiness`, `attribution_inputs`, the four D2 reason constants, both factories' `attribution=` seam), `ui/transcript.py` (the D4 line + `change`, the D2 status line, `role_auto_confirmed`), `speaker_eval.py` (the enrolled condition, `--enrolment`, leave-one-out, the replay provider, report columns), `scripts/measure-speakers.py`, `docs/testing/speaker-measurement.md`, `docs/security/threat-model.md` (surfaces 6–8), `docs/security/data-flow-map.md` (flow 7 + the logs non-flow), `CHANGELOG.md`; tests — NEW `desktop/tests/test_attribution.py`, additions to `test_speaker_eval.py` (two existing CLI tests adapted: stubs take the new keyword and `build_speaker_embedder` is stubbed), `test_ui_models.py`, `test_ui_screens.py`. Verified by the executor itself: every edited region re-read (the pipeline loop and the screen's populate path re-read again at this resume); no line over 100 characters in any touched Python file (grep, `^.{101,}$`, zero matches over `src/` and the four test modules); each new test traced against the code path it pins; the label-scheme cases hand-checked against D13. NOT run (no grant): ruff, mypy, pytest. NEEDS THE COMPOSER: `cd desktop && ../.venv/Scripts/ruff.exe check . && ../.venv/Scripts/mypy.exe && ../.venv/Scripts/pytest.exe -q` — baseline 1524 passed / ruff clean / mypy clean over 33 files; expected: mypy unchanged (no new source module; watch `ui/models.py`'s `AttributionInputs` alias and `transcription.py`'s `np: Any | None` flowing into `Any` parameters), pytest ≈ 1524 + ~48 (`test_attribution.py`: 9 pure attribute + 6 enrolled-cluster + 10 document + 15 pipeline, several parametrised) + 21 (`test_speaker_eval.py`) + 13 (`test_ui_models.py`, two parametrised ×4 and ×2) + 11 (`test_ui_screens.py`, one parametrised ×3) ≈ 1617 collected, none of the existing items changed except the two adapted CLI stubs. Risks the composer should watch for: (1) `test_attribution.py::TestAttributedPipeline::test_the_remainder_splits_into_speaker_2_and_speaker_3` and `test_zero_matches_runs_ordinary_two_means…` depend on the spectral 2-means separating 220/2600/700 Hz tones the way `test_speaker_labels_survive_batching` already proves for 220/2600 — if 700 Hz clusters with 220 Hz, the first test's expectation (`speaker_3`) is the fixture's, not the code's; (2) the Windows harness test `test_a_second_pass_that_segments_differently_is_a_harness_fault` counts VAD frames (`len(pcm) // FRAME_BYTES + 8`) to flip the second pass — if `segment_session_audio` calls the VAD more than once per frame the flip lands mid-first-pass and the test fails for the fixture's reason; (3) offscreen Qt: `change_role_button.click()` needs the button ENABLED (it is, once the config loads) — a disabled click is a silent no-op; (4) `test_ui_models.py` monkeypatches `models.load_profile` / `speaker_embedder_available` / `build_speaker_embedder` by module attribute — they are imported by name into `ui.models`, so the patches bind. On green: mark 2.1–2.5 🟩, then `/review-loop` from round 17 (max existing round 16), then `phase-complete` for the composer-seat codex peer pass. Decisions taken inside the plan's latitude (each recorded on its task): the three document fields are all-or-none; `has_text` is threaded into the labelling; the enrolled pass replays the plain pass's windows instead of paying Whisper twice; the readiness probe compares the pin while the worker compares the bytes, with the document-driven "did not run" line covering the gap; the microphone screen's model-presence line is left to Task 3.2 as Task 0.6 recorded. No new dependency, env var, migration or deploy config; nothing production-impacting; no MUST-PAUSE candidate accumulated; no commit made by the executor. Last plan sync: 2026-09-16 02:10 (this bullet).
- **PHASE 1 EXECUTOR, PEER ROUND 15 FIX LEG (2026-09-15 22:55, run=stage-1, legs b8-verify-r15 → b9-fix-r15) — codex confirmation round 15 confirmed PR-MED-019 and PR-REG-002 complete and PR-HIGH-003 complete in production code, and returned 0 CRIT / 0 HIGH / 1 MED / 1 LOW new; LEG 1 verified both (PR-MED-020 kept at MED: the sole cancel poll sat before the queue wait, so a Cancel from the target-reaching progress callback or during shutdown returned PCM; PR-LOW-024 confirmed LOW test-only: the consent regression test rejected a non-sentinel value and passed without the flag); the composer routed both to fixing under `gates=fix-biased`; LEG 2 APPLIED them (see the round's `/fix notes`): `raise_if_cancelled()` before both decision exits and after shutdown on the success path, failure precedence kept (+2 deterministic tests); the consent test now rejects a sentinel and asserts pydantic's `input_value` metadata is absent, the model-validator and vector tests gain the same representation-independent assertion, and a structural test pins the flag on both models (+1 test, one renamed). No custody or capture ordering otherwise changed; no dependency added. Every hunk re-read; no long lines. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest -q` in `desktop/` — baseline 1521; expected 1524 passed (+2 `test_enrolment.py`, +1 `test_practitioner_profile.py`), mypy 33 files; then the codex confirmation round 16 (the executor is resumed to verify). The round-15 Review History line stays the composer's (pending). Files changed this leg: `desktop/src/scribe_desktop/enrolment.py`, `desktop/tests/test_enrolment.py`, `desktop/tests/test_practitioner_profile.py`, this plan. No commit made by the executor. Last plan sync: 2026-09-15 22:55 (this bullet).**
- **PHASE 1 EXECUTOR, PEER ROUND 14 FIX LEG (2026-09-15 22:40, run=stage-1, legs b6-verify-r14 → b7-fix-r14) — codex `gpt-6-astra` peer round 14 returned 0 CRIT / 1 HIGH / 1 MED / 1 LOW over the Phase 1 diff; LEG 1 verified all three against the code (PR-HIGH-003 downgraded to MED with evidence: reachable only through a chained traceback rendering no shipped path produces, fixable by pydantic's own `hide_input_in_errors`; PR-MED-019 confirmed MED; PR-REG-002 confirmed LOW docs-only); the composer routed all three to fixing under `gates=fix-biased`; LEG 2 APPLIED them one at a time (see the round's `/fix notes`): both profile models hide their input in validation errors and the loader's `malformed` branch is `from None` with a structural count-and-locations message (+5 tests: three direct-error renderings, the loader's full rendering, the no-cause pin); `record_enrolment` holds the first capture failure in a slot written before the wake-up item and checks it before both decision exits and again after stream shutdown on the success path (+3 deterministic tests: failure behind the target block, overflow behind it, failure during shutdown); the custody wording in the module docstring, `_key_present` and threat-model surface 1 is scoped to an existing reusable key with the three permitted residue states named. No custody ordering changed; no dependency added. Fix-delta self-check PASS (9 hunks, 5 files); no long lines. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest -q` in `desktop/` — baseline 1514; expected 1522 passed (+5 in `test_practitioner_profile.py`, +3 in `test_enrolment.py`), mypy 33 files; then the codex confirmation round 15 (the executor is resumed to verify). The round-14 Review History line stays the composer's (pending). Files changed this leg: `desktop/src/scribe_desktop/practitioner_profile.py`, `enrolment.py`, `desktop/tests/test_practitioner_profile.py`, `test_enrolment.py`, `docs/security/threat-model.md`, this plan. No commit made by the executor. Last plan sync: 2026-09-15 22:40 (this bullet).**
- **PHASE 1 EXECUTOR, CONVERGENCE (2026-09-15 21:10, run=stage-1, leg stage-1-exec-b5-review; the b4 activation died on the account's session limit before starting round 13, nothing on disk changed) — round-12 fixes composer-verified GREEN (ruff clean, mypy 33 files, pytest 1514); `/review-loop` ROUND 13 (2 of cap 3, confirmation): 0 CRIT / 0 HIGH / 0 MED / 2 LOW, both 🆕 doc-only (LOW-001 the D15 blocker refusal was described as live before Phase 3 registers the benchmark — threat model §5 and CHANGELOG now say the hook ships and the registration is Phase 3's; LOW-002 the retention row's over-broad tripwire claim, now the documented limit) — APPLIED; Post-Fix Regression Check over the four round-12 hunks: none; Review History integrity: OK; Fix-delta self-check PASS; LOOP CONVERGED at round 2 of 3; no gate fired; no code changed since the last green run (docs and this plan only), so no suite is owed. PHASE 1 COMPLETE on the executor's side. Immediate next action: (1) the COMPOSER-seat cross-family codex peer pass (`gpt-6-astra`) over the Phase 1 diff — `git diff f1471f1` plus the six new files (`speaker_embedding.py`, `practitioner_profile.py`, `enrolment.py` and their tests); the executor is resumed per round to verify and fix; peer round numbering continues at 14; (2) the composer's commit; (3) Phase 2 (Tasks 2.1–2.5) under this run. Owed by Phase 3 (recorded on Task 3.1): `controller.set_enrolment_blocker(...)` wired in `MainWindow` for the benchmark, `closeEvent` refusing while enrolling (Task 3.2), and the `data-flow-map.md` flow 8 sentence about the pre-promotion candidate (Task 3.3). Files changed this leg: `docs/security/threat-model.md`, `docs/security/retention-schedule.md`, `CHANGELOG.md`, this plan. No commit made by the executor. Last plan sync: 2026-09-15 21:10 (this bullet).**
- **PHASE 1 EXECUTOR, REVIEW LEG (2026-09-15 18:40, run=stage-1, leg stage-1-exec-b3-review) — Tasks 1.1–1.4 🟩 (composer-verified GREEN 18:17: ruff clean, mypy 33 files, pytest 1513); `/review-loop` ROUND 12 (1 of cap 3) DONE: 0 CRIT / 0 HIGH / 1 MED / 3 LOW, all Fix-now and APPLIED one at a time (MED-001 the load-time smoke input is now a 440 Hz tone, not silence; LOW-001 the mock/module PCM-retention docstrings and their test; LOW-002 an uninspectable profile key refuses the save typed, new test; LOW-003 the custody-ordering wording in the module docstring and the threat model); Review History integrity: OK (the round-12 line parses); Fix-delta self-check PASS; no gate fired (LOW/MED, in-scope, not production-impacting — headless auto-disposable). Code changed, so handing off `reason=composer-run`: NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest -q` in `desktop/` (expected: ruff clean, mypy 33 files, pytest 1514 passed = 1513 + 1 new profile test; `test_records_lengths_never_bytes` renamed to `test_holds_only_the_callers_map_and_the_lengths_it_embedded`). On green the executor runs round 13 (the confirmation round with the Post-Fix Regression Check over the seven round-12 hunks); at convergence it runs the history checker and ends `phase-complete` for the composer-seat codex peer pass. Loop-log note: this executor has no grant to append a `ROLE: round` line to the run logs (only the `VERIFY_OK` emit is granted) — the composer owns that record for round 12. Files changed this leg: `desktop/src/scribe_desktop/speaker_embedding.py`, `practitioner_profile.py`, `desktop/tests/test_speaker_embedding.py`, `test_practitioner_profile.py`, `docs/security/threat-model.md`, this plan. No commit made by the executor. Last plan sync: 2026-09-15 18:40 (this bullet).**
- **PHASE 1 EXECUTOR, FIX LEG (2026-09-15 18:16, run=stage-1, leg stage-1-exec-b2-fix) — composer suites after the build: ruff clean, mypy clean over 33 files, pytest 1512 passed / 1 FAILED (`test_enrolment.py::TestEnrol::test_cancelling_vectors_are_refused` did not raise). Diagnosis: the FIXTURE was wrong, not `enrol()` — the `_Flip` double negated the mock's digest-derived vector, but the two VAD segments' bytes differ (different silence padding either side of each loud stretch), so +v1 − v2 never cancelled; `enrol()` already refuses a zero-norm mean. Fix: `_Flip` now returns ±e1 (the SAME unit vector, alternating sign) and the test asserts the even segment count it relies on and that both segments were embedded. No production code changed. Handing off `reason=composer-run` for `pytest tests/test_enrolment.py` + the full suite (expected 1513 passed); on green mark 1.1–1.4 🟩 and start `/review-loop` at round 12.**
- **PHASE 1 EXECUTOR (2026-09-15 18:10, run=stage-1, leg stage-1-exec-b1, Claude Code `claude-fable-5-1` via the spawn wrapper) — Tasks 1.1, 1.2, 1.3 and 1.4 BUILT and marked 🟨; NOT yet verified (verify=composer); handing off `reason=composer-run` for the suites; NO open gate.** Built (file-by-file records under each task): three new modules `desktop/src/scribe_desktop/speaker_embedding.py`, `practitioner_profile.py`, `enrolment.py`; `session.py` (the D15 `EnrolmentLease` / `begin_enrolment` / `end_enrolment` / `enrolling` / `set_enrolment_blocker`, `start`/`resume` refusals); `session_store.py` (`SESSION_KEY_DESCRIPTION`, the `description` keyword on both DPAPI helpers, the unwrap VERIFIES the stored description — session callers unchanged); `logging_setup.py` (three profile markers × three forms); `ui/microphone.py` (poll tick keeps the monitor closed while enrolling; benchmark refuses while enrolling); `ui/models.py` (`SessionControllerLike.enrolling`); `scripts/setup-models.py` (imports the pin constants from the module — single source); `scripts/speaker-embedding-smoke.py` (private front-end / load contract / `SpeakerModelError` RETIRED; imports the module's; Povey default, `--window hamming` kept); `scripts/README.md`; tests — NEW `desktop/tests/test_speaker_embedding.py`, `test_practitioner_profile.py`, `test_enrolment.py`; `test_setup_scripts.py` (the moved front-end and load-contract tests removed, identity pins added, `sys` import dropped); `test_ui_screens.py` (`FakeController.enrolling`, two poll-tick/benchmark tests); docs — `docs/security/retention-schedule.md` (profile row DRAFT + the model-cache row), `docs/security/threat-model.md` (a Phase 1 DRAFT section, five surfaces), `docs/security/data-flow-map.md` (the logs non-flow names the profile markers), `CHANGELOG.md`. Verified by the executor itself: every edited region re-read; no line over 100 characters in any touched desktop file (grep); each new test traced against the code path it pins; import order checked against ruff's isort conventions by eye. NOT run (no grant): ruff, mypy, pytest. NEEDS THE COMPOSER: `cd desktop && ../.venv/Scripts/ruff.exe check . && ../.venv/Scripts/mypy.exe && ../.venv/Scripts/pytest.exe -q` — baseline 1431 passed / ruff clean / mypy clean over 30 files; expected: mypy over 33 files, pytest = 1431 − 17 moved-or-superseded smoke items (7 front-end + 4 load-contract + 2 embed + 4 parametrised accept-one-vector + 5 parametrised refuse + 1 feature-dim = 23 collected items removed, 2 identity/help tests added → −21 in `test_setup_scripts.py`) + ~50 in `test_speaker_embedding.py` + ~32 in `test_practitioner_profile.py` (Windows) + ~18 in `test_enrolment.py` + 2 in `test_ui_screens.py` ≈ 1510 collected; the real-model class in `test_speaker_embedding.py` skips when the cache is not visible. Risks the composer should watch for: (1) mypy strictness on the three new modules (numpy arrays typed `Any`, lazy imports — the `speech.py` pattern); (2) `test_enrolment.py`'s threaded capture tests (a bounded 5 s open-wait and 10 s join; a slow host could make `test_queue_overflow_is_a_capture_error` racy — the queue bound is monkeypatched to 2); (3) `test_practitioner_profile.py::TestCustody::test_delete_unlinks_the_key_first` monkeypatches `Path.unlink` globally for the delete call only. On green: mark 1.1–1.4 🟩, then `/review-loop` from round 12 (max existing round 11), then `phase-complete` for the composer-seat codex peer pass. Open questions: none that block. Decisions taken inside the plan's latitude (recorded on the tasks): the benchmark refusal in `begin_enrolment` is through a registered blocker (the controller cannot see the UI `TaskThread`; wiring owed at Task 3.1/3.2, noted there); `load_profile` raises `ProfileUnusableError(reason="model")` on a model mismatch and the CONSUMER treats it as absent (reconciles Task 1.2's "naming which" with D3's "ABSENT"); the smoke's default window moved to the shipped Povey (`--window hamming` reproduces Task 0.4); the DPAPI unwrap verifies the description (the plan asked for the parameter; verifying is what makes it a binding). No new dependency, env var, migration or deploy config; nothing production-impacting; no MUST-PAUSE candidate accumulated; no commit made by the executor. Last plan sync: 2026-09-15 18:10 (this bullet).
- **PHASE 0 EXECUTOR, TASK 0.5 (2026-09-15, run=stage-0, legs a7-build-05 → a8-review-05, Claude Code `claude-fable-5-1`) — Task 0.5 🟩 (composer-verified GREEN 17:08–17:11: ruff clean, mypy 30 files, pytest 1431 = 1427 + 4); in-session `/review-loop` CONVERGED at round 9 (2 of cap 3); TASK 0.5 COMPLETE; peer round 10 (codex `gpt-6-astra`) VERIFIED (leg 1) and its THREE docs-only LOWs APPLIED (leg 2, 2026-09-15 ~17:27: the https-every-hop claim scoped to the speaker-embedding helper in flow 9 and the setup-models docstring; the smoke's usage text, example path and missing-file remedy describe the promoted `.onnx` with the `.candidate` kept for an unpinned entry; Task 0.6 carries the verbatim PowerShell command) — awaiting the composer's suite re-run (the smoke script changed: expected unchanged clean / clean / 1431) and then peer round 11 (confirmation); NO open gate.** Rounds 8 → 9: 5 LOW (all applied: the size string derives from the int, the unverified "MIT-licensed" claim dropped, an unused `capsys` parameter removed, flow 8's pre-promotion wording, the stale "leaves in candidate mode" claim in `scripts/README.md`) → 1 doc-only LOW (a ragged wrap, applied) → converged; the composer's suite re-run after round 8 stayed green (ruff clean, mypy 30 files, pytest 1431); Review History integrity OK on both rounds. No MUST-PAUSE gate fired: no CRIT/HIGH, no Defer/Accept, nothing production-impacting (no dependency, env var, migration or deploy config; the pin is a constant). Files of this task (`git status`): `scripts/setup-models.py`, `scripts/README.md`, `desktop/tests/test_setup_scripts.py`, `AGENTS.md`, `docs/security/data-flow-map.md`, this plan — nothing else; no commit made by the executor. Immediate next action: (1) the COMPOSER-seat cross-family codex peer pass over the Task 0.5 diff (`gpt-6-astra`; the executor is resumed per round to verify and fix) and the composer's commit; (2) PAUSE for the PRACTITIONER's Task 0.6 — from a normal terminal `.venv\Scripts\python.exe scripts\setup-models.py --only speaker-embedding` verifies the existing `.onnx.candidate` against the pin and promotes it to `wespeaker-voxceleb-resnet34-LM.onnx`; confirm the printed filename and the verified digest `7bb2f06e…ec068`; (3) D-P1 `[decision]` — its inputs are on Task 0.4 (candidate 1 confirmed; gap ≈ 0.84; suggested threshold constant ≈ 0.44; the Povey-window note for Task 1.1). Then Phase 1. Earlier record of this task's build — Last completed step: Task 0.4 (practitioner-authorised, composer-run, recorded on the task 2026-09-15). Task 0.5 — code complete (file-by-file record on the task): the pin (URL / 26530309 bytes / SHA-256) in `scripts/setup-models.py`, its docstring made truthful (the entry is pinned; candidate mode kept as the generic empty-pin route), `AGENTS.md` step 3, `docs/security/data-flow-map.md` flows 8 and 9, and `desktop/tests/test_setup_scripts.py` (+4 shipped-pin tests including THE pin test; one test renamed to say it runs under the shipped pin). Files changed this leg: those five plus this plan. Verified by the executor itself: every edited region re-read; no test line over 100 characters (grep); the pinned path's behaviour traced against each new test. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest -q` in `desktop/` (baseline 1427 passed / ruff clean / mypy clean over 30 files; expected 1431 collected, mypy unchanged); on green the executor marks 0.5 🟩 and runs `/review-loop` from round 8 (max existing round 7), then ends `phase-complete` for the composer-seat codex peer pass. After that: PAUSE for the practitioner's Task 0.6 (re-run `setup-models.py --only speaker-embedding` from a normal terminal — it verifies the candidate against the pin and promotes it; confirm the printed filename and digest), then D-P1 (`[decision]`; the Task 0.4 record already carries its inputs: candidate 1 confirmed, gap ≈ 0.84, suggested threshold ≈ 0.44, the Povey-window note for Task 1.1). No new dependency, env var or persisted artefact; nothing production-impacting. Last plan sync: 2026-09-15 (this bullet).
- **PHASE 0 EXECUTOR (2026-09-06, run=stage-0, legs stage-0-exec-a1 → a2-review, Claude Code `claude-fable-5-1` via the spawn wrapper) — Tasks 0.1 and 0.3 🟩 (composer-verified GREEN 14:43–14:46: ruff clean, mypy clean over 30 files, pytest 1408 passed = 1368 + 40); in-session `/review-loop` CONVERGED at round 5 (2 of cap 3); PHASE 0's AGENT TASKS COMPLETE; peer round 6 (codex `gpt-6-astra`, first cross-family code round) VERIFIED (leg 1) and its FOUR findings APPLIED (leg 2, 2026-09-06 ~15:10) — awaiting the composer's suite re-run, then peer round 7 (confirmation); NO open gate.** Round 6 → fix leg: PR-HIGH-002 (verified MED; https enforced on every redirect hop through a custom `HTTPRedirectHandler`, the stdlib default's downgrade-following PROVEN by a socket-free test), PR-MED-018 (one-vector-per-utterance output validation, no flattening anywhere), PR-LOW-020 (one typed load boundary from the import onward, offline/UNC/is_file still first), PR-LOW-021 (the smoke docstring, D12 and Task 0.4 now say the matrix can expose but not prove front-end compatibility; the model-card recipe is recorded beside it). Files touched by the fix leg: `scripts/setup-models.py`, `scripts/speaker-embedding-smoke.py`, `desktop/tests/test_setup_scripts.py` (+19 items → 59 in the module; expected suite 1408 → 1427), this plan. NEEDS THE COMPOSER: `ruff check .`, `mypy`, `pytest -q` in `desktop/`, then the codex confirmation round 7 (the executor is resumed to verify). Earlier in this bullet — Last completed step: round 5 — 0 / 0 / 0 / 0, skew=none, after the composer's suite re-run over the round-4 fixes stayed green (ruff clean, mypy 30 files, pytest 1408 passed); Review History integrity OK on both rounds (4: 4 LOW, all Applied — LOW-001 the candidate-mode skip line names the remedy for switching candidates in `setup-models.py`, LOW-002 the cosine matrix column width floor in `speaker-embedding-smoke.py`, LOW-003 the Task 0.4 recipe quotes `WAV_FORMAT_HELP` verbatim, LOW-004 the Task 1.1 "one front-end, one load contract" sub-bullet, Include in plan as a headless auto-disposable LOW; 5: clean). No MUST-PAUSE gate fired in either round: no CRIT/HIGH, no Defer/Accept, nothing production-impacting (no new dependency — numpy and onnxruntime are already `[ml]` — no env var, no migration, no deploy config). Files of this phase (`git status`): `scripts/setup-models.py`, `scripts/README.md`, `scripts/speaker-embedding-smoke.py` (new), `desktop/tests/test_setup_scripts.py` (new), the two plans — nothing else; no commit made by the executor (composer-seat only). Current in-progress step: None. Immediate next action: (1) the COMPOSER-seat cross-family codex peer pass over this diff (`gpt-6-astra`, read-only sandbox; the executor is resumed per round to verify findings and fix) and the composer's commit; (2) then the run PAUSES for the PRACTITIONER's Task 0.4 (instructions under that task: the download URL is theirs to supply via `--candidate-url`; report URL + size + SHA-256 and the smoke's I/O signature + cosine matrix onto the task); (3) then the agent's Task 0.5 (pin URL / size / SHA-256, remove candidate mode for this entry, `AGENTS.md` step 3, `data-flow-map.md` flow 9), PAUSE for 0.6, then D-P1. Open questions carried: the candidate URL (practitioner); whether the fetched model card's front-end matches the smoke's WeSpeaker recipe (Hamming, int16 scale, CMN; Povey is one flag away) — a D-P1 fact if not. Immediate next action after review convergence: END the leg with `reason=phase-complete` — the composer pauses the run for the practitioner's Task 0.4. Open questions: (1) the candidate's download URL — the plan pinned none and the executor invented none; `SPEAKER_EMBEDDING_URL` is EMPTY and the practitioner passes `--candidate-url` at Task 0.4 (instructions written under that task); Task 0.5 records URL + size + SHA-256 from the report. (2) The front-end follows WeSpeaker's inference recipe (Hamming window, int16-scale samples, CMN); if the fetched model card states a different front-end (Povey window is one flag away; a different bin count is not), that is a D-P1 fact to record, not a fix here. No new dependency (numpy and onnxruntime are already `[ml]`), no env var, no migration, nothing production-impacting: no MUST-PAUSE candidate accumulated. Last plan sync: 2026-09-06 (this bullet).
- Last completed step: plan peer-review PASS CONVERGED at round 3 (codex `gpt-6-astra` at high, 2026-09-05 23:54 UTC: zero new build-affecting findings, one record-only LOW applied) — rounds 1–3 = 15 → 3 → 0 build-affecting, 19 findings verified and applied; see `Review History`. Before that: round 2 (11 of 15 confirmed, four completed, 3 new), round 1 (15 findings), `/review-plan` pass 1; before that `/review-plan` hardening pass 1 (2026-09-05, Claude Code `claude-fable-5-1`): recovery-path attribution added (`build_recovery_runner`), the zero-match rule under D4 fixed (D13), the label scheme fixed (D13), Phase 5 re-shaped to an explicit chooser control plus remove/move (D14, practitioner Fix-now at the gate), the R3 clarification task 5.5 added, the digest-pin fact recorded. Planning complete before that (same day; exploration scratch `explore-practitioner-profile.md` consumed).
- Current in-progress step: None.
- Immediate next action: the plan is cross-family reviewed and execution-ready. PRACTITIONER first: Task 0.2 (read and ratify the consent text in `Config / Environment / Deployment Impact`). Then `/execute-loop` from Task 0.1 (Phase 0 as one-phase-and-stop: agent tasks 0.1 + 0.3, pause for the practitioner's 0.4 and 0.6, then 0.5 and D-P1) — the loop wizard runs first; the cross-family peer for the run is codex `gpt-6-astra` (xhigh for code rounds). Tasks 0.2 and 0.4 need the PRACTITIONER before Phase 1 can start.
- Open blockers / open questions: the speaker-embedding model candidate (Tasks 0.3–0.5, D-P1) — practitioner-run fetch.
- Phase 0 interleaving (for an `/execute-loop` run): agent tasks 0.1 and 0.3 first → PAUSE for the practitioner's 0.2 (consent ratification) and 0.4 (fetch + smoke, normal terminal) → agent task 0.5 → PAUSE for the practitioner's 0.6 (promote the pinned model) → D-P1 (`[decision]`, a planned hard-stop) → Phase 1. Run Phase 0 as one-phase-and-stop; Phases 1–3 may run back-to-back; Phase 6 is practitioner-only.
- Last plan sync: 2026-09-15 (`/document`, composer, at the Phase 1 close: rounds 12–16 compacted into the sidecar). Previous: 2026-09-15 (Task 0.5 close), 2026-09-06 (Phase 0 agent-work close), 2026-09-05 (created).
- Plan peer-review round 1 (codex `gpt-6-astra` xhigh): ATTEMPTED 2026-09-05 11:40-11:46 UTC and INCONCLUSIVE - the run died on the ChatGPT account's usage limit after reading the code ("You've hit your usage limit ... try again at Sep 6th, 2026 2:41 AM"), no findings emitted, nothing logged; the peer had finished READING every named source (25 commands, 135k tokens) and was cut off before writing; after the reset (or on purchased credits) RESUME that session rather than re-reading: `codex exec resume 01a0715e-f6ba-7bf0-9fa5-e6c6d823b30a -s read-only -m gpt-6-astra -c model_reasoning_effort=xhigh "Your reading is complete. Now output the full Round 1 block exactly as the OUTPUT section of the original prompt specifies, ending with the PEER-PLAN-ROUND-1 RESULT line." </dev/null` (fallback: re-run the unchanged prompt `.cursor/loops/practitioner-profile-plan-peer-r1-prompt.txt` as round 1). The plan is NOT cross-family reviewed until that round lands.
- Plan peer-review round 3 (codex `gpt-6-astra` high, session `01a07189-7ca5-7b40-a34f-d10b2c31755b`): ATTEMPTED 2026-09-05 12:27 UTC and INCONCLUSIVE - the account's 5-hour usage window (spent by the round-1 resume, round 2 and this start: ~318k astra tokens) tripped after two commands ("try again at Sep 6th, 2026 2:57 AM" local = 16:57 UTC); nothing emitted. Re-run FRESH after the reset with the unchanged prompt `.cursor/loops/practitioner-profile-plan-peer-r3-prompt.txt` via `practitioner-profile-plan-peer-r3-run.sh high` (the session read only the plan, so resuming saves little). Round 3 is the confirmation round; the pass is NOT converged until it lands clean.
- Retro report: .cursor/loops/retro-stage-2-2026-09-16.md (2026-09-16)
- Loop config: executor=claude-p model="claude-fable-5-1" effort=high profile=default; delegate=claude-p model="claude-opus-5" subagent_type="loop-delegate" agent_loc=profile; peer=codex model="gpt-6-astra" effort=high; architect=on; cadence=every-phase; caps=review:3,peer:5; gates=executor; cap-raise=executor; high-auto=on; peer-max=12; notify=action-only; scope=all; autocommit=on; isolation=none; merge=off; perms=scoped; liveness=10; monitor-delivery=auto; verify=composer
- **COMPOSER RUN-STATE (2026-09-17 05:36, run=stage-5 RESUMED in a NEW session — Phase 5 (Tasks 5.0–5.5), iso=cliniko-pp-20260917-053108-221b, composer=Claude Code session 4c55e6c8 on the Windows host; the paused run's marker cliniko-pp-20260916-122047-85c8 was closed complete (run-close) before this run claimed the checkout.** Wizard: Custom reproducing the stored line (claude-p claude-fable-5-1/high/default, architect ON with delegate claude-opus-5 via `loop-delegate` @ profile, peer codex gpt-6-astra/high, gates=executor / cap-raise=executor / high-auto=on, peer-max=12, notify=action-only, scope=all, autocommit=on, isolation=none, perms=scoped, liveness=10, monitor-delivery=auto, verify=composer); both keys attested in `.cursor/loops/cliniko-pp-20260917-053108-221b-policy.log` (high-auto `39ca90052d30`, gates `61659c487299`, zero revocations); sticky-ack `b68faeff1c14`; `Wake: layer1=bg-shell layer2=sentinel interval=10m delivery=bg`; the stage-5 logs from the paused run were rotated (`.20260917-0532` suffix); harness probe h2 PASSED 05:35 (grants permitted, auth live, 1.19 USD; EXIT:4 census-benign). Usage baseline: claude 5-hour 7% / week 16% (Fable 30%), codex not exposed. **Immediate next action:** resume architect session `5b7033df` (leg stage-5-arch-f8) with the practitioner's smoke feedback batched verbatim ("Learned phrases are not showing up after saving in the practioner profile.") through /execute's Deferral Confirmation Gate as one compact table; then composer suites → codex confirmation round (pass stage-5.p2, round 1, high) → the practitioner's re-smoke (MUST-PAUSE) + Task 5.5 R3 initials → `smoke-pass` → phase-close gate → flush → `/document` → commit → the hardening stage H1–H4. **PHASE 5 CLOSED 2026-09-17 12:55:** legs f8 (dead at spawn: `--resume` needs the full UUID) → f8b (must-pause: four clarification questions, answered) → f8c (the Save-note messaging fix, Task 5.6 `[pending-hardening]`, round 39: 2 LOW applied) → f8d (test-side assertion fix, round 40 clean) → f8e (re-smoke disposed no-defect-by-design); composer suites 1871 green after one caught failure; codex round 41 CLEAN (pass stage-5.p2 converged at round 1); the practitioner's re-smoke PASSED (four screenshots, 12:40–12:44) — `smoke-pass` recorded, phase-close gate 41+41 OK, flush clean, `/document` done, rounds 25–41 compacted to the sidecar, commit on the composer seat (never a push). Next: the scoped `/review-plan` over Task 5.6 at the boundary, then the hardening stage H1–H4 under `scope=all`. The capitalised-opener decision was taken at the close (option (ii) → Task 5.7 `[pending-hardening]`, scoped `/review-plan` with 5.6). Still the practitioner's: Task 5.5 R3 initials. Last plan sync: 2026-09-17 12:58 (this bullet).**
- **COMPOSER RUN-STATE (2026-09-16 12:26, run=stage-3 STARTED — Phase 3 (Tasks 3.1–3.4), iso=cliniko-pp-20260916-122047-85c8, composer=Claude Code session deada708 on the Windows host; a FRESH run after the paused stage-2 closed complete).** Wizard: Custom reproducing the stored line, practitioner switched to EXECUTOR-AUTONOMY (`gates=executor; cap-raise=executor; high-auto=on; peer-max=12; notify=action-only`, both keys attested in `.cursor/loops/cliniko-pp-20260916-122047-85c8-policy.log` — high-auto `e31e52f978e3`, gates `6eab762e7860`, zero revocations); architect ON (delegate `claude-opus-5` via `loop-delegate` @ profile — the harness probe h1 PROVED the pin: `modelUsage claude-opus-5[1m]`); peer codex `gpt-6-astra` (xhigh first round, high confirmations; the probe script's "codex ABSENT" is the `.cmd` gotcha); verify=composer; isolation=none (marker `…-85c8-active`); `Wake: layer1=bg-shell layer2=sentinel interval=10m delivery=bg`; sticky-ack `b68faeff1c14`. Preflight 1–14 complete 12:23; harness probe h1 passed (1.22 USD). **PHASE 3 BUILT + CONVERGED (14:31):** architect session `e6ddd71b` legs d1 (build, 2 delegations to opus-5 across the run — the tab + tests, then the PR-REG-005 test plumbing) → d2/d3 (in-session rounds 25–26, converged) → d4/d5 (LEG 1 verify + LEG 2 fix of codex round 27: 7 findings, HIGH-006 verified MED, all applied) → d6 (E501 wrap) → d7/d8 (round 28: 2 docs LOWs applied) — codex pass stage-3.p1 rounds 27 (xhigh; one limit death, resumed after 13:50) / 28 / 29 (high) CONVERGED pre-cap; suites ruff clean / mypy 34 / pytest 1666; Tasks 3.1–3.3 🟩, 51%. Executor cost so far ≈ 38 USD API-equivalent. **PHASE 3 CLOSED 2026-09-16 15:16 (`055d0aa`):** the practitioner's Task 3.4 smoke PASSED (~14:55; one environment step first — the speaker model re-fetched into the real cache from a normal terminal), `smoke-pass` recorded, phase-close history gate OK (29+29), the 9 queued notices flushed, `/document` (AGENTS.md Current Status + Last Session, lessons recurrence line, the installer-bundling decision in Deferred), commit on the composer seat (never a push). **PHASE 4 CLOSED 2026-09-16 16:15** (run=stage-4, architect session `ba32824b` legs e1–e5, one opus-5 delegation; in-session rounds 30–31 converged; codex pass stage-4.p1 rounds 32 (xhigh: PR-HIGH-007 verified MED + PR-LOW-034, both applied) / 33 (clean) CONVERGED pre-cap; suites ruff clean / mypy 34 / pytest 1705; Tasks 4.1–4.5 🟩, 67%; no UI ⇒ no smoke pause; phase-close gate 33+33, flush, `/document`, commit on the composer seat). Scoped `/review-plan` over Phase 5 DONE 2026-09-16 16:25 (composer, Claude Code `claude-fable-5-1`): D9 amended (auto-learn, review later; written on Save), the Excluded item narrowed to other speakers' lines, the Critical Constraint rewritten, consent text v2 RATIFIED and recorded, Task 5.0 added (consent v2 + version check + re-consent without re-record), Tasks 5.2/5.3/5.4 re-shaped, Goal/Agreed Scope siblings reconciled. **PHASE 5 BUILT + REVIEW-CONVERGED 2026-09-16 17:27** (run=stage-5, architect session `5b7033df` legs f1–f3, two opus-5 delegations; in-session rounds 34–35 converged; suites ruff clean / mypy 34 / pytest 1854; Tasks 5.0–5.4 🟩, 5.5 🟨 awaiting the practitioner's R3 initials; 80%). **PAUSED 17:31 at the usage-exhaustion MUST-PAUSE:** codex peer round 36 (pass stage-5.p1 round 1, xhigh) died on the 5-hour window mid-read (session `01a0a91e`, 73k tokens); the practitioner reset the pool at 17:42 and round 36 was resumed at high and landed (8 findings: HIGH-008 verified MED, MED-023/024, five docs LOWs — ALL APPLIED in legs f4/f5, suites ruff clean / mypy 34 / pytest 1863). **PAUSED AGAIN 18:07 at the usage-exhaustion MUST-PAUSE: confirmation round 37 (pass stage-5.p1 round 2, high) died mid-read on the 17:42 window (session `01a0a93f-afed-77a0-bbd5-4da166b95609`, 87k tokens); resets 22:42.** RESUMED 22:42 after the reset: round 37 landed (all round-36 fixes confirmed + PR-MED-025 — the atomic writer's unguarded cleanup error escaping the config boundary — verified MED and applied in legs f6/f7 with six real-writer pins; round 37 closed 1/1). Round 38 CLEAN 22:55 — pass stage-5.p1 CONVERGED (rounds 36–38); agent smoke = the 1869 run on the converged tree (ruff clean / mypy 34). **SMOKE RETURNED FEEDBACK 2026-09-17 ~00:05 — NO smoke-pass; RUN PAUSED by practitioner request (session full) — resume in a NEW session from this bullet.** Practitioner feedback, VERBATIM, to be relayed to the Phase 5 architect (session `5b7033df`, resumable) as the FIRST action of the next session: "Learned phrases are not showing up after saving in the practioner profile." Composer notes for the architect (routing only, no diagnosis): the smoke was on the Explorer-launched app against the practitioner's REAL config/profile roots; two things the architect should check first — (1) whether the consent-version gate was satisfied (the real profile was consent-v1; learning is OFF until "Confirm consent" with the opt-in ticked — did the tab make that path obvious enough?), (2) whether the user cue file / `section_cues.learned.json` under the real `%LOCALAPPDATA%` config root received the write on Save and the Practitioner tab's lists re-read it (the `learned_phrases_changed` signal path + `refresh_learned_phrases`), remembering the agent shell sees the MSIX-virtualised view, so the practitioner may need to report what the status line said after Save ("Learned …" / "not learned: …"). Route it through `/execute`'s Deferral Confirmation Gate as one compact table; `Fix now` if a defect, `Include in plan` if UI-touching beyond a one-site fix; the practitioner re-smokes after. ALSO STILL OWED by the practitioner: the Task 5.5 R3 initials on `docs/testing/shipping-gate.md:55`. RESUME RECIPE (next session): `/execute-loop` → Q1 Custom with this plan's stored `Loop config:` line (gates=executor / cap-raise=executor / high-auto=on as this run chose) → preflight (fresh iso-id + marker; the wrapper blob check; the delegate definition; the harness probe) → resume architect session `5b7033df` with the feedback batch (leg f8) → composer suites → codex confirmation round (pass stage-5.p2, round 1) → the practitioner's re-smoke (MUST-PAUSE) + R3 initials → `smoke-pass` → phase-close gate → flush → `/document` → commit → the hardening stage H1–H4. State at pause: Phase 5 Tasks 5.0–5.4 🟩, 5.5 🟨 (initials), 80%; in-session rounds 34–35 and codex rounds 36–38 CONVERGED; suites ruff clean / mypy 34 / pytest 1869; the 20-file Phase 5 diff UNCOMMITTED on `main` at `7696b28`. Original plan for after the smoke → then `smoke-pass` → `smoke-pass` → phase-close gate → flush → `/document` → commit → the hardening stage H1–H4. (Superseded resume note kept for the record: `codex exec -s read-only resume 01a0a93f-afed-77a0-bbd5-4da166b95609 -m gpt-6-astra -c model_reasoning_effort=high` with the finish-reading-then-write prompt (`.cursor/loops/stage-5-peer-r37-resume-prompt.txt` to be written from the r36 resume template), transcribe round 37, then (clean ⇒) agent smoke → the practitioner's live smoke + R3 initials (MUST-PAUSE) → `smoke-pass` → phase-close gate → flush → `/document` → commit → the hardening stage H1–H4. (Task 4.5 history: PRACTITIONER-decided 2026-09-16 15:20 — USE THE SHIPPED DEFAULTS (the gate-run cue file is a copy of `config_defaults/section_cues.json`; learning fills it from Phase 5 on) — relay to the architect at its next resume so 4.5 closes in this phase). **PRACTITIONER DECISION 2026-09-16 15:22 — Phase 5 learning changes shape: AUTO-LEARN, REVIEW LATER** (recommended option chosen over "fully silent" and over the ratified propose-then-approve): a phrase from one of the practitioner's OWN utterances is saved WITHOUT a click after an add/move, shown as "recently learned" on the Practitioner tab with one-click delete, the full list still deletable. Consequences owed BEFORE Phase 5 starts — a scoped `/review-plan` at the Phase 4/5 boundary: amend D9 and the Excluded item ("automatic, unapproved learning" is superseded for the practitioner's own utterances only), consent text v1 → v2 (the "only on your approval" sentence changes; existing enrolments re-consent once — the tab already withdraws a stale consent tick), the threat model gains the name/number/date/drug REFUSAL FILTER as the enforcing control (tested as a class), Phase 5 tasks 5.x re-shaped. Known host gap carried: `fold-delegation` refuses on Windows (staging files retained, 2 with records). Last plan sync: 2026-09-16 14:31 (this bullet).
- **PRIOR COMPOSER RUN-STATE (2026-09-16 10:08, PAUSED by practitioner stop at the Phase 2/3 boundary for the bootstrap UPDATE workflow — resume in a NEW session from this bullet — PHASE 2 + TASK 2.6 CLOSED — run=stage-2 continues under scope=all, iso=cliniko-pp-20260906-142504-eb4c, composer=Claude Code session a465e08f on the Windows host; executor claude-fable-5-1/high via the spawn wrapper — Phase 2 session `776c23ce` (legs c1–c13); ARCHITECT MODE ON from Phase 3 (delegate `claude-opus-5`, `subagent_type=loop-delegate`, profile-level definition `~/.claude/agents/loop-delegate.md`, verified at 09:43); peer codex gpt-6-astra, xhigh for a phase's first round, high for confirmation rounds, read-only sandbox).** `/execute-loop` scope=all, isolation=none (ownership marker `.cursor/loops/cliniko-pp-20260906-142504-eb4c-active`), verify=composer, gates=fix-biased, cap-raise=follow, high-auto=on attested (`05eccc53d07d`, zero revocations), caps review:3 / peer:5, autocommit=on (composer seat, never a push), liveness 10 min (sentinel), delivery bg. **Phase 0 COMPLETE 2026-09-15**, **Phase 1 CLOSED 2026-09-15** (`58e7bad`), **PHASE 2 CLOSED 2026-09-16 09:30** (`4caef36`; rounds 17–21). **TASK 2.6 CLOSED 2026-09-16 10:05:** practitioner chose option (a) at the boundary gate; scoped `/review-plan` amended D13 (+ every current-plan sibling, swept as a class after codex rounds 23–24 caught the instance-patches); built by the architect (leg c12, no delegate — below the bar), in-session round 22 clean, codex rounds 23–24 (1 LOW each, plan prose, composer-applied) accept-closed on the tail signature; suites ruff / mypy 33 / pytest 1636; every escape check clean, commit boundary held. Known host gap: `loop-journal.py fold-delegation` refuses on Windows (`os.O_DIRECTORY`) — the two staging files are retained, both 0 bytes; carried to the bootstrap-update window. **Immediate next action (new session, after the update is committed):** `/execute-loop` → Q1 `Custom` (two Active plans carry a `Loop config:`, so the reuse offer is omitted — answer every item with this plan's stored line: claude-p `claude-fable-5-1`/high/default, architect ON with delegate `claude-opus-5` (`loop-delegate` @ profile), peer codex `gpt-6-astra`/high, isolation=none, monitor-delivery auto, perms scoped, all other defaults) → preflight re-verifies the wrapper blob against HEAD, re-acks the (possibly updated) STICKY card, re-writes/verifies the delegate definition, re-runs the harness probe → spawn Phase 3 (Tasks 3.1–3.4) with a FRESH architect session (no session to resume — Phase 2's `776c23ce` is done); Phase 3 is UI-bearing: the live smoke at Task 3.4 is a MUST-PAUSE and the commit is refused without its `smoke-pass` record. Ownership marker CLOSED at 10:16 (`cliniko-pp-20260906-142504-eb4c-active.complete-20260916-101604`, run-close verdict=complete) so the bootstrap update's loop guard passes — the next session mints a FRESH iso-id + marker. `/retro` over stage-2 written: `.cursor/loops/retro-stage-2-2026-09-16.md` (stage-0/1 not yet covered). Last plan sync: 2026-09-16 10:17 (this bullet).**

## Review History
Each /review invocation appends a one-line entry here. Round NUMBERS
are never allocated by counting this section's entries — allocation
follows /review's **Detect review round** rule (the canonical
definition: the `Review Findings Log`'s round headers, with a legacy
highest-History-round fallback when the Log has no headers; every
findings writer follows it). Ignore the placeholder line when reading
this section.

- 2026-09-05 round 1: 0 CRIT / 1 HIGH / 14 MED / 0 LOW; skew=cross-family; action=amended-in-place (plan peer-review — independent cross-family Codex plan peer-review on `gpt-6-astra` xhigh, read-only sandbox, run as a resumed session after the first attempt died on the account's usage limit; block transcribed verbatim by the owning planning session (Claude Code `claude-fable-5-1`). Materiality verified: 15 build-affecting / 0 record-only / 0 invalid — every peer label upheld. All 15 applied as `/review-plan`-style amendments the same day (consent text, D3/D4/D5/D9/D13/D14, new D15/D16, Tasks 0.3/0.5/0.6/1.1/1.2/1.3/2.1–2.4/3.1/3.2/5.1/5.1b/5.2/5.4/5.5/6.1, Flow 2, Critical Constraints), siblings swept. NOT converged: a plan peer-loop never converges on round 1; round 2 is owed. Line owned by the seat that finishes the round.)
- 2026-09-05 round 2: 0 CRIT / 0 HIGH / 2 MED / 1 LOW; skew=cross-family; action=amended-in-place (plan peer-review — post-round-1-amendment independent cross-family Codex plan peer-review on `gpt-6-astra` at HIGH effort, read-only sandbox, fresh session; block transcribed verbatim by the owning planning session (Claude Code `claude-fable-5-1`). Round-1 confirmations 11 of 15: PR-HIGH-001, PR-MED-004, PR-MED-005, PR-MED-015 NOT CONFIRMED as amended and completed this round. New: PR-MED-016 (the name refusal ran after lowercasing — moved before normalisation), PR-MED-017 (Task 0.6 depended on a Phase 3 report line — re-anchored), PR-LOW-018 (stale summaries — reconciled); materiality verified 2 build-affecting / 1 record-only / 0 invalid, all applied the same day. NOT converged: two NEW build-affecting findings this round; round 3 (confirmation) owed. Line owned by the seat that finishes the round.)
- 2026-09-05 round 3: 0 CRIT / 0 HIGH / 0 MED / 1 LOW; skew=none; action=converged (plan peer-review — post-round-2-amendment independent cross-family Codex plan peer-review on `gpt-6-astra` at HIGH effort, read-only sandbox, fresh session after the usage-window reset; block transcribed verbatim by the owning planning session (Claude Code `claude-fable-5-1`). Round-2 confirmations 6 of 7 (PR-MED-017 completed by the one new finding). New: PR-LOW-019 (Flow 3 named the wrong model-report surface) — materiality verified record-only, applied the same day. PLAN PEER PASS CONVERGED: rounds 1–3 = 15 → 3 → 0 build-affecting findings (1H14M → 2M1L → 1L record-only), 19 findings verified and applied, every peer materiality label upheld. The plan is cross-family reviewed and execution-ready. Round finished by the owning planning session.)

- 2026-09-06 round 4: 0 CRIT / 0 HIGH / 0 MED / 4 LOW; skew=none; action=none (in-session `/review-loop` round 1 of cap 3 over the Phase 0 build — Tasks 0.1 + 0.3 — by the stage-0 executor, Claude Code `claude-fable-5-1`; four LOWs, all Fix-now, applied one at a time the same day; two candidates dropped at verification; no gate fired.)
- 2026-09-06 round 5: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (in-session `/review-loop` round 2 of cap 3 — full re-review of every changed file plus the Post-Fix Regression Check over the four round-4 hunks after the composer's suite re-run stayed green (ruff, mypy 30 files, pytest 1408); nothing found — LOOP CONVERGED at round 2 of 3. Cross-family codex `/peer-loop` NOT chained by the executor (composer seat).)
- 2026-09-06 round 6: 0 CRIT / 1 HIGH / 1 MED / 2 LOW; skew=cross-family; action=fixed (independent cross-family Codex peer review on `gpt-6-astra` at HIGH effort, read-only sandbox, composer seat, over the Phase 0 diff — Tasks 0.1 + 0.3; block transcribed verbatim by the composer (Claude Code `claude-fable-5-1`); executor LEG-1 verification: PR-HIGH-002 confirmed and downgraded to MED with evidence, the other three confirmed at their labels; all four routed to fixing at the composer seat under gates=fix-biased (recorded + notified) and applied in LEG 2; composer suites green after the fixes (ruff, mypy, pytest 1427). Line owned by the seat that finishes the round.)
- 2026-09-06 round 7: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=converged (independent cross-family Codex peer review on `gpt-6-astra` at HIGH effort, read-only sandbox, composer seat — confirmation round: every round-6 fix confirmed complete, class attack over the whole scope found nothing; peer pass CONVERGED 4 -> 0. Line owned by the seat that finishes the round.)

- 2026-09-15 round 8: 0 CRIT / 0 HIGH / 0 MED / 5 LOW; skew=none; action=none (in-session `/review-loop` round 1 of cap 3 over the Task 0.5 pin diff by the stage-0 executor, Claude Code `claude-fable-5-1`; five LOWs — a duplicated size constant, an unverified licence claim in a comment, an unused test fixture parameter, and two stale/imprecise doc sentences — all Fix-now, applied the same day; no gate fired.)
- 2026-09-15 round 9: 0 CRIT / 0 HIGH / 0 MED / 1 LOW; skew=none; action=none (in-session `/review-loop` round 2 of cap 3 — full re-review of every changed file plus the Post-Fix Regression Check over the five round-8 hunks after the composer's suite re-run stayed green (ruff, mypy 30 files, pytest 1431); one 🆕 doc-only LOW (a ragged hard-wrap left by the round-8 flow-8 edit), applied; LOOP CONVERGED at round 2 of 3. Cross-family codex `/peer-loop` NOT chained by the executor (composer seat).)
- 2026-09-15 round 10: 0 CRIT / 0 HIGH / 0 MED / 3 LOW; skew=cross-family; action=fixed (independent cross-family Codex peer review on `gpt-6-astra` at HIGH effort, read-only sandbox, composer seat, over the Task 0.5 diff; block transcribed verbatim by the composer (Claude Code `claude-fable-5-1`); executor LEG-1 verified all three docs-only LOW; routed to fixing at the composer seat under gates=fix-biased (recorded + notified) and applied in LEG 2; composer suites green after the fixes (ruff, mypy, pytest 1431). Line owned by the seat that finishes the round.)
- 2026-09-15 round 11: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=converged (independent cross-family Codex peer review on `gpt-6-astra` at HIGH effort, read-only sandbox, composer seat — confirmation round: every round-10 fix confirmed complete; peer pass over Task 0.5 CONVERGED 3 -> 0. Line owned by the seat that finishes the round.)

- 2026-09-15 round 12: 0 CRIT / 0 HIGH / 1 MED / 3 LOW; skew=none; action=none (in-session `/review-loop` round 1 of cap 3 over the Phase 1 build — Tasks 1.1–1.4 — by the stage-1 executor, Claude Code `claude-fable-5-1`; one MED (the embedder's load-time smoke inference fed digital silence, a degenerate input) and three LOWs (two claim-outruns-structure docstrings, one untyped stat failure), all Fix-now, applied one at a time the same day; two candidates dropped at verification; no gate fired; the three code-file fixes await the composer's suite re-run before round 13.)
- 2026-09-15 round 13: 0 CRIT / 0 HIGH / 0 MED / 2 LOW; skew=none; action=none (in-session `/review-loop` round 2 of cap 3 — full re-review of every changed file plus the Post-Fix Regression Check over the four round-12 hunks after the composer's suite re-run stayed green (ruff, mypy 33 files, pytest 1514); two 🆕 doc-only LOWs — the D15 blocker refusal described as live before Phase 3 registers the benchmark, and an over-broad "no repr, dump or JSON can reach a log" retention claim — both applied the same day; no code changed, no suite owed; LOOP CONVERGED at round 2 of 3. Cross-family codex `/peer-loop` NOT chained by the executor (composer seat).)
- 2026-09-15 round 14: 0 CRIT / 1 HIGH / 1 MED / 1 LOW; skew=cross-family; action=fixed (independent cross-family Codex peer review on `gpt-6-astra` at XHIGH effort, read-only sandbox, composer seat, over the Phase 1 diff; the first attempt died on the codex usage window after its read and the same session was resumed after the 22:18 reset; block transcribed verbatim by the composer (Claude Code `claude-fable-5-1`); executor LEG-1: PR-HIGH-003 confirmed and downgraded to MED with evidence, PR-MED-019 and PR-REG-002 confirmed; all three routed to fixing at the composer seat under gates=fix-biased (the HIGH under the run-start attestation; recorded + notified) and applied in LEG 2; composer suites green after the fixes (ruff, mypy 33 files, pytest 1521). Line owned by the seat that finishes the round.)
- 2026-09-15 round 15: 0 CRIT / 0 HIGH / 1 MED / 1 LOW; skew=cross-family; action=fixed (independent cross-family Codex peer review on `gpt-6-astra` at XHIGH effort, read-only sandbox, composer seat — confirmation round after round 14: the three round-14 fixes confirmed; PR-MED-020 and PR-LOW-024 verified by the executor and routed to fixing at the composer seat under gates=fix-biased (recorded + notified), applied in LEG 2; composer suites green (ruff, mypy 33 files, pytest 1524). Line owned by the seat that finishes the round.)
- 2026-09-15 round 16: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=converged (independent cross-family Codex peer review on `gpt-6-astra` at XHIGH effort, read-only sandbox, composer seat — confirmation round after round 15: both round-15 fixes confirmed complete, class attack found nothing; Phase 1 peer pass CONVERGED 3 -> 2 -> 0. Line owned by the seat that finishes the round.)

- 2026-09-16 round 17: 0 CRIT / 0 HIGH / 0 MED / 4 LOW; skew=none; action=none (in-session `/review-loop` round 1 of cap 3 over the Phase 2 build — Tasks 2.1–2.5 — by the stage-2 executor, Claude Code `claude-fable-5-1`; four LOWs — the pipeline's identity guard missing the digest, a duplicated has-text predicate, a copy-pasted UI reset, a harness input type whose repr carried the vector — all Fix-now, applied one per file the same day; one candidate dropped at verification; no gate fired; the three code-file fixes await the composer's suite re-run before round 18.)
- 2026-09-16 round 18: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (in-session `/review-loop` round 2 of cap 3 — full re-review of every changed file plus the Post-Fix Regression Check over the four round-17 hunks after the composer's suite re-run stayed green (ruff, mypy 33 files, pytest 1625); nothing found — LOOP CONVERGED at round 2 of 3. Cross-family codex `/peer-loop` NOT chained by the executor (composer seat); Task 2.6 stays pending its scoped review.)
- 2026-09-16 round 19: 0 CRIT / 2 HIGH / 1 MED / 2 LOW; skew=cross-family; action=fixed (independent cross-family Codex peer review on `gpt-6-astra` at XHIGH effort, read-only sandbox, composer seat, over the Phase 2 diff plus the Task 2.6 assessment (recommends option (a)); the first attempt died on the codex window and the round was re-run fresh after the 03:22 reset; executor LEG-1: both HIGHs verified MED with evidence, the rest at their labels; all five routed to fixing at the composer seat under gates=fix-biased (the HIGHs under the run-start attestation; recorded + notified) and applied in LEG 2; composer suites green (ruff, mypy 33 files, pytest 1635). Line owned by the seat that finishes the round.)
- 2026-09-16 round 20: 0 CRIT / 0 HIGH / 1 MED / 0 LOW; skew=cross-family; action=fixed (1 of 1 Applied 2026-09-16; composer suites + codex confirmation round 21 pending) (independent cross-family Codex peer review on `gpt-6-astra`, read at XHIGH then written at HIGH after the codex window reset (session resumed 08:49), read-only sandbox, composer seat — confirmation round after round 19: the five round-19 fixes confirmed; one new MED (PR-REG-004, the clinician-span extraction outside the leave-one-out pool's guard); block transcribed verbatim by the composer; executor LEG 1 verified it MED, the composer routed it to fixing under gates=fix-biased and LEG 2 applied it with its regression test. Line owned by the seat that finishes the round.)
- 2026-09-16 round 21: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=cross-family; action=converged (independent cross-family Codex peer review on `gpt-6-astra` at HIGH effort, read-only sandbox, composer seat — confirmation round after round 20: PR-REG-004 fix confirmed complete at `speaker_eval.py:1336-1344`/`:1427`/`:1440`, batch-isolation class and conversion siblings traced clean, Critical Constraints re-checked; block transcribed verbatim by the composer; PEER-LOOP CONVERGED — Phase 2 cross-family pass closed at 3 rounds (19–21))
- 2026-09-16 round 22: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (in-session `/review-loop` round 1 of cap 3 over the Task 2.6 diff — the amended D13 remainder rule — by the stage-2 architect-executor, Claude Code `claude-fable-5-1`, after the composer's suites stayed green (ruff, mypy 33 files, pytest 1636); every changed file re-read; zero actionable findings — LOOP CONVERGED at round 1. Cross-family codex confirmation NOT chained by the executor (composer seat).)
- 2026-09-16 round 23: 0 CRIT / 0 HIGH / 0 MED / 1 LOW; skew=cross-family; action=fixed (independent cross-family Codex peer review on `gpt-6-astra` at HIGH effort, read-only sandbox, composer seat — over the Task 2.6 diff: build verified correct at `transcription.py:875-877`; PR-LOW-027 — three current-plan prose siblings still prescribed the retired remainder split — applied by the composer as plan prose; codex confirmation round 24 pending)
- 2026-09-16 round 24: 0 CRIT / 0 HIGH / 0 MED / 1 LOW; skew=cross-family; action=fixed (independent cross-family Codex peer review on `gpt-6-astra` at HIGH effort, read-only sandbox, composer seat — confirmation after round 23: PR-LOW-027 partial (`:29` residue), PR-LOW-028 — further current-plan siblings of the retired split — both applied by the composer as one whole-plan class fix; build re-confirmed correct; PEER-LOOP ACCEPT-CLOSED on the tail signature (two consecutive docs-only rounds, nothing pending, no CRIT/HIGH) under gates=fix-biased + cap-raise=follow)

- 2026-09-16 round 25: 0 CRIT / 0 HIGH / 1 MED / 1 LOW; skew=none; action=none (in-session `/review-loop` round 1 of cap 3 over the Phase 3 build — Tasks 3.1–3.3 — by the stage-3 architect, Claude Code `claude-fable-5-1`, after the composer's suites were green (ruff, mypy 34 files, pytest 1659); one MED (a Stop pressed after the capture returned still saved the profile) and one LOW (an unused test attribute), both Fix-now, applied the same day with a regression test; two candidates dropped at verification; no gate fired; the code fix awaits the composer's suite re-run before round 26.)
- 2026-09-16 round 26: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (in-session `/review-loop` round 2 of cap 3 — full re-review of every changed file plus the Post-Fix Regression Check over the two round-25 hunks after the composer's suite re-run stayed green (ruff, mypy 34 files, pytest 1660); nothing found — LOOP CONVERGED at round 2 of 3. Cross-family codex `/peer-loop` NOT chained by the architect (composer seat).)
- 2026-09-16 round 27: 0 CRIT / 0 HIGH / 2 MED / 5 LOW; skew=cross-family; action=fixed (7 of 7 Applied 2026-09-16 by the architect in LEG 2 under gates=executor; composer suites + codex confirmation round 28 pending) (independent cross-family Codex peer review on `gpt-6-astra` at XHIGH effort, read-only sandbox, composer seat, over the Phase 3 diff — Tasks 3.1–3.3; the peer's labels were 1 HIGH / 2 MED / 4 LOW; architect LEG-1 verification 2026-09-16: PR-HIGH-006 verified MED with evidence (a same-login store fault is the only path; the fix is local), PR-MED-022 verified LOW with evidence (the shipped Start path has the identical ≤100 ms handoff window; a refused open fails typed), the other five confirmed at their labels — all seven in-phase, none production-impacting, all recommended Fix-now; composer disposition next.)
- 2026-09-16 round 28: 0 CRIT / 0 HIGH / 0 MED / 2 LOW; skew=cross-family; action=fixed (2 of 2 Applied 2026-09-16 by the architect in LEG 2 under gates=executor, docs + one comment; composer ruff/screen-tests + codex confirmation round 29 pending) (independent cross-family Codex peer review on `gpt-6-astra` at HIGH effort, read-only sandbox, composer seat — confirmation round after round 27: all seven round-27 fixes confirmed, two marked "partial" on prose only and completed by the two new findings; architect LEG-1 verification 2026-09-16: PR-LOW-032 and PR-LOW-033 confirmed docs-only LOW at their labels, both in-phase, both Fix-now; the pass's first docs-only round; composer disposition next.)
- 2026-09-16 round 29: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (independent cross-family Codex peer review on `gpt-6-astra` at high, read-only sandbox, composer seat — the second confirmation round of pass stage-3.p1 over Phase 3: both round-28 prose fixes confirmed complete, no new candidates; the pass CONVERGED at round 3 of cap 5, pre-cap)

- 2026-09-16 round 30: 0 CRIT / 0 HIGH / 0 MED / 2 LOW; skew=none; action=none (in-session `/review-loop` round 1 of cap 3 over the Phase 4 build — Tasks 4.1–4.5 — by the stage-4 architect, Claude Code `claude-fable-5-1`, after the composer's suites were green (ruff clean, mypy 34 files, pytest 1695); two LOWs — a docs claim that outran the Task 4.5 decision at three sites, and the frozen-model dict residue left unnamed — both Fix-now, applied the same day; two candidates dropped at verification; no gate fired; the comment hunk awaits the composer's ruff + module re-run before round 31.)
- 2026-09-16 round 31: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (in-session `/review-loop` round 2 of cap 3 — full re-review of every changed file plus the Post-Fix Regression Check over the six round-30 hunks after the composer's suite re-run stayed green (byte-identical JSON copy, README smoke exact, ruff clean, mypy 34 files, pytest 1695); nothing found — LOOP CONVERGED at round 2 of 3. Cross-family codex `/peer-loop` NOT chained by the architect (composer seat).)
- 2026-09-16 round 32: 0 CRIT / 0 HIGH / 1 MED / 1 LOW; skew=cross-family (codex gpt-6-astra, first Phase 4 code round, xhigh; the peer's labels were 1 HIGH / 1 LOW — architect LEG-1 verification 2026-09-16: PR-HIGH-007 verified MED with evidence (an emptied-but-valid-JSON packaged default is the only path; the consequence is a visible empty draft, no wrong content; the fix is a packaged-default completeness invariant at import plus a refusal test), PR-LOW-034 confirmed docs-only LOW; both in-phase, neither production-impacting, both Fix-now); action=fixed (2 of 2 Applied 2026-09-16 by the architect in LEG 2 under gates=executor — the packaged-default completeness guard + 10 test items, the two prose bounds; composer suites + codex confirmation round 33 pending)
- 2026-09-16 round 33: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (independent cross-family Codex peer review on `gpt-6-astra` at high, read-only sandbox, composer seat — the confirmation round of pass stage-4.p1 over Phase 4: PR-HIGH-007 (verified MED) and PR-LOW-034 confirmed complete, one candidate dropped; the pass CONVERGED at round 2 of cap 5, pre-cap)

- 2026-09-16 round 34: 0 CRIT / 0 HIGH / 0 MED / 3 LOW; skew=none; action=none (in-session `/review-loop` round 1 of cap 3 over the Phase 5 build — Tasks 5.0–5.5 — by the stage-5 architect, Claude Code `claude-fable-5-1`, after the composer's suites were green (ruff clean, mypy 34 files, pytest 1852); three LOWs — the capitalised-opener refusal unnamed as a residue in the security docs, the chooser losing its selection on every re-finalise, and the learning gate read once at review start going stale mid-review — all Fix-now, applied one at a time the same day with two new pins; three candidates dropped at verification; no gate fired; the two code hunks await the composer's suite re-run before round 35.)
- 2026-09-16 round 35: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (in-session `/review-loop` round 2 of cap 3 — full re-review of the Phase 5 surface plus the Post-Fix Regression Check over the eleven round-34 hunks after the composer's suite re-run stayed green (ruff clean, mypy 34 files, pytest 1854); nothing found — LOOP CONVERGED at round 2 of 3. Cross-family codex `/peer-loop` NOT chained by the architect (composer seat).)
- 2026-09-16 round 36: 0 CRIT / 0 HIGH / 3 MED / 5 LOW; skew=cross-family (codex gpt-6-astra, first Phase 5 code round: read at xhigh, cut by the pool, resumed at high after the practitioner reset it; the peer's labels were 1 HIGH / 2 MED / 5 LOW — architect LEG-1 verification 2026-09-16: PR-HIGH-008 confirmed a real hole and verified MED with evidence (only capitalised starter-homograph names after an opening filler can pass, the pinned heuristic admits the same word at a true segment start, the consequence is one deletable plaintext phrase; a local fix), PR-MED-023 and PR-MED-024 confirmed behavioral MED (a sidecar-only write failure misreported and unrefreshed; a sidecar orphan a retry cannot remove), the five LOWs confirmed docs/plan-prose only; all in-phase, none production-impacting, all recommended Fix-now); action=fixed (8 of 8 Applied 2026-09-16 by the architect in LEG 2 under gates=executor — the real-position flag on the candidate, the reported sidecar failure with the tab refresh, the repairable delete with orphan pruning, the five prose classes swept; composer suites + codex confirmation round 37 pending)
- 2026-09-16 round 37: 0 CRIT / 0 HIGH / 1 MED / 0 LOW; skew=cross-family (codex gpt-6-astra confirmation round at high — read cut by the pool at 18:07, resumed after the 22:42 reset; all eight round-36 fixes confirmed; architect LEG-1 verification 2026-09-16: PR-MED-025 confirmed behavioral MED at its label — a double-fault escape (write failure + temp-file cleanup failure) surfaces a raw `OSError` past the config write boundary, bypassing the committed-cue outcome and, in `save()`, the post-save refresh; in-phase, not production-impacting, recommended Fix-now at `_write_config_file` with an in-phase `on_confirm_consent` sibling); action=fixed (1 of 1 Applied 2026-09-16 by the architect in LEG 2 under gates=executor — the config write boundary folds any raw writer `OSError` into `NoteConfigWriteError`, the tab's consent slot catches it too, six pins through the real writer; composer suites + codex confirmation round 38 pending)
- 2026-09-16 round 38: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (independent cross-family Codex peer review on `gpt-6-astra` at high, read-only sandbox, composer seat — the second confirmation round of pass stage-5.p1 over Phase 5: PR-MED-025 confirmed complete with its six real-writer pins, bounded regression clean, no candidates; the pass CONVERGED at round 3 of cap 5, pre-cap)
- 2026-09-17 round 39: 0 CRIT / 0 HIGH / 0 MED / 2 LOW; skew=none; action=none (in-session `/review-loop` round 1 of cap 3 over the smoke-feedback fix leg f8c — the learning line names Save note on this tab while phrases are queued, the not-attributed note, the Save-time "nothing learned" reason — by the stage-5 architect, Claude Code `claude-fable-5-1`; two LOWs found by the missed-issue pass (a stale learning line on a cleared tab; a profile read on every Save), both Fix-now, applied the same day; one candidate dropped at verification; no gate fired; the code hunks await the composer's suite re-run before round 40.)
- 2026-09-17 round 40: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (in-session `/review-loop` round 2 of cap 3 — the confirmation round over the round-39 hunks after the composer's suite run (ruff clean, mypy 34 files, full suite 1 failed / 1870 passed: one new test's tooltip assertion pinned a phrase the tooltip never carried — corrected test-side, the tooltip wording is the intended design); the Post-Fix Regression Check found no affected caller, the missed-issue pass found nothing — LOOP CONVERGED at round 2 of 3. Cross-family codex `/peer-loop` NOT chained by the architect (composer seat).)
- 2026-09-17 round 41: 0 CRIT / 0 HIGH / 0 MED / 0 LOW; skew=none; action=none (independent cross-family codex `gpt-6-astra` peer review at high, read-only sandbox — the CONFIRMATION round for the Phase 5 smoke-feedback fix (legs f8c/f8d; in-session rounds 39–40), pass stage-5.p2 round 1 of cap 5; both round-39 LOWs confirmed applied, the round-40 tooltip assertion confirmed against the shipped clause, the enforcing controls (practitioner-only learning, the refusal filter, Save-only write) confirmed unweakened; 0 candidates / 0 dropped / 0 downgraded; 87,026 tokens; pass stage-5.p2 CONVERGED — transcribed by the composer, the finishing seat)

Format /review will append:
- YYYY-MM-DD round N: X CRIT / X HIGH / X MED / X LOW; skew=<class>; action=<rec>

## Review Findings Log
Each /review invocation appends a detailed findings block here, with
/fix updating per-finding Decision and Notes as it processes each one.
Findings carry stable IDs within a round (CRIT-001, HIGH-001,
MED-001, LOW-001, etc.) so they can be referenced across sessions
and tools without copy-paste. Plan peer-review rounds carry the
mode-distinct `Source: <Tool> plan peer-review` and a `Materiality:`
summary (canonical definitions in `/peer-review`; `/fix` never ingests
them). Closed-round compaction follows `/document` Step 6.7 (the
sidecar would be `findings-practitioner-profile.md`).

### Round 1 - 2026-09-05 - practitioner-profile plan, independent cross-family codex plan peer-review (round 1)
- Round status: Closed (15 of 15 Applied 2026-09-05 as plan amendments by the owning planning session; NOT converged — round 2 owed)
- Source: Codex plan peer-review
- Materiality: 15 build-affecting / 0 record-only / 0 invalid
- Compacted 2026-09-05 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-HIGH-001: Goal; Design Decision D9; Task 5.2; consent draft. — Clinician attribution does not exclude patient information from persistent learning — decision: Applied
  - PR-MED-002: Config / Environment / Deployment Impact; Tasks 0.2 and 5.2. — Consent promises encryption for phrases stored as plaintext JSON — decision: Applied
  - PR-MED-003: Design Decision D3; Task 1.1; Codebase Integration Notes. — The profile/model identity check is not bound to loaded model bytes — decision: Applied
  - PR-MED-004: Tasks 1.3, 3.1 and 3.2; Planned Workflow Summary, Flow 1. — Enrolment bypasses the existing capture and worker lifecycle guards — decision: Applied
  - PR-MED-005: D-P1; Tasks 1.1, 2.1 and 3.1. — The spectral-only decision outcome has no executable implementation branch — decision: Applied
  - PR-MED-006: Tasks 0.3–0.5; Current State / Handoff Note. — Phase 0 never installs the verified candidate at the runtime filename — decision: Applied
  - PR-MED-007: Schema / Data Changes; Design Decision D3; Tasks 1.1 and 2.1. — The similarity range rejects valid unconditional-fallback results — decision: Applied
  - PR-MED-008: D13; Tasks 2.1–2.3; Schema / Data Changes. — Segment-label membership does not guarantee a radio exists to auto-confirm — decision: Applied
  - PR-MED-009: Design Decision D4; Task 2.3; Phase 2 verification. — “Enable Generate immediately” conflicts with template confirmation — decision: Applied
  - PR-MED-010: D14; Tasks 5.1 and 5.1b; Phase 5 verification. — Review edits leave previous warning acknowledgements valid — decision: Applied
  - PR-MED-011: Goal; D9; Task 5.2. — Appending a learned cue can leave the demonstrated misrouting unchanged — decision: Applied
  - PR-MED-012: Design Decision D5; Task 1.2; Phase 1 verification. — Per-file atomic writes do not make re-enrolment a safe profile replacement — decision: Applied
  - PR-MED-013: Agreed Scope; Task 6.1; Phase 6 verification. — The shared recording protocol does not resolve role-play versus acoustic identity — decision: Applied
  - PR-MED-014: Task 2.4; Phase 2 harness verification. — The inherited scorer suppresses metrics for all-matched enrolled speech — decision: Applied
  - PR-MED-015: Task 5.5; Phase 6 gate verification. — R3’s replacement denominator and removal counting are ambiguous — decision: Applied

### Round 2 - 2026-09-05 - practitioner-profile plan, post-round-1-amendment independent cross-family codex plan peer-review (round 2)
- Round status: Closed (3 of 3 Applied 2026-09-05 as plan amendments; the four unconfirmed round-1 amendments completed; NOT converged — round 3 owed)
- Source: Codex plan peer-review
- Materiality: 2 build-affecting / 1 record-only / 0 invalid
- Compacted 2026-09-05 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-MED-016: D9; Task 5.2; Critical Constraints. — Content-token normalisation defeats the newly mandated name refusal — decision: Applied
  - PR-MED-017: Task 0.6; Task 3.2; Phase 0 handoff sequence. — Task 0.6 requires an app status feature that is built only in Phase 3 — decision: Applied
  - PR-LOW-018: Agreed Scope, Piece 3; Accepted Assumptions; Task 5.5. — Summary wording retains superseded scoring and activation claims — decision: Applied

### Round 3 - 2026-09-05 - practitioner-profile plan, post-round-2-amendment independent cross-family codex plan peer-review (round 3, confirmation)
- Round status: Closed (1 of 1 Applied 2026-09-05, record-only) — PASS CONVERGED at this round
- Source: Codex plan peer-review
- Materiality: 0 build-affecting / 1 record-only / 0 invalid
- Compacted 2026-09-05 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-LOW-019: Planned Workflow Summary, Flow 3; Task 3.2. — Flow 3 still names the wrong model-report surface — decision: Applied

### Round 4 - 2026-09-06 - practitioner-profile Phase 0 (Tasks 0.1 + 0.3), in-session `/review-loop` round 1 of cap 3
- Round status: Closed (4 of 4 Applied 2026-09-06 by Claude Code, one at a time, each verified by re-read; the two code fixes await the composer's suite re-run before round 5)
- Source: Claude Code
- Compacted 2026-09-06 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — scripts/setup-models.py — Applied (2026-09-06, Claude Code — the skip line now says "to fetch a DIFFERENT 
  - LOW-002 — scripts/speaker-embedding-smoke.py — Applied (2026-09-06, Claude Code — `width = max([len(name
  - LOW-003 — this plan, Task 0.4 "How to run it" sub-bullet — the Audacit — Applied (2026-09-06, Claude Code — the Task 0.4 sub-bullet now carries the `WAV_
  - LOW-004 — scripts/speaker-embedding-smoke.py — Applied (2026-09-06, Claude Code — the "One front-end, one load contract" sub-bu

### Round 5 - 2026-09-06 - practitioner-profile Phase 0 (Tasks 0.1 + 0.3), in-session `/review-loop` round 2 of cap 3 (confirmation)
- Round status: Closed (0 findings) — LOOP CONVERGED at this round
- Source: Claude Code
- Compacted 2026-09-06 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 6 - 2026-09-06 - practitioner-profile Phase 0 (Tasks 0.1 + 0.3), independent cross-family codex peer review (first cross-family code round)
- Round status: Closed (4 of 4 Applied 2026-09-06 by the executor's `/fix` leg after the composer routed all four to fixing under `gates=fix-biased`; composer suites green after the fixes and every fix confirmed complete by codex confirmation round 7)
- Source: Codex peer-review
- Compacted 2026-09-06 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-HIGH-002 — scripts/setup-models.py:185 — Candidate downloads can silently lose HTTPS protection before their trust pin is established — Applied
  - PR-MED-018 — scripts/speaker-embedding-smoke.py:228 — Arbitrary model outputs are flattened into purported speaker embeddings — Applied
  - PR-LOW-020 — scripts/speaker-embedding-smoke.py:179 — Dependency and session-setup failures escape the promised typed load boundary — Applied
  - PR-LOW-021 — scripts/speaker-embedding-smoke.py:28 — The smoke overstates its ability to detect a wrong front-end — Applied

### Round 7 - 2026-09-06 - practitioner-profile Phase 0 (Tasks 0.1 + 0.3), independent cross-family codex peer review (confirmation round after round 6)
- Round status: Closed — No new findings — converged
- Source: Codex peer-review
- Compacted 2026-09-06 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".


### Round 8 - 2026-09-15 - practitioner-profile Phase 0 Task 0.5 (the model pin), in-session `/review-loop` round 1 of cap 3
- Round status: Closed (5 of 5 Applied 2026-09-15 by Claude Code, one at a time, each verified by re-read; the two code-file fixes await the composer's suite re-run before round 9)
- Source: Claude Code
- Compacted 2026-09-15 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — scripts/setup-models.py — Applied (the string is now an f-string over the int; the "25.3 MiB" label and da
  - LOW-002 — scripts/setup-models.py — Applied (comment now reads "80-bin Kaldi fbank in, 256-dim embedding out"; licen
  - LOW-003 — desktop/tests/test_setup_scripts.py — Applied (parameter removed; siblings: none — every other `capsys` in the module 
  - LOW-004 — docs/security/data-flow-map.md — Applied (reworded: "until that plan's Task 0.6 promotes it, the file exists only
  - LOW-005 — scripts/README.md — Applied (the entry now says the explicit path reads the promoted `.onnx` or a `.

### Round 9 - 2026-09-15 - practitioner-profile Phase 0 Task 0.5 (the model pin), in-session `/review-loop` round 2 of cap 3 (confirmation)
- Round status: Closed (1 of 1 Applied 2026-09-15 by Claude Code, doc-only) — LOOP CONVERGED at this round
- Source: Claude Code
- Compacted 2026-09-15 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — docs/security/data-flow-map.md — Applied (paragraph re-wrapped, no words changed; doc-only, no suite needed

### Round 10 - 2026-09-15 - practitioner-profile Phase 0 Task 0.5 (pin the model), independent cross-family codex peer review
- Round status: Closed (3 of 3 Applied 2026-09-15 by the executor's `/fix` leg after the composer routed all three to fixing under `gates=fix-biased`; the composer's suite re-run and the codex confirmation round 11 are pending — the round re-opens only if the peer refutes a fix)
- Source: Codex peer-review
- Compacted 2026-09-15 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-LOW-022 — docs/security/data-flow-map.md:125 — HTTPS redirect documentation exceeds the installed guard’s scope — Applied
  - PR-REG-001 — scripts/speaker-embedding-smoke.py:190 — Smoke guidance still promises a candidate file after pinned setup — Applied
  - PR-LOW-023 — .cursor/plans/plan-practitioner-profile.md:466 — Task 0.6’s command is not runnable verbatim from project-root PowerShell — Applied

### Round 11 - 2026-09-15 - practitioner-profile Phase 0 Task 0.5 (pin the model), independent cross-family codex peer review (confirmation round after round 10)
- Round status: Closed — No new findings — converged
- Source: Codex peer-review
- Compacted 2026-09-15 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 12 - 2026-09-15 - practitioner-profile Phase 1 (Tasks 1.1–1.4: embedder, profile custody, enrolment, D15), in-session `/review-loop` round 1 of cap 3
- Round status: Closed (4 of 4 Applied 2026-09-15 by Claude Code, one at a time, each verified by re-read; the three code-file fixes — `speaker_embedding.py`, `practitioner_profile.py` and their tests — await the composer's suite re-run before round 13)
- Source: Claude Code
- Compacted 2026-09-15 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — desktop/src/scribe_desktop/speaker_embedding.py:41 — Applied (both docstrings now say exactly what is held — the caller's map and the
  - LOW-002 — desktop/src/scribe_desktop/practitioner_profile.py:203 — Applied (any other `OSError` is now `StoreWriteError("profile key custody blob i
  - LOW-003 — desktop/src/scribe_desktop/practitioner_profile.py:25 — Applied (both texts now state exactly what the structure enforces — the key is n

### Round 13 - 2026-09-15 - practitioner-profile Phase 1 (Tasks 1.1–1.4), in-session `/review-loop` round 2 of cap 3 (confirmation)
- Round status: Closed (2 of 2 Applied 2026-09-15 by Claude Code, doc-only — no suite owed) — LOOP CONVERGED at this round
- Source: Claude Code
- Compacted 2026-09-15 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — docs/security/threat-model.md — Applied (both now say the hook exists and the app registers the benchmark worker
  - LOW-002 — docs/security/retention-schedule.md — Applied (the row now says a repr, full dump or JSON carries a marker and is drop

### Round 14 - 2026-09-15 - practitioner-profile Phase 1 (Tasks 1.1–1.4), independent cross-family codex peer review (first cross-family code round)
- Round status: Closed (3 of 3 Applied 2026-09-15 by the executor's `/fix` leg after the composer routed all three to fixing under `gates=fix-biased` — PR-HIGH-003 at its executor-verified MED; the composer's suite re-run and the codex confirmation round 15 are pending — the round re-opens only if the peer refutes a fix)
- Source: Codex peer-review
- Compacted 2026-09-15 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-HIGH-003 — desktop/src/scribe_desktop/practitioner_profile.py:292 — Profile validation errors expose sensitive input values — Applied
  - PR-MED-019 — desktop/src/scribe_desktop/enrolment.py:193 — A queued capture failure can be bypassed by target completion — Applied
  - PR-REG-002 — desktop/src/scribe_desktop/practitioner_profile.py:25 — Revised custody wording overstates key continuity — Applied

### Round 15 - 2026-09-15 - practitioner-profile Phase 1 (Tasks 1.1–1.4), independent cross-family codex peer review (confirmation round after round 14)
- Round status: Closed (2 of 2 Applied 2026-09-15 by the executor's `/fix` leg after the composer routed both to fixing under `gates=fix-biased`; the composer's suite re-run and the codex confirmation round 16 are pending — the round re-opens only if the peer refutes a fix)
- Source: Codex peer-review
- Compacted 2026-09-15 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-MED-020 — desktop/src/scribe_desktop/enrolment.py:210 — Cancellation during the final block can return successful PCM — Applied
  - PR-LOW-024 — desktop/tests/test_practitioner_profile.py:224 — Consent error regression does not check the rejected input — Applied

### Round 16 - 2026-09-15 - practitioner-profile Phase 1 (Tasks 1.1–1.4), independent cross-family codex peer review (confirmation round after round 15)
- Round status: Closed
- Source: Codex peer-review
- Compacted 2026-09-15 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 17 - 2026-09-16 - practitioner-profile Phase 2 (Tasks 2.1–2.5: attribution, document fields, auto-confirm, harness, docs), in-session `/review-loop` round 1 of cap 3
- Round status: Closed (4 of 4 Applied 2026-09-16 by Claude Code, one per file, each verified by re-read; the three code-file fixes — `transcription.py`, `ui/transcript.py`, `speaker_eval.py` — and their test pins await the composer's suite re-run before round 18)
- Source: Claude Code
- Compacted 2026-09-16 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — desktop/src/scribe_desktop/transcription.py:1135 — the pipeline's profile/embedder identity guard checked `model_id` and `embedding_dim` but not `model_sha256` — Applied
  - LOW-002 — desktop/src/scribe_desktop/transcription.py:1246 — `has_text` was computed inline — Applied
  - LOW-003 — desktop/src/scribe_desktop/ui/transcript.py:303 — the auto-confirm reset — Applied
  - LOW-004 — desktop/src/scribe_desktop/speaker_eval.py:1013 — `EnrolmentInputs`' default dataclass `repr` rendered the profile — Applied

### Round 18 - 2026-09-16 - practitioner-profile Phase 2 (Tasks 2.1–2.5), in-session `/review-loop` round 2 of cap 3 (confirmation)
- Round status: Closed (no findings) — LOOP CONVERGED at this round
- Source: Claude Code
- Compacted 2026-09-16 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 19 - 2026-09-16 - practitioner-profile Phase 2 (Tasks 2.1–2.5), independent cross-family codex peer review (first cross-family code round)
- Round status: Closed (5 of 5 Applied 2026-09-16 by the executor's `/fix` leg after the composer routed all five to fixing under `gates=fix-biased` — PR-HIGH-004 and PR-HIGH-005 at their executor-verified MED; the composer's suite re-run and the codex confirmation round 20 are pending — the round re-opens only if the peer refutes a fix)
- Source: Codex peer-review
- Compacted 2026-09-16 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-HIGH-004 — desktop/src/scribe_desktop/ui/transcript.py:320 — Generation availability hides the D2 fallback notice — Applied
  - PR-HIGH-005 — desktop/src/scribe_desktop/ui/models.py:897 — Unusable profile blobs can be classified as never enrolled — Applied
  - PR-REG-003 — desktop/src/scribe_desktop/speaker_eval.py:1319 — Leave-one-out preparation bypasses per-recording error isolation — Applied
  - PR-LOW-025 — desktop/src/scribe_desktop/speaker_eval.py:668 — One-cluster output does not prove every segment matched — Applied
  - PR-LOW-026 — docs/security/threat-model.md:404 — Attribution documentation states an unconditional 30-second window cap — Applied

### Round 20 - 2026-09-16 - practitioner-profile Phase 2 (Tasks 2.1–2.5), independent cross-family codex peer review (confirmation round after round 19)
- Round status: Closed (1 of 1 Applied 2026-09-16 by the executor's /fix leg after the composer routed it to fixing under gates=fix-biased; the composer's suite re-run and the codex confirmation round 21 are pending — the round re-opens only if the peer refutes the fix)
- Source: Codex peer-review
- Compacted 2026-09-16 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-REG-004 — desktop/src/scribe_desktop/speaker_eval.py:1330 — Clinician-span extraction still escapes leave-one-out isolation — Applied

### Round 21 - 2026-09-16 - practitioner-profile Phase 2 (Tasks 2.1–2.5), independent cross-family codex peer review (confirmation round after round 20)
- Round status: Closed
- Source: Codex peer-review
- Compacted 2026-09-16 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 22 - 2026-09-16 - practitioner-profile Task 2.6 (the amended D13 remainder rule), in-session `/review-loop` round 1 of cap 3
- Round status: Closed (no findings) — LOOP CONVERGED at this round
- Source: Claude Code
- Compacted 2026-09-16 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 23 - 2026-09-16 - practitioner-profile Task 2.6 (the amended D13 remainder rule), independent cross-family codex peer review
- Round status: Closed (1 of 1 Applied 2026-09-16 by the composer as the completion of its scoped /review-plan sibling sweep — plan prose only, docs-only under gates=fix-biased; the codex confirmation round 24 is pending — the round re-opens only if the peer refutes the fix)
- Source: Codex peer-review
- Compacted 2026-09-16 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-LOW-027 — .cursor/plans/plan-practitioner-profile.md:234 — Current plan summaries still prescribe the retired remainder split — Applied

### Round 24 - 2026-09-16 - practitioner-profile Task 2.6 (the amended D13 remainder rule), independent cross-family codex peer review (confirmation round after round 23)
- Round status: Closed (1 of 1 Applied 2026-09-16 by the composer — plan prose only, docs-only under gates=fix-biased; the pass accept-closes on the tail signature (rounds 23–24 docs-only, nothing pending) per cap-raise=follow)
- Source: Codex peer-review
- Compacted 2026-09-16 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-LOW-028 — .cursor/plans/plan-practitioner-profile.md:217 — Remaining current-plan summaries still prescribe remainder clustering — Applied

### Round 25 - 2026-09-16 - practitioner-profile Phase 3 (Tasks 3.1–3.3: the Practitioner tab, first run + report lines, docs), in-session `/review-loop` round 1 of cap 3
- Round status: Closed (2 of 2 Applied 2026-09-16 by Claude Code, the architect, each verified by re-read; the code fix in `ui/practitioner.py` and its regression test await the composer's suite re-run before round 26)
- Source: Claude Code
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — desktop/tests/test_ui_screens.py:530 — an unused `released` `threading.Event` attribute the spec listed and no test reads (the de — see sidecar

### Round 26 - 2026-09-16 - practitioner-profile Phase 3 (Tasks 3.1–3.3), in-session `/review-loop` round 2 of cap 3 (confirmation)
- Round status: Closed (no findings) — LOOP CONVERGED at this round
- Source: Claude Code
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 27 - 2026-09-16 - practitioner-profile Phase 3 (Tasks 3.1–3.3), independent cross-family codex peer review (first cross-family code round)
- Round status: Closed (7 of 7 Applied 2026-09-16 by Claude Code, the architect, one at a time, each verified by re-read; the code fixes and their tests await the composer's suite re-run before the codex confirmation round 28)
- Source: Codex peer-review
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-HIGH-006 — desktop/src/scribe_desktop/ui/practitioner.py:389 — Unusable profile presence automatically supplies consent — Applied
  - PR-MED-021 — desktop/src/scribe_desktop/ui/practitioner.py:432 — Re-record cannot change the learning opt-in — Applied
  - PR-MED-022 — desktop/src/scribe_desktop/ui/practitioner.py:457 — Enrolment starts before the idle microphone monitor is handed over — Applied
  - PR-LOW-029 — desktop/src/scribe_desktop/ui/models.py:869 — The speaker report labels file presence as readiness — Applied
  - PR-LOW-030 — CHANGELOG.md:8 — Stop/save prose still contradicts the implemented cancellation boundary — Applied
  - PR-LOW-031 — docs/security/threat-model.md:490 — Delete/read race documentation guarantees a fallback that is not unconditional — Applied
  - PR-REG-005 — desktop/src/scribe_desktop/ui/main_window.py:104 — The new profile reads escape the tests’ temporary roots — Applied

### Round 28 - 2026-09-16 - practitioner-profile Phase 3 (Tasks 3.1–3.3), independent cross-family codex peer review (confirmation round)
- Round status: Closed (2 of 2 Applied 2026-09-16 by Claude Code, the architect — prose and one comment, each fixed as a class after a sibling sweep; the comment hunk awaits the composer's ruff + screen-test re-run before the codex confirmation round 29)
- Source: Codex peer-review
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-LOW-032 — docs/security/threat-model.md:412 — Stop/save residue still starts too late in the documentation — Applied
  - PR-LOW-033 — docs/security/threat-model.md:403 — The consultation handoff residue claims an unenforced maximum delay — Applied

### Round 29 - 2026-09-16 - practitioner-profile Phase 3 (Tasks 3.1–3.3), independent cross-family codex peer review (second confirmation round)
- Round status: Closed
- Source: Codex peer-review
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 30 - 2026-09-16 - practitioner-profile Phase 4 (Tasks 4.1–4.5: the per-practitioner cue file — packaged defaults, loader + model, provider from config, tests + docs, the gate-run copy), in-session `/review-loop` round 1 of cap 3
- Round status: Closed (2 of 2 Applied 2026-09-16 by Claude Code, the architect, each verified by re-read; the comment hunk in `note_config.py` means ruff + `test_note_config.py` await the composer's re-run before round 31)
- Source: Claude Code
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — docs/testing/shipping-gate.md:22 — the "Preparing" bullet still described the run's `section_cues.json` as owed "once the pra — see sidecar
  - LOW-002 — desktop/src/scribe_desktop/note_config.py:799 — both models are `frozen=True` and the plan's Schema line calls the field "frozen", but the — see sidecar

### Round 31 - 2026-09-16 - practitioner-profile Phase 4 (Tasks 4.1–4.5), in-session `/review-loop` round 2 of cap 3 (confirmation)
- Round status: Closed (no findings) — LOOP CONVERGED at this round
- Source: Claude Code
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 32 - 2026-09-16 - practitioner-profile Phase 4 (Tasks 4.1–4.5), independent cross-family codex peer review (first cross-family code round)
- Round status: Closed (2 of 2 Applied 2026-09-16 by Claude Code, the architect, in LEG 2 under gates=executor; the `note.py` guard + tests and the two comment hunks await the composer's suite re-run before codex confirmation round 33)
- Source: Codex peer-review
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-HIGH-007 — n/a — [HIGH] `desktop/src/scribe_desktop/note.py:777` — An emptied packaged cue table silently becomes the default — see sidecar
  - PR-LOW-034 — n/a — [LOW] `desktop/src/scribe_desktop/note_config.py:6` — Config immutability and mutation protection remain overstated — see sidecar

### Round 33 - 2026-09-16 - practitioner-profile Phase 4 (Tasks 4.1–4.5), independent cross-family codex peer review (confirmation round)
- Round status: Closed
- Source: Codex peer-review
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 34 - 2026-09-16 - practitioner-profile Phase 5 (Tasks 5.0–5.5: consent v2 + re-consent, review edits, auto-learn on Save, the learned-phrase lists, docs, the R3 clarification), in-session `/review-loop` round 1 of cap 3
- Round status: Closed (3 of 3 Applied 2026-09-16 by Claude Code, the architect, each verified by re-read; the two code hunks in `ui/note.py` + the two new pins in `test_ui_screens.py` await the composer's suite re-run before round 35)
- Source: Claude Code
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — docs/security/threat-model.md:238 — the refusal filter's over-refusal of a capitalised opener outside the transcript's common- — see sidecar
  - LOW-002 — desktop/src/scribe_desktop/ui/note.py:849 — the utterance and section choosers were rebuilt from scratch on EVERY re-finalise, includi — see sidecar
  - LOW-003 — desktop/src/scribe_desktop/ui/note.py:761 — the learning gate was read ONCE at `begin_review` and re-read only at Save, so a practitio — see sidecar

### Round 35 - 2026-09-16 - practitioner-profile Phase 5 (Tasks 5.0–5.5), in-session `/review-loop` round 2 of cap 3 (confirmation)
- Round status: Closed (no findings) — LOOP CONVERGED at this round
- Source: Claude Code
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 36 - 2026-09-16 - practitioner-profile Phase 5 (Tasks 5.0–5.5), independent cross-family codex peer review (first cross-family code round)
- Round status: Closed (8 of 8 Applied 2026-09-16 by Claude Code, the architect, in LEG 2 under gates=executor; the code hunks in `note_config.py` / `ui/note.py` / `ui/practitioner.py` and the eight new pins await the composer's suite re-run before codex confirmation round 37)
- Source: Codex peer-review
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-HIGH-008 — desktop/src/scribe_desktop/ui/note.py:770–771 — Source-position loss weakens the name-refusal gate — Applied (at the verified MED)
  - PR-MED-023 — desktop/src/scribe_desktop/ui/note.py:833–841 — Partial append is reported as no learning and does not refresh the review list — Applied
  - PR-MED-024 — desktop/src/scribe_desktop/note_config.py:1362–1372 — Partial deletion leaves an orphaned plaintext phrase that retry cannot remove — Applied
  - PR-LOW-035 — .cursor/plans/plan-practitioner-profile.md:248 — Normative D9 still specifies per-phrase approval — Applied
  - PR-LOW-036 — docs/security/threat-model.md:609–610 — Threat model incorrectly excludes whole short utterances — Applied
  - PR-LOW-037 — docs/security/threat-model.md:252–253 — Documentation promises dates for every learned phrase — Applied
  - PR-LOW-038 — .cursor/plans/plan-practitioner-profile.md:285 — Critical Constraint conflates edit admission with learner ownership — Applied
  - PR-LOW-039 — docs/testing/shipping-gate.md:54 — Required proposal-only R3 example contains transcript assertions — Applied

### Round 37 - 2026-09-16 - practitioner-profile Phase 5 (Tasks 5.0–5.5), independent cross-family codex peer review (confirmation round)
- Round status: Closed (1 of 1 Applied 2026-09-16 by Claude Code, the architect, in LEG 2 under gates=executor; the two code hunks and six pins await the composer's suite re-run before codex confirmation round 38)
- Source: Codex peer-review
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - PR-MED-025 — desktop/src/scribe_desktop/note_config.py:1260 — Raw temporary-file cleanup errors bypass the committed-cue outcome — Applied

### Round 38 - 2026-09-16 - practitioner-profile Phase 5 (Tasks 5.0–5.5), independent cross-family codex peer review (second confirmation round)
- Round status: Closed
- Source: Codex peer-review
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 39 - 2026-09-17 - practitioner-profile Phase 5 smoke-feedback fix (leg f8c: the Save-note messaging + the not-attributed note), in-session `/review-loop` round 1 of cap 3
- Round status: Closed (2 of 2 Applied 2026-09-17 by Claude Code, the architect, each verified by re-read; the code hunks in `ui/note.py` / `ui/models.py` and the test changes await the composer's suite re-run before round 40)
- Source: Claude Code
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".
  - LOW-001 — desktop/src/scribe_desktop/ui/note.py — `clear()` blanked the learning label and then `_update_controls()` re-rendered it from the — see sidecar
  - LOW-002 — ui/note.py — the learning status (one profile read) was re-read on EVERY Save, including a review with — see sidecar

### Round 40 - 2026-09-17 - practitioner-profile Phase 5 smoke-feedback fix (leg f8c), in-session `/review-loop` round 2 of cap 3 (confirmation)
- Round status: Closed (no findings) — LOOP CONVERGED at this round; one test-side assertion corrected in this leg (below), no production code changed
- Source: Claude Code
- Compacted 2026-09-17 → findings-practitioner-profile.md — full per-finding blocks live in that sidecar; treat a missing or partial sidecar as an error, never as "no findings".

### Round 41 - 2026-09-17 - practitioner-profile Phase 5 smoke-feedback fix (Tasks 5.2/5.3), independent cross-family codex peer review (confirmation round, pass stage-5.p2)` block in the exact `## Review Findings Log` format: `- Round status:` (`Open (N pending)` when N ≥ 1, else `Closed` with `No new findings — converged`), `- Source: Codex peer-review`, `- Baseline:`, `- Files reviewed:`, `- Validation basis:`, `- Finding verification: C candidates / D dropped / E downgraded`; then `#### Confirmed / disputed (rounds 39–40)` — one line per applied LOW; then one subsection per NEW finding (`PR-<SEV>-<nnn>`, continuing from PR-HIGH-008 / PR-MED-025 / PR-LOW-039 / PR-REG-005, full structured fields, `/fix decision: Pending`). A clean round is the valuable result; never invent findings.

=== HARD RULES ===
- REPORT only; write nothing; run no suite. Verify by READING; quote lines; cite `file:line`. Apply the Finding Verification filter; report the counts.
- SAFETY WEIGHT as in rounds 36–38: the refusal filter and the practitioner-only learning rule are the enforcing controls — any weakening is HIGH.
- End with one summary line exactly: PEER-ROUND-41 RESULT: <n> findings (CRIT <a> / HIGH <b> / MED <c> / LOW <d>).

## Tasks
Phases are grouped for `/execute-loop` (a `/review-loop` + cross-family peer pass at each boundary). No phase heading carries `[gates: high-auto-ok]`: this plan touches key custody, a biometric artefact, clinical-record content and a ratified safety relaxation, so HIGH findings pause for the practitioner by design. Every task is `[executor: premium-only]` in substance (Executor tier line); the label is omitted per task because the whole plan is premium.

### Phase 0 — Bookkeeping, consent, model candidate
- [x] 🟩 0.1: **Record the gate pause by pointer.** `plan-phase3a-note-pipeline.md`: a dated bullet at the top of `Current State / Handoff Note` and one sub-bullet under Task 9.1 ("RUN PAUSED 2026-09-05 until `plan-practitioner-profile.md` Phase 3 ships; rubric v1 unchanged; the single recording set is that plan's Task 6.1"), the START HERE step 4 line annotated; `AGENTS.md` Next priority → this plan; CHANGELOG untouched (no code). Verification: the two files read back; the Phase 3A history checker still passes.
  - **Done 2026-09-06 (executor, run stage-0):** the three pointers written into `plan-phase3a-note-pipeline.md` — a new top bullet in `Current State / Handoff Note` (the interim 2026-09-05 bullet kept beneath it as the decision record), the verbatim sub-bullet under Task 9.1, and the START HERE step 4 annotation; `AGENTS.md` "Next priority" already names this plan from Task 0.1 (read, left unchanged); CHANGELOG untouched. Verified: both files read back; `python3 scripts/loop-history-check.py .cursor/plans/plan-phase3a-note-pipeline.md` → 72 + 72, OK (before and after).
- [x] 🟩 0.2: **Ratify the consent text** (PRACTITIONER). Read the draft in `Config / Environment / Deployment Impact`; approve or edit; record "ratified <date>, version consent-v1" on this task. Blocks: 3.1.
  - **Ratified 2026-09-05, version consent-v1, as written** (practitioner, in session: "consent ok") — the post-round-1 wording (plain-text phrases, the no-patient-information judgement the practitioner's) with the off-by-default learning checkbox. Task 3.1 ships this text verbatim as `CONSENT_TEXT_V1`.
- [x] 🟩 0.3: **Add the speaker-embedding candidate to `scripts/setup-models.py`.** Registry entry `speaker-embedding` (candidate name, URL, expected size; SHA-256 pin initially EMPTY = "candidate mode": the script downloads to `models/speaker-embedding/<name>.onnx.candidate`, prints size and SHA-256, and does not promote it until a pin exists; the smoke takes an explicit model path so it can read the candidate file); `--only speaker-embedding` accepted; `scripts/README.md` line. Plus `scripts/speaker-embedding-smoke.py`: loads the ONNX (the `SileroVad` contract), embeds each WAV given on the command line with the numpy front-end (D12), prints the cosine matrix and dims — text-free. Verification: `--help` and the `--only` validation tested without network; the smoke's front-end unit-tested on synthetic PCM (shape, determinism).
  - **Done 2026-09-06 — composer-verified GREEN (14:43–14:46): ruff clean, mypy clean over 30 files, pytest 1408 passed (1368 baseline + 40 new items); venv CLI smoke: `setup-models.py --help` prints the candidate/pinned docstring, `--only bogus` exits 2 (argparse usage error; pinned by `test_unknown_only_is_refused_naming_the_choices`) naming the valid set with `speaker-embedding`, `speaker-embedding-smoke.py --help` prints its usage.** Built 2026-09-06 (executor, run stage-0). `scripts/setup-models.py`: registry constants `SPEAKER_EMBEDDING_NAME` (`wespeaker-voxceleb-resnet34-LM`), `SPEAKER_EMBEDDING_URL` (EMPTY — the plan pinned no URL and an executor never invents one; the practitioner passes `--candidate-url https://...` at Task 0.4 and Task 0.5 records it), `SPEAKER_EMBEDDING_EXPECTED_SIZE` (the plan's "about 26 MiB", marked unverified), `SPEAKER_EMBEDDING_SHA256` (EMPTY = candidate mode), a 1 MiB size floor (an HTML error page is refused instead of reported as a digest); `fetch_speaker_embedding(root, candidate_url=None)` — candidate mode: fetch (https only) to `<name>.onnx.candidate`, print bytes + human size + SHA-256, never promote, an existing candidate re-reported without network; pinned mode (for 0.5/0.6, built now because it is the same function): a present candidate is verified and promoted by one `replace`, a mismatch exits with both digests and leaves the candidate un-promoted, otherwise download-verify-write as silero, `--candidate-url` refused; `--only speaker-embedding` in the valid set (`valid_only_names()`); a run WITHOUT `--only` prints `[skip] speaker-embedding: candidate mode` while unpinned (the standard setup is unchanged) and includes the entry once pinned; `main(argv=None)` for the no-network CLI tests. `scripts/speaker-embedding-smoke.py` (NEW): `--model <path>` (explicit, so the `.candidate` file is readable) + WAVs; `apply_offline_env` + `assert_offline_env`, then `load_session` under the `SileroVad` contract (offline asserted BEFORE `import onnxruntime`, UNC refused, telemetry off, CPU only, `SpeakerModelError` on every load failure); WAVs through `speaker_eval.read_wav_pcm` (16 kHz mono 16-bit enforced); numpy Kaldi-style front-end per D12 (`fbank`: 25 ms / 10 ms snip-edges framing, DC removal, 0.97 pre-emphasis, Hamming default / `--window povey`, 512-point FFT without the Nyquist bin, 80 Kaldi-mel triangles 20 Hz–Nyquist, float32-eps log floor, no dither, per-utterance mean subtraction); `embed` batches rank-3 inputs, refuses a fixed feature dim ≠ 80 as a front-end mismatch, L2-normalises; prints the I/O signature, per-file seconds / frames / dim, the embedding dimension and the cosine matrix — names and numbers only. `scripts/README.md` gains the smoke line. Tests: `desktop/tests/test_setup_scripts.py` (NEW, 40 collected items — 39 functions, one parametrised ×2; both scripts loaded through `importlib` from `scripts/`, `urlopen` replaced by a refusing stub for every test, every fetch pointed at `tmp_path`): CLI `--help` / `--only` / `--candidate-url` validation before the cache is consulted; candidate mode (lands as `.candidate` with size + digest printed, never promoted, no `.part` residue; existing candidate re-reported without network; empty URL, non-https URL and a tiny body refused with nothing written; default run skips visibly); pinned mode (promote, wrong digest refuses and leaves the candidate, `--candidate-url` refused, present match skipped, pinned download verifies / wrong bytes write nothing, pinned-without-URL is a pin-step error); front-end (snip-edges shape for five lengths, float32, finite, bit-identical on identical input, CMN zero-mean vs raw, short / odd / unknown-window refused, Kaldi window endpoints, filterbank geometry, a 1 kHz tone peaking within 120 Hz of the matching filter centre), cosine matrix, text-free rendering; load contract (offline required; missing and UNC refused with `onnxruntime` made un-importable, so a check placed after the import would fail loudly); `main` end-to-end through a fake session on three synthetic WAVs (dims, matrix, durations; a stereo WAV refused; an absent model surfaces as a `SystemExit` naming setup-models). NOT run by the executor (composer-run verification): ruff, mypy, pytest.
- [x] 🟩 0.4: **Fetch the candidate and run the smoke** (PRACTITIONER, normal terminal): `setup-models.py --only speaker-embedding`; report the printed size + SHA-256; record two ~20 s recordings of yourself (different days or rooms if possible) and one other person as 16 kHz mono WAVs; run the smoke; paste the cosine matrix onto this task. Blocks: 0.5, D-P1.
  - **How to run it (written by Task 0.3, 2026-09-06).** The plan pinned NO download URL, so the fetch needs one from you: find the WeSpeaker VoxCeleb ResNet34-LM ONNX export (the plan's first candidate; the model card names its ONNX file), then from a normal PowerShell in the project folder run `.venv\Scripts\python.exe scripts\setup-models.py --only speaker-embedding --candidate-url <https URL of the .onnx>` — it prints the size and SHA-256 and leaves the file as `%LOCALAPPDATA%\ClinikoScribe\models\speaker-embedding\wespeaker-voxceleb-resnet34-LM.onnx.candidate` (not yet promoted). Paste the URL you used, the size and the SHA-256 onto this task. Then `.venv\Scripts\python.exe scripts\speaker-embedding-smoke.py --model "<that .candidate path>" me-1.wav me-2.wav other.wav` (16 kHz, mono, 16-bit PCM WAV — the harness's own recipe, quoted by the reader when it refuses a file: in Audacity, Project Rate 16000 → Tracks > Mix > Mix Stereo Down to Mono → Export Audio > WAV 16-bit PCM) and paste the printed I/O signature and cosine matrix here. ALSO paste (or link) the model card's preprocessing recipe — window type, frame length and shift, number of mel bins, expected sample scale (int16 range or [-1, 1]), and whether it mean-normalises per utterance — beside the matrix: the smoke checks separation and refuses only a wrong bin count; it cannot prove the other settings match, so D-P1 reads the card and the matrix together. If the card says its input is not 80-bin fbank, or names a different window or scale, say so — that is a D-P1 fact, not a smoke failure.
  - **Done 2026-09-15 (practitioner-authorised; the composer ran both commands from the project folder with the sandbox off, writes landed in the real cache — total cache 3.0 GiB).** Fetch: `setup-models.py --only speaker-embedding --candidate-url https://huggingface.co/Wespeaker/wespeaker-voxceleb-resnet34-LM/resolve/main/voxceleb_resnet34_LM.onnx` → candidate `%LOCALAPPDATA%\ClinikoScribe\models\speaker-embedding\wespeaker-voxceleb-resnet34-LM.onnx.candidate`, **size 26530309 bytes (25.3 MiB), SHA-256 `7bb2f06e9df17cdf1ef14ee8a15ab08ed28e8d0ef5054ee135741560df2ec068`**, not promoted. Recordings (Audacity, 16 kHz mono 16-bit PCM; mock content, no real people): `me-1.wav` 29.0 s and `me-2.wav` 23.1 s (different content), `me-3.wav` 23.4 s (the practitioner reading the mock shoulder-assessment script), `other.wav` 27.3 s (a second real person reading the same script). Smoke I/O signature: input `feats` shape `['B','T',80]` float, output `embs` shape `['B',256]` float; front-end 80-bin fbank, hamming, per-utterance mean removed; embedding dim 256. Cosine matrix:
    ```text
                  me-1.wav   me-2.wav   me-3.wav  other.wav
      me-1.wav       1.000      0.890      0.851     -0.008
      me-2.wav       0.890      1.000      0.886      0.012
      me-3.wav       0.851      0.886      1.000      0.001
      other.wav     -0.008      0.012      0.001      1.000
    ```
    Same-speaker 0.851–0.890, different-speaker −0.008–0.012: a gap of ≈0.84, well above the 0.2 D-P1 expects; the same-words/different-voice pair (`me-3` vs `other`) scores 0.001. Model card / repo `config.yaml` (read 2026-09-15): `fbank_args: num_mel_bins 80, frame_length 25, frame_shift 10, dither 1.0` (training; WeSpeaker inference uses no dither), `feat_dim 80`, `embed_dim 256`, ResNet34 TSTP pooling, VoxCeleb2 dev, large-margin fine-tune (`do_lm: true`); WeSpeaker's inference recipe applies Kaldi fbank on int16-scale samples with per-utterance CMN — the smoke's front-end matches (80 bins, 25/10 ms, int16 scale, CMN; window Hamming — WeSpeaker's `kaldi.fbank` default is Povey, which the smoke exposes as `--window povey`; the observed separation is unaffected, D-P1 records this as the one setting to pin at Task 1.1). D-P1 inputs: candidate 1 confirmed; suggested threshold constant = the midpoint ≈ 0.44 (re-checked in Phase 6).
- [x] 🟩 0.5: **Pin the model** — SHA-256, size and URL into `setup-models.py` (candidate mode removed for this entry); `AGENTS.md` step 3 size note; `docs/security/data-flow-map.md` flow 9 (the one network flow) gains the model. Verification: the pin test (a wrong digest refuses, like silero).
  - **Done 2026-09-15 — composer-verified GREEN (17:08–17:11): ruff clean, mypy clean over 30 files, pytest 1431 passed (1427 + 4, 99 s).** Built 2026-09-15 (executor, run stage-0, leg a7). `scripts/setup-models.py`: `SPEAKER_EMBEDDING_URL` = the Hugging Face `resolve/main` URL from Task 0.4, `SPEAKER_EMBEDDING_SIZE_BYTES = 26_530_309` (new; the exact size), `SPEAKER_EMBEDDING_EXPECTED_SIZE` = "26530309 bytes (25.3 MiB), recorded 2026-09-15", `SPEAKER_EMBEDDING_SHA256` = `7bb2f06e…ec068` (trust-on-first-download pin from the practitioner's fetch, the comment says so and notes that `resolve/main` is not an immutable ref — the digest pins the bytes, as for silero); the entry therefore runs the PINNED path: a present `.onnx.candidate` is verified against the pin and promoted (Task 0.6), otherwise download-verify-write, a mismatch refuses and leaves the candidate, `--candidate-url` refused, a run without `--only` includes the entry; the candidate-mode machinery (an EMPTY pin) is kept as generic code for a future candidate and the module docstring now says exactly that — the entry is pinned, candidate mode is the route it took; the `--candidate-url` usage line dropped, the no-URL message and the candidate report no longer name Tasks 0.4/0.5. `AGENTS.md` step 3: the entry is pinned (~25 MiB, WeSpeaker VoxCeleb ResNet34-LM, SHA-256-verified, candidate promoted on the next run, included in a run without `--only`). `docs/security/data-flow-map.md`: flow 8's cache inventory lists the model (and the pre-promotion `.candidate` beside it); flow 9 names the model, its Hugging Face source, its silero-style SHA-256 custody (trust-on-first-download from the practitioner's own fetch) and the https-on-every-hop rule. Tests (`desktop/tests/test_setup_scripts.py`): `test_default_run_includes_the_entry_once_pinned` → `…_under_the_shipped_pin` (no pin monkeypatch); new `TestShippedSpeakerEmbeddingPin` (4 tests, nothing monkeypatches the pin): the constants are a real https `.onnx` URL, the recorded size (above the floor, quoted in the human string) and a 64-hex digest; THE PIN TEST — a candidate whose bytes hash differently is refused naming both digests and left un-promoted with no `<name>.onnx`; `--candidate-url` refused through the real CLI path before any opener; a pinned download of wrong bytes (fake opener, asked for exactly the shipped URL) writes nothing. The unpinned-mode tests keep their `""` monkeypatch (they now test the generic candidate machinery); every test stays network-free (the autouse guard refuses `urlopen` and `build_opener`). NOT run by the executor: ruff, mypy, pytest (expected 1427 + 4 = 1431). NOT run: `setup-models.py` itself (Task 0.6 is the practitioner's promotion).
- [x] 🟩 0.6: **Promote the pinned model** (PRACTITIONER, normal terminal): from the project folder in a normal PowerShell run `.venv\Scripts\python.exe scripts\setup-models.py --only speaker-embedding` (round 10 PR-LOW-023: the verbatim command) — it verifies the candidate file against the pin and promotes it to `wespeaker-voxceleb-resnet34-LM.onnx` (PR-MED-006) — then confirm from the script's own output the promoted filename and the verified digest (optionally re-run the Task 0.3 smoke against the promoted path). The in-app report of the model's presence is Task 3.2's (the microphone screen's model status label, `refresh_model_status`) and is checked at the Task 3.4 smoke, not here (round 2 PR-MED-017). Blocks: D-P1's Phase 1 start.
  - **Done 2026-09-15 17:32 (practitioner-authorised; the composer ran it from the project folder with the sandbox off):** `setup-models.py --only speaker-embedding` → `[ok  ] speaker-embedding promoted from candidate -> %LOCALAPPDATA%\ClinikoScribe\models\speaker-embedding\wespeaker-voxceleb-resnet34-LM.onnx`, size 26530309 bytes, SHA-256 `7bb2f06e9df17cdf1ef14ee8a15ab08ed28e8d0ef5054ee135741560df2ec068` (verified against the pin); the directory holds only the promoted `.onnx` (no candidate left); an independent re-hash of the promoted file matches. Task 3.2's in-app status label is checked at the Task 3.4 smoke, as the task says.
- [x] 🟩 D-P1: **Choose the model and the attribution threshold.**  `[decision]`
  - Options: candidate 1 (WeSpeaker ResNet34-LM) / candidate 2 (ECAPA-TDNN export) / spectral features only (`SpectralSpeakerEmbedder`, D16 — no model)
  - Decide after: Task 0.4's cosine matrix shows same-speaker similarity clearly above different-speaker (a gap of at least 0.2 is the expected shape); the threshold constant is set at the midpoint and re-checked in Phase 6
  - Blocks: 1.1, 2.1
  - **Chosen 2026-09-15 (practitioner):** candidate 1 — WeSpeaker VoxCeleb ResNet34-LM ONNX (pinned at Task 0.5, promoted at Task 0.6; `SHIPPED_SPEAKER_EMBEDDER` = the onnx embedder, 256-dim, `model_id` naming this export); attribution threshold constant **0.50** (stricter than the 0.44 midpoint the composer recommended — the practitioner chose fewer false matches over recall; same-speaker 0.85–0.89 vs other ≈0.00 on the Task 0.4 smoke); re-checked in Phase 6 against the full recording set. Front-end for Task 1.1: 80-bin Kaldi fbank, 25/10 ms, int16-scale samples, per-utterance CMN; window Povey (WeSpeaker's `kaldi.fbank` default) — pin it in `speaker_embedding.py` and retire the smoke's private copy per the Task 1.1 sub-bullet.

### Phase 1 — Embedder and profile custody (foundation)
- [x] 🟩 1.1: **`speaker_embedding.py` (new).** `SpeakerEmbedder` (protocol) + `OnnxSpeakerEmbedder(model_path)`: `assert_offline_env` before `import onnxruntime`; `SessionOptions`, telemetry off, UNC refused, `SpeakerModelError` at load (missing/corrupt/wrong I/O shape, probed at construction — the `VadModelError` contract); `embed(pcm16: bytes) -> np.ndarray` L2-normalised; the numpy fbank front-end (D12) as `_fbank(pcm)`; `default_speaker_model_path()`, `SHIPPED_SPEAKER_EMBEDDER` (D-P1's choice), `speaker_embedder_available()` (spectral: always; onnx: a STAT-only, UNC-safe probe of the pinned file), `build_speaker_embedder()` returning the selected class (D16), `MockSpeakerEmbedder` (deterministic vectors keyed by a caller-supplied map) for tests. `model_id` + `model_sha256` properties — the digest computed from the model FILE'S BYTES once at construction and checked against the pinned value, a mismatch raising `SpeakerModelError` so a shape-compatible substitute cannot pass as the pinned model (round 1 PR-MED-003); `SpectralSpeakerEmbedder` alongside (D16). Verification: Phase 1 tests; mypy override for onnxruntime already exists.
  - **One front-end, one load contract (round 4 LOW-004, 2026-09-06):** `scripts/speaker-embedding-smoke.py` carries a PRIVATE copy of the fbank front-end, the load contract and a script-local `SpeakerModelError` so the D-P1 smoke can run before this module exists. When this task lands, the smoke imports `_fbank` (or the embedder) and `SpeakerModelError` from `speaker_embedding.py` and its private copies are retired, and the Task 0.3 front-end tests in `desktop/tests/test_setup_scripts.py` move to (or are superseded by) the module's suite — the D-P1 evidence and the shipped front-end must be the same code. The smoke keeps its explicit `--model` path (the `.candidate` file) and the `--window` flag.
  - **Built 2026-09-15 (executor, run stage-1, leg b1) — NOT yet composer-verified.** `desktop/src/scribe_desktop/speaker_embedding.py` (new): the pin constants `SPEAKER_MODEL_NAME` / `SPEAKER_MODEL_SUBDIR` / `SPEAKER_MODEL_SIZE_BYTES` / `SPEAKER_MODEL_SHA256` defined ONCE here and IMPORTED by `scripts/setup-models.py` (its `SPEAKER_EMBEDDING_*` names are now aliases; `speaker_embedding_paths` builds from the same subdirectory constant; `_sha256_of` delegates to the module's streamed `sha256_of_file`) — never a second literal; `ONNX_MODEL_ID` (= the export name), `SPECTRAL_MODEL_ID = "spectral-cmn-v1"`, `MOCK_MODEL_ID`; `SHIPPED_SPEAKER_EMBEDDER = "onnx"` and `ATTRIBUTION_THRESHOLD = 0.50` (D-P1); the front-end (`fbank`, `mel_filterbank`, `analysis_window`, `filter_center_frequencies_hz`, geometry constants) moved verbatim from the smoke with `DEFAULT_WINDOW = "povey"` and the filterbank/windows cached READ-ONLY; `load_onnx_session(path, *, expected_sha256)` = the smoke's contract plus the digest check (before the import; mismatch names both digests); `embed_features` + `_utterance_vector` moved; `SpeakerEmbedder` protocol (`model_id`, `model_sha256`, `embedding_dim`, `embed`); `OnnxSpeakerEmbedder(model_path=None)` — offline, UNC, missing, digest vs pin (PR-MED-003), typed import, options/telemetry/session, then the I/O probe and ONE smoke inference on 1 s of silence so `embedding_dim` is KNOWN and a wrong layout dies at load; `SpectralSpeakerEmbedder` (dim 25, wraps `transcription._segment_embedding` through a deferred import, L2-normalised); `MockSpeakerEmbedder(vectors_by_exact_pcm, embedding_dim=4)` (mapped or digest-derived deterministic vectors; records LENGTHS only); `speaker_model_available` (STAT-only, UNC-safe, mirrors `vad_model_available`), `speaker_embedder_available(kind)`, `build_speaker_embedder(kind)` (raises for onnx when absent — never substitutes spectral, PR-MED-005). Smoke: `scripts/speaker-embedding-smoke.py` imports `fbank` / `load_onnx_session` / `embed_features` / `SpeakerModelError` (module names `load_session` / `embed` kept for the tests' monkeypatch), keeps `--model`, `--window` (default now the shipped Povey; `hamming` reproduces the Task 0.4 matrix), `describe_io`, `cosine_matrix`, `render_matrix`; `scripts/README.md` line updated. Tests: `desktop/tests/test_speaker_embedding.py` (new): the D-P1 constants and the single-sourcing pin (the script's constants ARE the module's objects), the moved front-end tests (+ the Povey default, read-only caches, offline required), the load contract through `sys.modules` stubs (offline, missing/UNC before import, DIGEST MISMATCH before import, un-importable runtime typed, step-named failure, CPU-only + telemetry off), `embed_features`, `OnnxSpeakerEmbedder` through a fake runtime (probe + one smoke inference, wrong layout / feature axis / raising run fail at load, default path), the spectral and mock embedders, availability + factory, and a skip-if-absent real-model test; `test_setup_scripts.py` keeps the smoke's `main`-path and matrix tests and pins the identity of the imported objects.
- [x] 🟩 1.2: **`practitioner_profile.py` (new).** `PractitionerProfile` (frozen pydantic: `schema_version=1`, `model_id`, `model_sha256`, `embedding: tuple[float, ...]`, `embedding_dim`, `created_at`, `enrolment_speech_seconds`, `device_name`, `consent: ConsentRecord(accepted_at, consent_text_version, learning_opt_in)`); `default_profile_root()`; `save_profile(profile)` (first enrolment: fresh `SessionCrypto`, DPAPI-wrap with the profile description, key written first; re-enrolment: the EXISTING key reused and only `voice.enc` replaced atomically — D5, PR-MED-012; AES-GCM with the v1 AAD; fault-injection tests around each step prove a failed re-enrolment leaves the prior profile usable); `load_profile() -> PractitionerProfile | None` (absent → None; unreadable/undecryptable/wrong AAD/wrong model → `ProfileUnusableError` naming which); `delete_profile()` (key unlink first, then blob, idempotent); the DPAPI helpers in `session_store.py` gain a `description` parameter (session callers unchanged). No logging anywhere in the module. Verification: Phase 1 tests incl. the log-tripwire fixture extension.
  - **Built 2026-09-15 (executor, run stage-1, leg b1) — NOT yet composer-verified.** `desktop/src/scribe_desktop/practitioner_profile.py` (new): `ConsentRecord` + `PractitionerProfile` (frozen, `extra="forbid"`, `schema_version: Literal[1]`; `model_id` pattern, `model_sha256` 64-hex-or-empty, `embedding` finite and non-zero with `embedding_dim` agreement, aware timestamps, finite positive speech seconds, single-line printable `device_name`; `to_bytes`/`from_bytes` canonical JSON); `default_profile_root()` (mirrors `default_sessions_root`, no UNC refusal, same residual); `save_profile` (first enrolment: fresh `SessionCrypto`, key wrapped and written FIRST with `PROFILE_KEY_DESCRIPTION`, then `voice.enc` via `atomic_write_bytes` under AAD `PROFILE_AAD`; re-enrolment: the existing key unwrapped and reused, only the blob replaced; an existing key that cannot be unwrapped REFUSES the save typed (`reason="key"`) and touches nothing; the in-memory key destroyed on every exit); `load_profile(root, model_id, model_sha256)` → `None` when `voice.enc` is absent (a key with no blob is absent), else `ProfileUnusableError(reason ∈ key | blob | authentication | malformed | model)` — the D3 "model mismatch → ABSENT for attribution" reading is the CONSUMER's (Phase 2 treats `model` as absent and D2 reports it), the loader names which; `profile_present()` (a stat, for D10); `delete_profile()` key-first, idempotent, `StoreWriteError` naming the file on any other unlink failure. `session_store.py`: `SESSION_KEY_DESCRIPTION` constant; `wrap_key_to_file(..., *, description=)` and `unwrap_key_from_file(..., *, description=)` — the unwrap now VERIFIES the blob's stored description (a session key blob presented as the profile key, or the reverse, is `KeyCustodyError("wrapped for a different store")`); session callers unchanged. `logging_setup.py`: `_PAYLOAD_SIGNATURES` gains `embedding` / `enrolment_speech_seconds` / `consent_text_version` in quoted and unquoted forms. No logging import anywhere in the module (pinned). Tests: `desktop/tests/test_practitioner_profile.py` (new): the model boundary; tripwire (profile and consent reprs/dumps/JSON dropped, markers disjoint from EVERY `ALLOWED_KEYS` rendering, no logging channel); Windows custody — round trip (exactly `key.dpapi` + `voice.enc`, ciphertext only), key-first deletion order, typed delete failure, re-enrolment keeps the key bytes and replaces only the blob, fault injection (blob write fails on re-enrolment → old profile loads; key write fails first time → nothing on disk; blob write fails after the key → absent, retry reuses the key), unusable key refuses the save, in-memory key destroyed after save AND load, every `reason` (blob-without-key, dead key, truncated/empty/tampered blob, wrong AAD, authentic-but-invalid content — message carries no pydantic detail, model id / sha mismatch, unreadable blob, a session key blob in the profile root and the reverse).
- [x] 🟩 1.3: **`enrolment.py` (new).** `record_enrolment(backend, device_id, *, target_speech_seconds=30.0, max_seconds=90.0, on_progress) -> bytes` — opens `CaptureBackend.open_stream` into an in-memory buffer (the microphone screen's monitor-stream shape), VAD-gates with `SileroVad` to count speech seconds, stops at the target or the cap, returns PCM (raises `EnrolmentTooShortError` under the target); `enrol(pcm, embedder) -> tuple[np.ndarray, float]` (mean of per-VAD-segment embeddings, L2-normalised, speech seconds); the caller drops the PCM. Never creates a session or touches any store; it runs INSIDE `SessionController.begin_enrolment()` (D15) so Start/Resume, the monitor poll and the benchmark refuse meanwhile. Verification: mock-backend tests; "no file written anywhere" pinned with a temp `LOCALAPPDATA`.
  - **Built 2026-09-15 (executor, run stage-1, leg b1) — NOT yet composer-verified.** `desktop/src/scribe_desktop/enrolment.py` (new): `record_enrolment(backend, device_id, *, frame_probability=None → SileroVad, target_speech_seconds=30.0, max_seconds=90.0, on_progress, should_stop)` — `open_stream` into a bounded queue (device thread) consumed on the CALLING thread (the Phase 3 `TaskThread`): VAD-frames each block against `START_THRESHOLD` for a running speech count, `EnrolmentProgress(speech_seconds, captured_seconds, level)` per block, returns the PCM at the target, `EnrolmentTooShortError` at the cap (numbers only), `EnrolmentCaptureError` for open failure / device loss / queue overflow / 10 s of silence from the stream, `EnrolmentCancelledError` via `should_stop`; on EVERY exit the stream is stopped, the buffers cleared and the VAD reset (`segment_pcm`'s reset contract); `enrol(pcm, embedder, *, frame_probability=None) -> (vector, speech_seconds)` — `segment_pcm` with the shipped thresholds, one `embedder.embed` per segment, L2-normalised mean, `EnrolmentTooShortError` on no speech, `EnrolmentError` on a cancelling mean. `session.py` (D15): `EnrolmentLease` token; `begin_enrolment()` refuses (`SessionActivityError`) while a session is recording / paused / processing (transcription runs in `processing`), while a discard reservation is held, while the registered blocker returns a reason, or while another enrolment is held; `end_enrolment(lease)` idempotent / foreign token raises; `enrolling` property; `set_enrolment_blocker(callable → str | None)` — the app registers the microphone screen's benchmark (`is_busy`) here at Task 3.1/3.2 wiring; `start()` and `resume()` refuse while enrolling (before any state check). `ui/microphone.py`: `_poll_level` closes the monitor and does NOT reopen it on any tick while `controller.enrolling` (not an error state; the tab shows its own level), resuming when released; `on_run_benchmark` refuses while enrolling with a visible message. `ui/models.py`: `SessionControllerLike.enrolling`. Tests: `desktop/tests/test_enrolment.py` (new): `record_enrolment` on a worker thread over `MockCaptureBackend` with a level VAD stub — stops at the target and releases the device (PCM is exactly the delivered prefix; VAD reset twice; NOTHING under a temporary `LOCALAPPDATA`), too-short at the cap (nothing persisted), device loss, open failure, cancellation, a silent stream times out, queue overflow, parameter validation; `enrol` — exact mean through a per-segment vector map, no speech / empty refused, cancelling vectors refused; the D15 tokens (second acquisition, foreign token, blocker reason, discard reservation, resume refused before any state check) and BOTH start orders on Windows (enrolment → Start refused with no session dir created; Start → begin refused in recording / paused / processing, allowed on queued, and Start refused without retiring the queued session); `test_ui_screens.py`: the poll tick during enrolment (closed, stays closed, reopens on release) and the benchmark refusal.
- [x] 🟩 1.4: **Phase 1 tests + docs stubs.** `tests/test_speaker_embedding.py`, `tests/test_practitioner_profile.py`, `tests/test_enrolment.py`; the retention-schedule row and the threat-model surface drafted (finalised in 3.3). Verification: the Phase 1 list in `Validation / Verification`; suite green.
  - **Done 2026-09-15 — composer-verified GREEN (18:17, after the b2 test-fixture fix): ruff clean, mypy clean over 33 source files, pytest 1513 passed in 71 s (1431 baseline − 21 smoke items moved or superseded + 103 new).** The same results close Tasks 1.1–1.3 (their records above were written at build time; the one failure the first run found — `test_cancelling_vectors_are_refused` — was the test's own fixture, fixed in leg b2, no production code changed). The three test modules are recorded under Tasks 1.1–1.3. Docs (DRAFT, finalised at 3.3): `docs/security/retention-schedule.md` — the voice-profile row (indefinite until deleted, never swept, content, DPAPI description binding, key-first deletion, tripwire markers) and the speaker model added to the model-cache row; `docs/security/threat-model.md` — a new "Practitioner profile — Phase 1 foundation" section with five surfaces (the biometric derivative at rest and what the structure enforces + residuals; no enrolment audio persisted; never logged; the model pinned by digest not shape; enrolment's exclusive microphone as a correctness guard, not a boundary) — the D4 responsibility boundary is deferred to Task 3.3 as planned; `docs/security/data-flow-map.md` — the no-clinical-content-in-logs non-flow names the profile markers (the profile store / enrolment flow itself is Task 3.3's flow 12); `CHANGELOG.md` Added entry. Forward notes: (a) Task 3.1/3.2 must wire `controller.set_enrolment_blocker(lambda: "a benchmark is running" if microphone_screen.is_busy else None)` in `MainWindow` — the controller cannot see the benchmark `TaskThread` otherwise; (b) `data-flow-map.md` flow 8 still says the speaker model "exists only as `.onnx.candidate` until Task 0.6 promotes it" — stale since 0.6 (2026-09-15), out of Phase 1's scope, to be corrected at Task 3.3.

### Phase 2 — Attribution in the pipeline, auto-confirm, harness
- [x] 🟩 2.1: **`transcription.py` attribution.** *(The remainder clause of this spec — `_cluster_embeddings` over the rest as `speaker_2`/`speaker_3` — is SUPERSEDED by Task 2.6, 2026-09-16: the whole remainder is `speaker_2` until D-S1; the rest of this record is the as-built history.)* `transcribe_session(..., speaker_embedder: SpeakerEmbedder | None = None, enrolled_profile: PractitionerProfile | None = None)`: inside the windowed loop compute the model embedding per segment when both are supplied (the spectral embedding still computed for the remainder clustering); at the end `attribute_speakers(similarities, spectral_embeddings, threshold)` → labels per D13 (matched → `speaker_1`; the rest → `_cluster_embeddings` among themselves as `speaker_2`/`speaker_3`; zero matches → ordinary 2-means over all segments with the higher-similarity cluster as `speaker_1`; degenerate policy unchanged and still mirrored with `label_speakers`); set `enrolled_speaker` (always when a profile is applied and at least one cluster holds a segment with text — D3/D13; a document with no segments carries no attribution fields), `enrolment_similarity` (raw cosine in [-1, 1]), `speaker_model_id` on the document; without both inputs the current path runs byte-for-byte. `ui/models.py` `build_transcriber` AND `build_recovery_runner` pass `build_speaker_embedder()` + the profile when `speaker_embedder_available()` and a profile loads whose `model_id` matches the selected embedder (else report the fallback, D2); `recover_session_transcription` gains the same two parameters. Verification: Phase 2 tests; the existing batching/plaintext-bound tests unchanged.
  - **Done 2026-09-16 — composer-verified GREEN (02:17, after the c3/c4 fix legs): ruff clean, mypy clean over 33 source files, pytest 1624 passed in 75 s (1524 baseline + 100 new items across the four Phase 2 test modules; no existing item changed except the two adapted harness CLI stubs).** The same results close Tasks 2.2–2.5. Built 2026-09-15/16 (executor, run stage-2, legs c1 → c2-resume). `desktop/src/scribe_desktop/transcription.py`: `transcribe_session(..., speaker_embedder=None, enrolled_profile=None)` — one without the other, or a profile whose `model_id` / `embedding_dim` differ from the embedder's, is a `ValueError` (the composition layer never calls it that way); with both, numpy is taken even for a single segment, the profile vector is unit-normalised once, and INSIDE the windowed loop each segment slice (the same `window_pcm[lo:hi]` the spectral embedding uses) is embedded by the model and reduced to ONE raw cosine (`_cosine`: a zero-norm or non-finite embedding scores -1, float dust bounded to [-1, 1] — never a 0–1 clamp) before the loop advances, so no model embedding outlives an iteration; a slice shorter than one front-end frame (`MIN_ATTRIBUTION_PCM_BYTES` = `FRAME_LENGTH` × 2, single-sourced from `speaker_embedding`) gets no similarity instead of a refusal. New pure functions: `attribute_speakers(similarities, embeddings, has_text, np, *, threshold=ATTRIBUTION_THRESHOLD)` — D13: matched (≥ threshold, a missing similarity never matches) → `speaker_1`, the remainder `_cluster_embeddings` among themselves renamed `speaker_2`/`speaker_3` by first appearance (a lone or degenerate remainder is all `speaker_2`); zero matches → ordinary 2-means and the higher-mean-similarity cluster AMONG CLUSTERS WITH TEXT becomes `speaker_1` (labels swapped), with no textual cluster the clustering's first-appearance order stands; `enrolled_cluster(labels, similarities, has_text)` — the D3/D13/PR-MED-008 rule: highest `math.fsum` mean similarity among labels holding ≥1 textual segment and ≥1 similarity, ties to the first-appearing label, `None` when nothing qualifies. The degenerate policy (any empty slice / fewer than two embeddings → all `speaker_1`) is UNCHANGED and still mirrored with `label_speakers`; `SPEAKER_3` constant added. The document gets the three fields together (all three set iff `enrolled_cluster` chose one; `speaker_model_id` = the embedder's id). `recover_session_transcription` passes both through. Decision inside the plan's latitude: the `has_text` per-segment flag is threaded into `attribute_speakers` because D13 scopes BOTH the zero-match choice and `enrolled_speaker` to clusters holding text. Tests: `desktop/tests/test_attribution.py` (new; numpy-gated pipeline tests inject `MockSpeakerEmbedder` keyed by the pipeline's own segment slices from a plain first run — matched/remainder/all-matched/zero-match-swap/`speaker_3`/single segment/negative raw cosine/textless matched cluster/no text/no segments/byte-identical without inputs/one-input and foreign-profile refusals/the recovery entry point on Windows; pure tests over `attribute_speakers` and `enrolled_cluster`). The existing `test_transcription.py` is untouched.
- [x] 🟩 2.2: **`TranscriptDocument` fields** (Schema / Data Changes) with the validators `enrolled_speaker ∈ {labels of segments that have transcribed text}` and `enrolment_similarity` finite in [-1, 1]; `read_transcript` round-trip; `models.speaker_quotations` unchanged. Verification: round-trip + validator tests.
  - **Built 2026-09-15 (executor, run stage-2, leg c1) — NOT yet composer-verified.** `TranscriptDocument` gains `enrolled_speaker: str | None`, `enrolment_similarity: float | None`, `speaker_model_id: str | None` (all default `None`; `schema_version` stays 1; an artefact JSON without the keys parses unchanged — pinned). Validators: `enrolment_similarity` finite and in [-1, 1]; `speaker_model_id` non-empty when set; a model validator requires the THREE fields set together or not at all (a tightening within the plan's latitude, recorded here — it lets the UI treat the trio as one fact) and `enrolled_speaker ∈ {labels of segments with transcribed text}` — the lying-field defence at construction. `segment_has_text(segment)` is the ONE definition of "holds text" (any non-blank word), pinned equal to the key set of `models.speaker_quotations` (the radios' source); `speaker_quotations` and `format_transcript_text` are unchanged. (Round 17 LOW-002: the one definition is now `words_have_text(words)`, with `segment_has_text` delegating to it and the pipeline calling it per segment.) Tests in `test_attribution.py`: round trip through `to_bytes`/`from_bytes` and `write_transcript`/`read_transcript`, the old-artefact read, each refusal (absent label, textless label, out-of-range/NaN/inf similarity, every partial combination, empty model id), the whole cosine range accepted.
- [x] 🟩 2.3: **Auto-confirm on the Transcript screen (D4).** `_populate_generation_controls`: when `document.enrolled_speaker` is set, pre-check that radio, render the line "Clinician: confirmed from your voice profile (similarity 0.xx) — change" (a link-styled button); Generate stays gated on the template profile, config, recovery and lease exactly as today (PR-MED-009); "change" un-checks and shows the manual radios with `speaker_role`'s " (suggested)"; when `None`, today's behaviour. `set_role` / `_selected_role` unchanged (the selection stays the UI state `generate()` reads). Status line for the fallback cases (D2). Verification: offscreen tests listed for Phase 2; the lying-field defence (a document whose `enrolled_speaker` names no segment is refused at construction, so the UI never sees it).
  - **Built 2026-09-15 (executor, run stage-2, leg c1) — NOT yet composer-verified.** `ui/models.py` (the D2/D3/D16 composition layer, the other half of Task 2.1's factory work): `AttributionReadiness(profile_present, profile, reason)` + `attribution_readiness(profile_root=None, kind=SHIPPED_SPEAKER_EMBEDDER)` — a GUI-safe probe (a stat, a stat, a profile read; NO model loaded): no `voice.enc` → nothing to report; model file absent → `SPEAKER_MODEL_MISSING_REASON`; the profile read against `shipped_embedder_identity()` (NEW in `speaker_embedding.py`: `(ONNX_MODEL_ID, SPEAKER_MODEL_SHA256)` or `(SPECTRAL_MODEL_ID, "")` — known without loading, because the onnx embedder refuses any other digest) → `reason="model"` → `PROFILE_REENROL_REASON`, any other unusable reason → `PROFILE_UNUSABLE_REASON.format(reason=<word>)` (structural word only, never a field); `attribution_inputs(kind)` — the WORKER step both factories call at run time: readiness → `build_speaker_embedder` (a `SpeakerModelError` falls back to `(None, None)`; `OfflineEnvError` deliberately not caught) → the profile re-checked against the BUILT embedder's `model_id` / `model_sha256` / `embedding_dim`; `build_transcriber(model_name=None, *, attribution=attribution_inputs)` and `build_recovery_runner(...)` both pass `speaker_embedder=` / `enrolled_profile=` (the keyword is the test seam; the app's callers pass nothing). `ATTRIBUTION_DID_NOT_RUN_REASON` covers the residual the probe cannot see (a model FILE present but refusing to load): the screen reads that off the document. `ui/transcript.py`: `attribution_readiness_provider` injected (default the real probe); a plain-text `attribution_label` + link-styled flat `change_role_button` ("change") in a row above the radios; `role_prompt_label` is now an attribute; in `_populate_generation_controls`, when `enrolled_speaker` + `enrolment_similarity` are set (and the radio exists — belt and braces, the validator is the defence) the radio is checked, the line "Clinician: confirmed from your voice profile (similarity 0.xx) -" is shown (ASCII hyphen, the app's label convention; the plan's em dash is the same line) with the button, and the manual radios + prompt are HIDDEN (replaced, not merely pre-checked); otherwise, with transcribed speech, the readiness probe runs and a present profile yields its reason line (no button, radios visible, none checked); `change_role()` un-checks via the group's exclusivity toggle, hides the line, shows the radios (with `speaker_role`'s " (suggested)" marker they always carried) and re-runs `_update_controls` — Generate is then disabled until a radio is chosen; `role_auto_confirmed` property; `_reset_generation_state` clears the line; `change_role_button` enabled with the other controls. `set_role` / `_selected_role` / `generate()` unchanged — the pre-check satisfies the ROLE predicate only (PR-MED-009; pinned: Generate disabled until the template profile is chosen). NOT touched: `model_report_lines` (the model-presence line on the microphone screen is Task 3.2's by Task 0.6's record). Tests: `test_ui_models.py` (readiness: each branch, the shipped identity passed to the loader, no model built on the GUI path, the structural-word-only line, a vanished blob; inputs: no profile / load failure / identity re-check on id, sha and dim; BOTH factories parametrised with an injected resolver — resolved at call time, passed through by keyword, `(None, None)` on fallback); `test_ui_screens.py::TestTranscriptAutoConfirm` (pre-check + exact line + radios hidden + Generate still profile-gated; a negative similarity shown raw; `change` reverts with the suggestion marker and re-gates Generate; `generate()` reads the pre-checked role; the manual path when `None`; the three D2 reason lines and the did-not-run line; the probe not consulted for an attributed document; the recovered view; a new document resets; the lying field refused at construction for an absent AND a textless label).
- [x] 🟩 2.4: **`speaker_eval.py` enrolled condition.** `--enrolment <wav>` (16 kHz mono; refused otherwise) → the enrolment vector via `enrol()`; a third `ConditionResult` "enrolled" over the shipped pipeline with the embedder + a synthetic in-memory profile; the enrolled condition computes `cluster_metrics` even when every predicted label is `speaker_1` (an all-matched output is a measurable false-positive case, not a merged one — PR-MED-014; the legacy two conditions keep their merged semantics); a `RoleOutcome` for the auto-confirmed label (correct when the matched cluster is mostly the `clinician` label); leave-one-out from labelled clinician spans as the documented fallback when no enrolment WAV is given; report columns; `scripts/measure-speakers.py` flag; `docs/testing/speaker-measurement.md`. Teardown contract untouched (the profile is in memory only — never written by the harness). Verification: harness tests with `MockSpeakerEmbedder`.
  - **Built 2026-09-15 (executor, run stage-2, leg c1) — NOT yet composer-verified.** `speaker_eval.py`: `ENROLLED` condition; `EnrolmentInputs(embedder, profile)` + `enrolment_inputs(pcm, embedder, frame_probability)` — the shipped `enrolment.enrol` wrapped in an in-memory `PractitionerProfile` (`HARNESS_DEVICE_NAME`, `consent-v1`, `learning_opt_in=False`); the module imports NO profile store (pinned: `save_profile` / `delete_profile` / `default_profile_root` absent from its namespace), so the teardown contract is untouched; `clinician_pcm(pcm, track)` (the clinician-labelled spans, clipped, in track order); `_ReplayProvider` — the plain pass records each window's words by the SHA-256 of the window PCM and the enrolled pass replays them, so the second `transcribe_session` over the same temporary store costs no second Whisper run (design decision inside the plan's latitude: the plan-faithful two-pass design without the 2× cost; an unseen window still reaches the real provider); `_transcribe_in_temporary_store(..., enrolment=None)` returns `(document, enrolled_document | None)` under the unchanged key-first teardown on every path; `evaluate_recording(..., enrolment=None)` adds a `HarnessFaultError` when the enrolled pass segmented the store differently; `score_document(..., enrolled_document=None)` → the third `ConditionResult` (`enrolled=True`: never `merged` — a one-cluster output IS scored whatever produced it, every segment matching or a degenerate clustering after none did, PR-MED-014 / peer round 19 PR-LOW-025; `auto_confirm: RoleOutcome | None` via `auto_confirm_outcome(document, labels, truths)` — `role_outcome` judged for `enrolled_speaker`, with `RoleOutcome.margin` carrying `enrolment_similarity` as its docstring now says; `enrolment_similarity`); `RecordingResult.has_condition`, `condition()` raises `KeyError` for an absent one; the report gains `Auto-confirm | Auto-confirmed | Similarity` columns and an `enrolled` aggregate row (with three auto-confirm count columns, `-` for the legacy rows; no `enrolled` row when it was not measured; the existing row prefixes still match); `main`: `--enrolment WAV` (16 kHz mono, refused like a recording; the shipped `build_speaker_embedder()` — absent model = error under the flag), else with ≥2 pairs the LEAVE-ONE-OUT fallback (`_leave_one_out_pool`: each recording's clinician spans; each recording enrolled from the OTHERS' pool, a pool with no speech skips that recording's condition visibly; an absent model prints `[skip] enrolled condition: …`; one pair prints the `give --enrolment` hint). `scripts/measure-speakers.py` docstring shows the flag; `docs/testing/speaker-measurement.md` gains "The enrolled condition" (what it measures, the replay, the in-memory profile, the leave-one-out stand-in and why Task 6.1's read-aloud is what Phase 6 records, the two new verdict columns) and the intro names Phase 6's threshold re-check. Tests (`test_speaker_eval.py`): the two CLI tests' stubs now accept the `enrolment` keyword and stub `build_speaker_embedder` (the leave-one-out path would otherwise touch the real model), plus `TestEnrolmentInputs`, `TestAutoConfirmOutcome`, `TestScoreDocumentEnrolled` (third condition + report cells, all-matched scored vs merged, absent condition absent from the report, count mismatch, text-free rows), Windows `TestEvaluateRecordingEnrolled` with an ML-free zero-crossing `_FrequencyEmbedder` (the enrolled labels equal the D13 expectation while `after` is unchanged and the provider was called once; the other voice's profile confirms the patient WRONG visibly; a failing enrolled pass still tears down; a drifting VAD is a harness fault), `TestMainEnrolled` (the flag, a stereo refusal, the absent-model error, leave-one-out from the OTHER recordings' spans, the one-pair hint, the no-speech pool skip).
- [x] 🟩 2.5: **Threat-model + data-flow entries for attribution and D4** (drafted; finalised in 3.3). Verification: docs re-read against the code.
  - **Built 2026-09-15 (executor, run stage-2, leg c1) — NOT yet composer-verified (docs; re-read against the code by the executor).** `docs/security/threat-model.md`: the practitioner-profile section is now "Phases 1–2" and gains surfaces 6 (attribution keeps the windowed plaintext bound; the three non-content fields; both entry points; every fallback yields no attribution, reported, never a failed transcription; residual: the probe compares the pin, not bytes), 7 (the confirmed role stays a UI selection — D1; the lying field unconstructable) and 8 (the D4 auto-confirm RESPONSIBILITY BOUNDARY: unconditional by the practitioner's ratified choice, the accepted risk, the compensating controls, the declined margin-gated variant kept as the documented hardening). `docs/security/data-flow-map.md`: flow 7 describes the optional in-process profile application (the profile read, the model from flow 8, per-segment embedding inside the window reduced to one cosine, the three fields under the same key, the recovery path identical, nothing written back); the logs non-flow states why the new fields register no tripwire marker. `CHANGELOG.md` Added entry for Phase 2.
- [x] 🟩 2.6: **Apply the amended D13 remainder rule — with a profile applied, the whole non-practitioner remainder is `speaker_2` (option (a); `Chosen:` recorded).** Found 2026-09-16 by the Phase 2 suites (executor leg c3): `_cluster_embeddings` is degenerate only for byte-identical features, so the matched branch of `attribute_speakers` (`desktop/src/scribe_desktop/transcription.py` `:875-884`) always split one other voice into `speaker_2` + `speaker_3` in a two-person consultation. Scoped `/review-plan` 2026-09-16 (composer, Phase 2/3 boundary; the codex peer's round-19 assessment recommended (a)).
  - Chosen: (a) — practitioner, 2026-09-16. Rejected: (b) keep the split as documented residue (misleading third speaker at the Phase 3 smoke); (c) an inertia single-voice test on the remainder (that IS estimating k — D-S1's job, needs the shared recording set).
  - Code: in `attribute_speakers`' matched branch label every index in `rest` `SPEAKER_2` (drop the `_cluster_embeddings` call over the remainder and the `renamed` map); the zero-match branch, the no-profile path, `enrolled_cluster`, the `< 2` degenerate policy and the `SPEAKER_3` constant are untouched. Docstrings: the function's (`:846-868`) and the module's (`:52`) now state the whole-remainder rule and name D-S1 as what restores a third label.
  - Tests (`desktop/tests/test_attribution.py`): retarget `test_remainder_splits_into_speaker_2_and_speaker_3_by_first_appearance` (`:206`) to assert `[SPEAKER_1, SPEAKER_2, SPEAKER_2, SPEAKER_2]`-shaped labels; the pipeline test at `:447-456` and the document test at `:487-498` (`speaker_quotations` / `format_transcript_text`) assert exactly `{speaker_1, speaker_2}`; the row fixture at `:399` may keep a literal `speaker_3` row only where it pins label-name independence. `test_speaker_eval.py:578` stays (label-name independence of `cluster_metrics`).
  - Docs: `docs/security/data-flow-map.md:100` and the CHANGELOG Phase 2 entry state the whole-remainder rule; no threat-model claim names the split.
  - Verification: composer-run `ruff check .`, `mypy`, `pytest -q` in `desktop/` (green; the retargeted assertions are the pins). Blocks: nothing; ships before Phase 3's live smoke (Task 3.4) so the practitioner never sees a spurious third speaker.
  - **Done 2026-09-16 — composer-verified GREEN (09:51): ruff clean, mypy clean over 33 files, `pytest tests/test_attribution.py` 51 passed, full suite 1636 passed (no item added or removed).** Built 2026-09-16 (architect-executor, run stage-2, leg c12; no delegate — a one-branch change). `transcription.py`: `attribute_speakers`' matched branch now returns `speaker_1` for every matched index and `speaker_2` for every other (the `_cluster_embeddings` call over the remainder and the `renamed` map are gone; `_cluster_embeddings` remains the zero-match branch's and `label_speakers`' clustering); the function docstring states the whole-remainder rule, why the split was retired, and that `SPEAKER_3` returns only with D-S1; the module docstring (`:51-54`) and the `SPEAKER_3` constant's comment say the same (the constant and its export stay). Tests: `test_attribution.py` — the pure remainder test retargeted to `[speaker_1, speaker_2, speaker_2, speaker_2]` (+ `speaker_3` absent), the heard-second pipeline test to `[speaker_2, speaker_1, speaker_2]`, the two-other-voices pipeline test to `[speaker_1, speaker_2, speaker_2]` with exactly `{speaker_1, speaker_2}` rendered and no `speaker_3` in the transcript text; the `:399` fixture keeps its literal `speaker_3` row (label-name independence of the has-text predicate); `test_speaker_eval.py` — the wrong-profile harness test to `(speaker_2, speaker_1, speaker_2)` and its now-unused `SPEAKER_3` import dropped (`:578` keeps the literal string). Docs: `docs/security/data-flow-map.md` flow 7 and the CHANGELOG Phase 2 entry state the whole-remainder rule; no threat-model claim named the split (checked). NOT run (no grant): ruff, mypy, pytest — expected 1636 passed (no item added or removed), mypy 33 files.
### Phase 3 — Practitioner surface and documentation
- [x] 🟩 3.1: **`ui/practitioner.py` — the Practitioner tab.** Consent text (`CONSENT_TEXT_V1` + version constant in `ui/models.py`, ratified in 0.2) with the consent checkbox and the learning opt-in checkbox; device pick (the microphone screen's device list); "Record my voice (about a minute)" with a live level and a speech-seconds counter, running `record_enrolment` on a `TaskThread` (GUI thread never blocks; the whole capture → embed → save sequence runs under `begin_enrolment()` — D15 — so the monitor poll, Start/Resume and the benchmark refuse meanwhile and Re-record/Delete are disabled); on success `enrol` + `save_profile`, PCM dropped, status "Voice profile saved <date> (model <id>)"; "Re-record" and "Delete voice profile" (confirm dialog); a "Learned phrases" list placeholder (Phase 5); disabled states only when the SELECTED embedder is unavailable (the onnx file absent — the message names `setup-models.py`; spectral is always available, D16). Verification: Phase 3 offscreen tests.
  - **Wiring owed from Phase 1 (2026-09-15):** the controller cannot see the microphone screen's benchmark `TaskThread`, so `MainWindow` must register it — `controller.set_enrolment_blocker(lambda: "a benchmark is running" if self.microphone_screen.is_busy else None)` — for `begin_enrolment` to refuse while the benchmark runs (D15; the reverse refusal is already in `MicrophoneScreen.on_run_benchmark`). The tab's capture → `enrol` → `save_profile` → result handler sequence holds the `EnrolmentLease` and releases it on every path; `record_enrolment`'s `on_progress` fires on the worker thread and must be marshalled to the GUI thread; the PCM is dropped after `enrol`.
  - **Done 2026-09-16 — composer-verified GREEN (13:01): ruff clean, mypy clean over 34 source files, `pytest tests/test_ui_screens.py tests/test_ui_models.py` 194 passed, full suite 1659 passed in 71 s (1636 + 23). The same results close Tasks 3.2 and 3.3.** Built 2026-09-16 (architect, run stage-3, leg stage-3-arch-d1; the widget and its tests implemented by the `claude-opus-5` `loop-delegate` against the architect's spec and verified line by line). `desktop/src/scribe_desktop/ui/practitioner.py` (new): `PractitionerScreen(controller, backend, *, profile_root, embedder_kind, embedder_factory, embedder_available, vad_available, capture, embed, readiness_provider, confirm_delete, clock)` — the first-run banner; the "Voice profile" status group (`attribution_readiness` is the ONE source: "No voice profile yet." / "Voice profile saved <date> (model <id>)." / the D2 reason line for a present-but-unusable or stale profile, with Delete available); the "Consent" group (`CONSENT_TEXT_V1` verbatim, the consent checkbox and the learning opt-in — the consent box ticked and DISABLED while a READABLE profile exists (its own consent record pre-ticks it; an unreadable blob never does — peer round 27 PR-HIGH-006), withdrawal is Delete; the learning box editable whenever idle, applying at the next (re-)record (PR-MED-021); left as they are after a failed enrolment so consent is never re-ticked for a retry); the "Record your voice" group (its own device combo from the backend's list + Refresh, the availability line naming `scripts/setup-models.py --only speaker-embedding` / silero when the SELECTED embedder or the VAD model is absent — D16, Record / Stop, a level bar, "Speech heard: N s of 30 s", the enrolment status line); "Delete voice profile" (confirmation, `delete_profile`); the Phase 5 "Learned phrases" placeholder; a 5 s availability timer (stats only). The D15 sequence: `on_record` → `begin_enrolment` (a `SessionActivityError` renders "Cannot record now - …" and nothing starts) → on the `TaskThread` the embedder is built FIRST (a load failure fails before the read-aloud), the capture runs through the `capture` seam with `on_progress` = the `_progress_reported` Qt signal's emit (queued to the GUI slot) and `should_stop` = a `threading.Event`, `enrol` runs, `del pcm` (D8), the profile is built (`ConsentRecord(accepted_at=now, consent_text_version=CONSENT_TEXT_VERSION, learning_opt_in)`; `device_name` through `profile_device_name`, so a device name can never fail validation) and `save_profile` writes it; both handlers re-read the store, render, and release the lease in `finally` — AFTER the status update, on every path; Stop sets the event and the capture raises `EnrolmentCancelledError` → "Enrolment stopped - nothing was saved.". `ui/models.py`: `CONSENT_TEXT_VERSION` / `CONSENT_TEXT_V1` (the ratified text, verbatim) / `CONSENT_CHECKBOX_LABEL` / `LEARNING_OPT_IN_LABEL` / `FIRST_RUN_BANNER`; `SessionControllerLike` gains `begin_enrolment` / `end_enrolment` / `set_enrolment_blocker`. Decisions inside the plan's latitude: the tab has its own device combo (the same backend list — no coupling to the microphone screen's selection); the learning opt-in changes only with a re-record in this phase (noted on Task 5.3); `_update_controls` runs after `task.start()` because `is_busy` reads `QThread.isRunning()`. Tests (`test_ui_screens.py` `TestPractitionerScreen`, 13; the Windows-only ones go through the real DPAPI store under `tmp_path` with a readiness provider that omits the shipped-identity pin because the stub embedder's id is not the shipped one): consent text verbatim + nothing enabled; Record enabled only with consent AND availability (the model line, the VAD line); the banner shows; the end-to-end save under the lease (busy UI observed; a hook on `end_enrolment` proves the status line was written BEFORE the release; the profile read back with the consent version, opt-in and device name; the banner hidden by the save); a controller refusal reaches neither the capture nor the store; a capture failure releases the lease, saves nothing and keeps consent ticked; Stop cancels and saves nothing; a failed re-enrolment keeps the previous profile; Delete is key deletion and resets consent, a declined confirmation deletes nothing; the device-name sanitiser (tab, U+2028, 300 chars); an unusable blob reported with Delete available; Stop/Delete inert when idle.
- [x] 🟩 3.2: **First-run behaviour + report lines.** `MainWindow`: at startup with no profile, select the Practitioner tab and show a banner ("Set up your voice profile so the app always knows which words are yours — you can still record without it"); `models.model_report_lines()` gains the speaker model + profile state (D2 visibility; shown by the microphone screen's `refresh_model_status` label — the surface Task 0.6 defers to, PR-MED-017); `closeEvent` refuses close while the enrolment activity is held (D15). Verification: offscreen tests.
  - **Built 2026-09-16 (architect, run stage-3, leg stage-3-arch-d1) — NOT yet composer-verified.** `ui/main_window.py`: the "Practitioner" tab between Note and Status (a `profile_root` keyword for tests); `controller.set_enrolment_blocker(self._enrolment_blocker)` — "a benchmark is running" while `microphone_screen.is_busy` (the wiring Phase 1 owed); the D10 first run — `not practitioner_screen.profile_present` → `show_first_run_banner()` and the tab selected (presence is `attribution_readiness`'s verdict, so an unusable blob is PRESENT: no banner, the tab's status line names it); `closeEvent` refuses while `controller.enrolling or practitioner_screen.is_busy` ("Voice enrolment in progress - wait for it to finish (or Stop it) before closing."), before the existing busy chain. `ui/models.py` `model_report_lines(*, profile_root=None, kind=SHIPPED_SPEAKER_EMBEDDER)` returns FOUR lines: the two existing plus `speaker_model_report_line(kind)` ("Speaker model (<id>): installed - verified when it loads" — a stat claims presence only, peer round 27 PR-LOW-029; spectral "ready (built in)" / "MISSING - run scripts/setup-models.py --only speaker-embedding (voice attribution is off until then)") and `voice_profile_report_line(...)` (`PROFILE_NOT_ENROLLED_LINE` / "Voice profile: enrolled <date> (model <id>)" / the D2 reason line), both from `attribution_readiness` — a stat and one profile read; the microphone screen's 5 s timer now performs that read (a few ms of DPAPI on the GUI thread; a watch item, cache it if the smoke shows sluggishness). Tests: `TestMainWindow` +5 (the first-run tab + banner; a saved shipped-identity profile keeps the Microphone tab, Windows-only, admitting the absent-model status line; the blocker registered and reporting a running benchmark; close refused while `enrolling`; close refused while the tab's worker runs), `test_constructs_all_screens` → 7 tabs, `profile_root=tmp_path` at all 16 `MainWindow` sites; `test_ui_models.py` `TestPractitionerReportLines` +5 (the speaker line both ways, the profile line not enrolled / enrolled / stale, the consent text's version and promises) and the report-lines test → 4 lines over `tmp_path`.
- [x] 🟩 3.3: **Docs.** `threat-model.md`: surface 5 — the practitioner's own biometric at rest (same-user residual; DPAPI; deletion) and the D4 auto-confirm relaxation as a practitioner-ratified responsibility boundary with its named residual; `retention-schedule.md`: profile row (indefinite until deleted; deletion = key deletion; never swept) and the consent record; `data-flow-map.md`: flow 12 (microphone → in-memory PCM → embedding → profile store), the model-cache entry, the explicit non-flows (no enrolment audio on disk); `design-system.md`: the first-run surface convention and the confirmation-line pattern; `AGENTS.md` Subsystem Documentation pointer; CHANGELOG. Verification: every claim traced to a call site (the docs review class).
  - **Built 2026-09-16 (architect, run stage-3, leg stage-3-arch-d1) — every claim re-read against the code by the architect; NOT yet composer-verified (no suite impact).** `docs/security/threat-model.md`: the section is "Phases 1–3" with the DRAFT marker removed; surface 1's residual points at surface 9; surface 2 names the tab's worker dropping its PCM reference before the profile is built; surface 5 states the blocker wiring is LIVE (`MainWindow` registers the benchmark), the close refusal, and that the lease spans capture → embed → save → the tab's handler on every path; NEW surface 9 — consent, first run and deletion as same-user UI state: what the consent gate, the stored version, the disabled box and the confirmed Delete enforce, and the residuals (no identity check — anyone at the login can enrol or delete; the record is a version and a time, not a copy of the text; the opt-in changes only by re-record; a Delete racing a transcription's profile read yields the visible fallback when the key is gone before the read unwraps it, while a read past the unwrap completes on its in-memory copy — peer round 27 PR-LOW-031). `docs/security/retention-schedule.md`: the profile row final (Phases 1–3), the consent record's custody stated (inside `voice.enc` only, shares its lifetime). `docs/security/data-flow-map.md`: flow 12 (microphone → in-memory PCM → embedding → profile store, under the lease; re-record, delete, the readers), flow 8's stale `.candidate` sentence replaced (Task 1.4's forward note), the "no enrolment audio on disk" non-flow with its two pins, the components row, the logs non-flow's pointer. `docs/design-system.md`: the tab in the surfaces list, the enrolment worker under long work, the refuse-during-live-work bullet extended (close and benchmark vs enrolment), two new conventions — first-run surfaces ask, never block; a confirmation line replaces a choice the app made, with a one-click way back. `AGENTS.md` Subsystem Documentation pointer (enrolment / tab / store → the three security docs); `CHANGELOG.md` Phase 3 entry.
- [x] 🟩 3.4: **Phase 3 smoke by the PRACTITIONER**: fresh start → banner → consent → enrol → a mock consultation → auto-confirmed role shown → Generate. Record PASS/FAIL here.
  - Done (2026-09-16 ~14:55, PRACTITIONER, Explorer-launched app): **PASS** — the whole flow ran on the real profile store (banner → consent → enrol → mock consultation → auto-confirmed role → Generate). One environment step first, not a defect: the Practitioner tab correctly reported the speaker model absent because the Task 0.6 promotion had landed in the MSIX-virtualised cache the agent shell sees; `.venv\Scripts\python.exe scripts\setup-models.py --only speaker-embedding` from a normal terminal at the repo root installed it into the real cache and Refresh re-probed green. Practitioner decision recorded in `Deferred — Actionable Later`: bundle the models with the installer when commercialising (option 1).

### Phase 4 — Per-practitioner cue file
- [x] 🟩 4.1: **Ship the defaults file.** `config_defaults/section_cues.json` generated from `_RAW_SECTION_CUES` (17 keys, 98 phrases, canonical key order); `note.py` derives `_RAW_SECTION_CUES` / `DEFAULT_SECTION_CUES` FROM the packaged file at import (one source; a test pins equality with the 17/98 counts). Verification: counts test.
  - **Done 2026-09-16 — composer-verified GREEN 15:43 (Tasks 4.1–4.4 together): ruff clean, mypy clean over 34 files, the three test modules 463 passed, full suite 1695 passed (1666 + 29).**
  - **Built 2026-09-16 15:37 (architect, run=stage-4, leg stage-4-arch-e1).** `desktop/src/scribe_desktop/config_defaults/section_cues.json` (new; `schema_version: 1`, the 17 canonical keys in canonical order, the 98 phrases transcribed from the literal — the architect re-read every phrase against the old literal before deleting it). `note.py`: `_DEFAULTS_RESOURCE_DIR` and `SECTION_CUES_FILENAME` are now single-sourced HERE (exported; `note_config` imports both, because it imports this module), `_load_shipped_section_cues()` reads the packaged file through `importlib.resources` and `_parse_shipped_section_cues(payload)` (split out at peer round 32 PR-HIGH-007) checks SHAPE (an object of canonical keys to lists of strings) AND COMPLETENESS (every canonical key present, every list non-empty — the packaged default's rule only; a user override may be sparse under D6), raising `RuntimeError` at import on a broken install — every content rule stays `SectionCuesFile`'s (Task 4.2), and the shipped file is validated against that model by test; `_RAW_SECTION_CUES` is that read's result and `DEFAULT_SECTION_CUES` derives from it exactly as before; the cue comment now carries the first-cut honesty note and the D6/D7 pointers. Tests (delegate, spec-verified): `test_note.py::TestShippedSectionCues` (3: 17 sections / 98 phrases / canonical order; both constants equal the packaged file; every phrase a distinct, non-empty content-token run).
- [x] 🟩 4.2: **Loader + model.** `note_config.py`: `SectionCuesFile(schema_version: Literal[1], section_cues: Mapping[NoteSectionKey, tuple[_TriggerText, ...]])` with the D6 validations; `NoteConfig.section_cues`; a `normalised_cues()` accessor returning the `DEFAULT_SECTION_CUES` shape; `load_note_config` reads the fourth filename with the same precedence; module docstring updated (a missing key = no cues for that section). Verification: Phase 4 loader tests; digest-pin inventory updated.
  - **Built 2026-09-16 15:37 (architect, leg stage-4-arch-e1; NOT yet verified).** `note_config.py`: `SECTION_CUES_FILENAME` (imported from `note`) is the fourth entry of `CONFIG_FILENAMES`; `_check_section_cues(cues)` is THE cue-phrase validator (a phrase with no content tokens → "could never route an utterance"; a normalised duplicate within or across sections → "is the same phrase as … once normalised"), called by `SectionCuesFile` (so a malformed user file names ITS file through `_parse_config_blob`) and again inside `NoteConfig._check_config` (so a config assembled without the file model is held to the same rule); `SectionCuesFile` (`extra="forbid"`, frozen, `schema_version: Literal[1] = 1`, `section_cues: Mapping[NoteSectionKey, tuple[_TriggerText, ...]] = {}` — an unknown key is refused by the `Literal`, the trigger text bound and control-character rule apply per phrase); `NoteConfig.section_cues` (same type, default EMPTY like the other three fields — the first-run default is the loader's) enters `to_bytes()` and so `config_digest` (D7); `NoteConfig.normalised_cues()` returns the provider shape; `load_note_config` reads/parses the fourth blob with the same `_read_config_blob` / `_parse_config_blob` precedence and passes `section_cues=`; module docstring: "four" filenames plus a paragraph on the fourth file (missing key = no cues; phrases never enter a note). Digest-pin inventory: confirmed against Key Findings — no literal `config_digest` pin exists in the suite; the fourteen equality pins compare objects and need no edit. Tests (delegate, spec-verified): `test_note_config.py::TestSectionCuesFile` (12: unknown key, blank, control character, no content tokens, the 200-char bound both sides, duplicate within / across sections, unknown schema version, extra field, a missing key has no cues, the same rule on `NoteConfig` without the file model, frozen) and `TestSectionCuesLoader` (9: absent → the shipped default equals `DEFAULT_SECTION_CUES` and creates nothing; malformed / unknown key / duplicate name the file (typed `NoteConfigInvalidError`); a directory at the filename is `NoteConfigUnreadableError`, never a fallback; whole-file replacement leaves the other files' defaults; a user file byte-identical to the shipped one leaves the digest unchanged; one phrase removed and one added give three distinct digests; the canonical round-trip preserves cues and digest), plus the `(SECTION_CUES_FILENAME, SectionCuesFile)` pair in `TestShippedDefaultsPackaging`.
- [x] 🟩 4.3: **Provider from config.** `ui/models.py` `build_note_generator(provider_factory: Callable[[NoteConfig], NoteModelProvider] = lambda cfg: ExtractiveNoteProvider(cues=cfg.normalised_cues()))`; tests that pass a factory updated. Verification: a user file that removes/adds a cue changes routing through the window path (offscreen test).
  - **Built 2026-09-16 15:37 (architect, leg stage-4-arch-e1; NOT yet verified).** `ui/models.py`: a named module-level `_extractive_provider_from_config(config) -> NoteModelProvider` (`ExtractiveNoteProvider(cues=config.normalised_cues())`) is the default `provider_factory: Callable[[NoteConfig], NoteModelProvider]`, and the worker calls `provider_factory(config)` with the config it just loaded — the same object the `NoteGenerationResult` carries, so the cues that routed are the ones the draft's `config_digest` binds (a name rather than the plan's lambda: greppable, docstring-bearing, mypy-strict clean). "Tests that pass a factory": NONE existed (`provider_factory` appeared only in `ui/models.py`); the one production caller, `ui/transcript.py:94`, passes the builder itself and is unaffected. Tests (delegate, spec-verified): `test_ui_models.py::TestProviderFromConfig` (5, through the REAL generator the Transcript screen's worker runs — a transcript written under a real `SessionCrypto` and read back from disk, the shipped `template-a` profile): the shipped cues route "Your home exercise is the wall slide" (clinician, non-question) to `advice_home_exercise` and nowhere else; a user file without that cue routes it nowhere and the two results' config digests differ; a user file mapping "wall slide" to `treatment_performed` re-routes the same utterance; a passed factory receives the very config object the result carries; the default factory yields an `ExtractiveNoteProvider`. No Qt is involved: the window path's GUI part is the thread hand-off already pinned by the screen tests, and the generator is the callable it hands over.
- [x] 🟩 4.4: **Phase 4 tests + docs.** Loader properties (absent/malformed/unknown key/blank/duplicate/digest), `note_check.py:140-146` honesty note updated to name the file, `data-flow-map.md` flow 11 + `retention-schedule.md` config row name the fourth file, `docs/testing/shipping-gate.md` "Preparing" names it. No literal digest pin exists (Key Findings), so the fourteen equality pins need no edit. Verification: suite green.
  - **Built 2026-09-16 15:37 (architect, leg stage-4-arch-e1; NOT yet verified).** Tests: recorded on 4.1–4.3 (29 new items; the loader properties are `TestSectionCuesLoader` + `TestSectionCuesFile`). Docs: `note_check.py` honesty note names `config_defaults/section_cues.json` and states the lexicons, unlike the cues, are NOT configurable; `docs/security/data-flow-map.md` flow 11 names the fourth file, says the digest binds the cue set too, and separates the two claims that used to be one — autofill/prefill text reaches the note only as proposals, cue phrases never enter a note at all (they place a verbatim `transcript`-provenance utterance; a hand-edited cue file can misplace transcript text but cannot add text); `docs/security/retention-schedule.md` config row names the file, adds "no duplicate cue phrase" to the structure list, states the same routing-not-content fact, and names the file as where the ratified consent text says approved phrases will be kept (Phase 5, NOT yet built — today nothing in the app writes any config file); `docs/testing/shipping-gate.md` "Preparing" names the run's `section_cues.json` as owed at Task 4.5; `docs/testing/shipping-gate-config/README.md` says the file belongs there, is not there yet, and that the shipped default applies while absent (install step 2 mentions it conditionally); CHANGELOG entry. Class sweep of the sibling claim (the lesson: fix the class, not the instance): `docs/security/threat-model.md` surface 1 also listed the three files and claimed "nothing from config reaches `note.enc` without … confirmation" — the fourth file added as a ROUTING input and the claim narrowed to "no text AUTHORED in config" (docs-only LOW, do-the-work, in scope — auto-disposed under the headless rule and recorded here). `note.py`'s own comment still says `note_config` "owns the other three filenames" — correct as written (three there, the fourth here).
- [x] 🟩 4.5: **The gate-run cue file** (PRACTITIONER supplies phrases; executor authors): `docs/testing/shipping-gate-config/section_cues.json` = the shipped defaults plus the practitioner's own phrases for advice, treatment, plan and follow-up; README updated; validated through the loader. Verification: the loader smoke in the README.
  - Not built at the Phase 4 build leg (2026-09-16 15:39): the practitioner's phrases had not arrived; the README already names the file as owed here and the loader now accepts it — author it under this run once the phrases land.
  - **Practitioner decision 2026-09-16 15:20 (relayed by the composer): "use the defaults"** — no practitioner phrases are added now; their own phrasing is to accrue through Phase 5's learning instead. **Built 2026-09-16 15:45 (architect, leg stage-4-arch-e2-review; NOT yet verified — verify=composer):** `docs/testing/shipping-gate-config/section_cues.json` is a BYTE-IDENTICAL copy of the shipped `config_defaults/section_cues.json` (17 keys, 98 phrases; the loader's `test_a_user_file_identical_to_the_shipped_one_leaves_the_digest_unchanged` pins that such a copy leaves the config digest unchanged, so the gate run's cue set IS the shipped set); README: the "belongs here / not here yet" paragraph replaced by the decision, install step 2 copies it unconditionally, the loader smoke prints the cue count (17 sections, 98 phrases). Smoke NOT run by the architect (no grant) — the composer verifies the copy byte-for-byte and runs the README command from a normal terminal; expected output `1 rules, 3 prefills, 17 cue sections, 98 cue phrases`.
  - **Done 2026-09-16 — composer-verified GREEN 15:52:** `cmp` of the two `section_cues.json` files → identical; the README loader smoke printed exactly `1 rules, 3 prefills, 17 cue sections, 98 cue phrases`; ruff clean, mypy 34 files, `test_note_config.py` 158 passed, full suite 1695 (unchanged after the round-30 comment hunk).

### Phase 5 — Consented phrase learning
- [x] 🟩 5.0: **Consent v2 + version check (prerequisite for learning).** `ui/models.py`: `CONSENT_TEXT_V2` (the ratified text VERBATIM from `Config / Environment / Deployment Impact`), `CONSENT_TEXT_VERSION = "consent-v2"`, `CONSENT_TEXT_V1` kept as history, the learning checkbox label re-worded ("… lines I add or move during review (saved when I save the note)"); `ui/practitioner.py`: the consent box is pre-ticked ONLY from a readable profile whose `consent_text_version == CONSENT_TEXT_VERSION` — a `consent-v1` record leaves it unticked + editable with a one-line notice ("The consent text has changed — please read it and tick again"), Record needs a fresh tick, and `learning_opt_in` is treated as OFF until re-consented; **re-consent without re-record**: a "Confirm consent" action re-saves the SAME vector under the existing key with a v2 consent record (`save_profile` replaces only `voice.enc`), and the same path lets the practitioner toggle the learning opt-in without re-recording (absorbs the Phase 3 note formerly on 5.3); `ui/models.py` `attribution_readiness` / the profile-state report line name a stale consent version. Verification: offscreen tests — v1 record → unticked + notice + learning off; v2 record → ticked; re-consent writes v2 and keeps the vector (DPAPI, Windows-only); label text pinned.
  - **Done 2026-09-16 — composer-verified GREEN 17:16 (Tasks 5.0–5.4 together): ruff clean, mypy clean over 34 files, the four test modules 766 passed, full suite 1852 passed (1705 + 147).**
  - **Built 2026-09-16 16:55 (architect, run=stage-5, leg stage-5-arch-f1).** `ui/models.py`: `CONSENT_TEXT_VERSION = "consent-v2"`; `CONSENT_TEXT_V2` = the ratified text (one reading recorded: the plan's blockquote emphasises *you* with markdown asterisks; the label is `PlainText`, so the word ships without the markup — every other character verbatim); `CONSENT_TEXT_V1` kept with its own literal `Version consent-v1.`; `LEARNING_OPT_IN_LABEL` re-worded as specified; NEW `CONSENT_STALE_NOTICE`, `CONFIRM_CONSENT_BUTTON_LABEL`, `consent_is_current(profile)`; `voice_profile_report_line` appends ` - consent text updated, confirm it on the Practitioner tab` for a readable profile with an older version (`attribution_readiness` itself is unchanged — attribution still runs on a stale-consent profile; consent v1 covered the fingerprint, and only LEARNING is gated on the version — a reading the architect made explicit in surface 9). `ui/practitioner.py`: the label shows `CONSENT_TEXT_V2`; `refresh_profile_state` derives `_consent_current`, pre-ticks ONLY a current record, withdraws a tick that came from a record found stale (a hand tick stands — the PR-HIGH-006 shape), shows `consent_notice_label` for a stale readable record, keeps the learning checkbox on the stored value; `_update_controls`: the consent box is editable unless a CURRENT readable record exists, the NEW `confirm_consent_button` is visible with a readable profile and enabled when consent is ticked and (the record is stale OR the learning checkbox differs from the stored opt-in); `on_confirm_consent` re-saves `profile.model_copy(update={"consent": ConsentRecord(now, CONSENT_TEXT_VERSION, learning_checkbox)})` through `save_profile` (existing key, `voice.enc` only; GUI thread; no lease — no microphone); the learning note names Confirm consent; `consent_current` property. `practitioner_profile.ConsentRecord` docstring names both versions. Tests: delegate 2 (recorded in the handoff bullet).
- [x] 🟩 5.1: **"Add a transcript line" on the Note tab (D14).** An explicit control group beside the transcript panel (the panel stays a non-interactive text box): an utterance chooser listing the utterances not already in the note, labelled by segment index + speaker + the first words; a section chooser limited to the sections that utterance may enter (clinician-owned → the confirmed clinician's non-questions only; the `_route` ownership rule reused, not re-implemented); Add appends a `transcript`-provenance `NoteAssertion` (contiguous coords for the whole utterance, id `m<segment>`) to the screen's working draft through the content-change helper (acknowledgements cleared, note un-saved, re-finalised — D14, PR-MED-010); Undo removes it again the same way; edits disabled after Save. Verification: Phase 5 tests incl. Check 1 reconstruction and the duplicate refusal.
  - **Built 2026-09-16 16:55 (architect, leg stage-5-arch-f1; NOT yet verified).** `note.py`: the ownership rule EXTRACTED from `ExtractiveNoteProvider._route` into `spoken_by_confirmed_clinician`, `section_admits_utterance`, `admissible_sections` and `first_matching_section` (`_route` now calls the last with the same inputs — behaviour identical, one implementation); `provider_assertion_id` (`x%04d`) / `manual_assertion_id` (`m%04d`) and `whole_utterance_assertion` (the provider builds its assertions through it too, so a manual line and a routed line are the same shape and Check 1 rebuilds both by the one rule). `ui/models.py`: `working_draft(draft, removed=, additions=)` (the D14 draft: filtered + appended, canonical order, proposals unchanged, validated by `NoteDraft`), `eligible_utterances` + `UtteranceChoice` (segments with quotable text not already in the working note, each with its admissible sections), `section_title`. `ui/note.py`: the "Edit the note" `QGroupBox` under the transcript panel — `utterance_combo` / `section_combo` (the sections that line may enter) / `add_line_button`, the line-editor rows, `learning_label`, `edit_status_label` (PlainText); `add_line(segment_index, section_key)` refuses after Save, an unknown segment, a segment already in the working note, a disallowed section and a wordless segment, else adds `m<segment>` and goes through `_after_content_change` (renamed from `_after_resolution_change`, the one PR-MED-010 path: acknowledgements cleared, un-saved, re-finalised); `_refinalise` finalises `models.working_draft(...)`; `undo_line`; `_update_controls` freezes the edit widgets after Save; the transcript panel unchanged (`NoTextInteraction`). Tests: delegate 2.
- [x] 🟩 5.1b: **Remove / Move a routed line (D14).** Per rendered `transcript`-provenance assertion: "Remove line" adds its id to the screen's removed set; "Move to section…" = remove + add through the 5.1 path under the same ownership rules; the content-change helper filters removed ids out of the working draft before `finalise_note` (acknowledgements cleared — PR-MED-010); an omission warning the checker raises for a removed line is acknowledgeable (review severity) and named in the warning copy; Undo restores; frozen after Save. Verification: Phase 5 tests (remove → warning acknowledgeable, never blocking; undo; move; frozen after Save).
  - **Built 2026-09-16 16:55 (architect, leg stage-5-arch-f1; NOT yet verified).** `ui/models.py`: `editable_lines` + `EditableLine` (state `routed` / `removed` / `moved` (provider id removed AND a manual line for the same segment — the move's add leg is not listed twice) / `added`, with `allowed_sections` minus the current one); `WARNING_COPY["high_risk_omission"].clear_hint` names a removed line as a cause (review severity unchanged — never a block). `ui/note.py`: `remove_line(id)` (provider lines only; a manual id delegates to undo), `move_line(id, key)` (a provider line: its id into the removed set + `m<segment>` under the new section; a manual line: its section replaced; the ownership rule applied to the target), `undo_line(id)` (a manual id drops the addition; a provider id drops the removal AND the re-added leg of a move); rows rendered per `editable_lines` with Remove line / Move (a per-row section combo) / Undo add / Undo; all through `_after_content_change`; frozen after Save. Tests: delegate 2.
- [x] 🟩 5.2: **Auto-learn on Save (D9 as amended).** After an add or a move of one of the practitioner's OWN utterances (ownership rule reused from 5.1 — a patient's line can never reach this path; pinned), when `learning_opt_in` is on and the consent record is CURRENT (5.0): compute the candidate phrase = the utterance's leading 2–4 content tokens (`content_tokens`, stored normalised); REFUSE the candidate (a small "not learned: contains a name/number/date/medication" note in the edit control's status line — never a modal) when ANY SOURCE token, in original case and before normalisation, is name-like per `is_name_like_token(word, first_in_segment=…)`, numeric, date-shaped (`\d{1,2}[/.-]\d{1,2}`, month names, `\d{4}`) or medication-shaped (a `-mab/-nib/-pril/-olol/-statin/-cillin/-mycin/-azole/-pine/-sartan` suffix, or a `mg/mcg/ml/mL` unit within two tokens) — the filter is the ENFORCING control, implemented once in `note_config.py` (`refuse_learning_candidate(tokens, *, first_in_segment) -> str | None` returning the refusal class) and tested as a CLASS against an adversarial fixture (capitalised clinician-spoken names, sentence-initial names, dosages, dates in three formats, drug names with and without units — PR-HIGH-001 / PR-MED-016); a dry-run first-match check that only NOTES when an earlier section's cue would still win (the phrase is learned anyway; never a silent cue deletion — PR-MED-011); the accepted candidate is QUEUED on the screen's working draft (section, phrase, source segment) and written ONLY when the note is Saved: `note_config.append_user_cues(pairs)` (creates the user file from the shipped default if absent; atomic replace; duplicates skipped under the loader's normalisation; the loader re-validates the result and a failure leaves the file untouched and is reported in the status line); an add undone before Save, a removal, a move's remove leg and a discarded note teach nothing; opt-in off or consent stale → nothing queued and a one-line hint pointing at the Practitioner tab. Verification: Phase 5 tests — queue-then-write on Save, undo-before-save writes nothing, the refusal fixture (every class refused, benign phrases accepted), a patient utterance never queued, duplicate skipped, loader failure leaves the file untouched.
  - **Live smoke: FIRST RUN 2026-09-17 ~00:05 did NOT pass — "Learned phrases are not showing up after saving"; cause settled by the practitioner's answers at leg f8c: the queued-phrase status was read as the outcome and the review left by an exit that writes nothing (Cancel / Delete-and-complete / Complete), because the tab never named Save note on THIS tab as the one control that learns. Fixed as messaging only (legs f8c–f8d: the learning line names Save note while phrases are queued, a not-attributed line says so, Save after edits that queued nothing says why; rounds 39–40 in-session + codex round 41 converged). RE-SMOKE 2026-09-17 12:40–12:44 PASSED on the six steps (four screenshots: "2 phrases queued - press Save note on this tab …" → "Learned 2: 'do a bit of' …; 'alright so you've come' …" → both listed under Recently learned with today's date and under All learned phrases). One new item, verbatim: "It worked this time. I realise that it didnt work when i didn't add any extra lines. So it doesn't save if the line doesn't get added from the drop down box and it's automatically added." — disposed NO DEFECT (by design): D9 as amended learns ONLY from a line the practitioner ADDS or MOVES; a line the generator placed was routed by a cue the practitioner already has, so learning its opening words would add a redundant or competing cue; the rule is stated on the Note tab's learning line ("lines you add or move are learned when you press Save note on this tab"), on the opt-in label ("lines I add or move during review") and in consent text v2 ("lines you add or move while reviewing a note").**
  - **Built 2026-09-16 16:55 (architect, leg stage-5-arch-f1; NOT yet verified).** `note_config.py` (the "Consented phrase learning" block): `refuse_learning_candidate(tokens, *, first_in_segment, following=())` — THE filter, one implementation: name-like (`transcription.is_name_like_token`, `first_in_segment` honoured for `tokens[0]` only), date-shaped (`\d{1,2}[/.-]\d{1,2}`, a four-digit run, month names + abbreviations), numeric (`is_number_token`), medication-shaped (the ten listed suffixes on an alphabetic token longer than the suffix, `spine` / `supine` exempted BY NAME — an exemption admits, never widens; a `mg` / `mcg` / `ml` unit anywhere in the candidate or within `_UNIT_WINDOW = 2` raw words after it), checked in that order, first class reported; `propose_learning_phrase(word_texts)` — the leading content tokens (each word admitted or dropped by `content_tokens` itself), at most 4, None below 2, returning the normalised phrase + the raw source words + the following two raw words; `append_user_cues(pairs, *, config_root, learned_at)` — reads the user file or the shipped default through the loader's own precedence, stores phrases NORMALISED, skips `empty` and `duplicate` (any section, the loader's normalisation), validates the EXACT bytes by both loader rules BEFORE any write, then two atomic replaces (`session_store.atomic_write_bytes`, the cue file first, then the sidecar `section_cues.learned.json` = `LearnedSidecarFile {phrase: {section, learned_at}}`) — the ONE write path into the config directory (`_write_config_file` creates it; the loader stays read-only); `load_learned_phrases` / `delete_user_cue` (Task 5.3); NEW `NoteConfigWriteError`. Decisions recorded: the candidate takes up to FOUR leading tokens (the more specific end of the 2–4 range — less misrouting of other lines); the dose-unit window looks only FORWARD (the phrase is leading, and no filler is a unit); a naive `learned_at` in a hand-edited sidecar is read as UTC. `ui/models.py`: `LearningStatus` + `learning_status(profile_root)` — one `load_profile` WITHOUT the embedder identity (learning needs consent, not the model), the four off-reasons as constants pointing at the Practitioner tab, `LEARNING_ON_LINE`. `ui/note.py`: `_consider_learning(assertion)` after an add or a move — the ownership test FIRST and silently (`spoken_by_confirmed_clinician`; any other speaker's line stops there, whatever the status), then the status read at `begin_review` (hint appended to the status line when off), `propose_learning_phrase` (too short → noted), the filter (`Not learned: contains a name/number/date/medication (<class>).`), an already-a-cue check against the composed config's cues, the PR-MED-011 dry run through `note.first_matching_section` over the proposed cue set (a note when an earlier section still wins — never a deletion), then the QUEUE (`_learning_queue[m<segment>] = (section, phrase)`); undo drops the entry; a move's remove leg queues nothing; `clear()` (cancel / abandon / new transcript) drops the queue; `save()` — after `on_save` succeeded — calls `_write_learned_phrases`: the status re-read (a profile deleted or opted out mid-review → "Nothing learned"), `append_user_cues(..., learned_at=now)`, `NoteConfigError` reported on the status line WITHOUT un-saving the note, the `learned_phrases_changed` signal on a write. `NoteScreen(config_root=, learning_status_provider=)` — a screen built without a provider never reads the profile store and reports learning off (tests stay off the default store, PR-REG-005); `MainWindow(config_root=)` passes both roots. Tests: delegate 1 (the filter fixture, the helpers) + delegate 2 (the screen).
- [x] 🟩 5.3: **Practitioner tab: recently learned + all learned phrases.** A "Recently learned" list (the last 20 learned phrases with section and date, newest first — the date recorded beside each learned phrase in a sidecar `section_cues.learned.json` `{phrase: {section, learned_at}}` written atomically with the cue file, never read by the loader) and the full "Learned phrases" list (phrases in the user `section_cues.json` not present in the shipped default, grouped by section); Delete on either removes the phrase from the cue file (atomic replace; the loader stays the only reader of the cue file) and from the sidecar; the learning opt-in toggle and re-consent live on 5.0's path. Verification: offscreen tests — list contents, delete updates both files, empty states, a sidecar entry with no cue-file phrase is dropped on read.
  - **Re-smoke 2026-09-17 12:44 PASSED on the real config root:** after Save note the Practitioner tab listed both learned phrases under Recently learned (dated 2026-09-17, section Presenting complaint) and under All learned phrases, with the delete hint shown (the practitioner's screenshot 4).
  - **Built 2026-09-16 16:55 (architect, leg stage-5-arch-f1; NOT yet verified).** `note_config.py`: `load_learned_phrases(config_root) -> LearnedPhrases(recent, by_section)` — no user file → nothing learned (the sidecar ignored); "learned" = every user-file phrase whose normalised form the shipped default carries in NO section, grouped by the section the file holds it in now; "recent" = the sidecar entries that still name such a phrase (an orphan is dropped on read), newest first, capped at `RECENTLY_LEARNED_LIMIT = 20`; a malformed cue file or sidecar raises the loader's typed errors; `delete_user_cue(phrase, *, config_root)` — normalised match from whichever section, bytes validated before the two atomic replaces, False with no user file or no match, an emptied section keeps its key with `[]`. `ui/practitioner.py`: the placeholder replaced by `recently_learned_list` + `delete_recent_button` and `learned_phrases_list` + `delete_learned_button` (each item's `UserRole` data is the stored phrase), `learned_phrases_note_label` (PlainText: the empty state `NO_LEARNED_PHRASES_TEXT`, the delete hint, or a loader error), `refresh_learned_phrases()` (construction, after a delete, and on the Note tab's `learned_phrases_changed` signal wired in `MainWindow`), `delete_learned_phrase(phrase)`, `PractitionerScreen(config_root=)`. Tests: delegate 1 (the helpers) + delegate 2 (the tab + wiring).
- [x] 🟩 5.4: **Docs + tests.** Threat-model surface 1 (config as note-content input) rewritten for the auto-learn path: the refusal filter is the enforcing control and the after-the-fact list is the review — naming the semantic limit honestly (the app cannot classify meaning; a benign-looking phrase that IS patient-identifying in context passes the filter — the residue the practitioner's review covers, PR-HIGH-001); the consent-version check as a control; surface 2 (clinician-asserted content) extended to review edits (subtract-only, D14); data-flow-map flow 13 (review → queued phrase → Save → cue file + sidecar); `retention-schedule.md` (learned phrases + sidecar: plain text, until deleted); `design-system.md` (the edit controls, the not-learned status note, the recently-learned list); `AGENTS.md` pointer; CHANGELOG. Verification: suite green; every claim traced to a call site (the docs-accuracy class).
  - **Built 2026-09-16 16:55 (architect, run=stage-5, leg stage-5-arch-f1; NOT yet verified — verify=composer).** `docs/security/threat-model.md`: Phase 3A surface 1 gains the auto-learn paragraph — the five bounding call sites named (`ui/note.py` `_consider_learning` over `note.spoken_by_confirmed_clinician`; `ui.models.learning_status` + `consent_is_current`, read at review start AND at Save; `note_config.propose_learning_phrase`; `refuse_learning_candidate` with its four classes; the Save-only write with loader-validated bytes and atomic per-file replace) and the shape-only honest limit (three named residues: a contextually identifying benign phrase, an unlisted drug name without a unit, a benign word with a listed suffix bar the two anatomical exemptions); surface 2 gains the review-edits paragraph (`whole_utterance_assertion` + Check 1, `admissible_sections` + Check 3, subtract-only, the working draft, Check 4 acknowledgeable, no free text, frozen at Save); the practitioner-profile section is re-headed Phases 1–5, surface 1's consent clause says v2, surface 9 is rewritten for the CURRENT text / stale record / "Confirm consent" and its opt-in residual replaced by the re-consent-needs-a-readable-profile residual; NEW surface 10 (learned phrases at rest: where, what a phrase is, the controls by call site, retention, five residuals incl. the two-file write). `docs/security/data-flow-map.md`: the component row names flow 13; flow 11 says the loader stays read-only and the app writes through flow 13; flow 12 says the current version is `consent-v2`, a v1 record is readable-not-current, "Confirm consent" is one atomic replace, and the Note tab reads the profile too; NEW flow 13 (review edit → queued phrase → Save → cue file + sidecar, every step a call site); the plaintext non-flow names the learned phrase as the one thing the app writes into the config class, structurally never a patient's line, shape-filtered, reviewable — not guaranteed non-clinical. `docs/security/retention-schedule.md`: the profile row (re-consent replaces only `voice.enc`; v1/v2 versions) and the config row (the tab's Delete; the "not yet built" clause removed) updated, NEW learned-phrases row (cue-file entries + sidecar, until deleted, the five gates, `delete_user_cue`, the orphaned-sidecar note). `docs/design-system.md`: the Practitioner-tab sentence, consent cue → `CONSENT_TEXT_V2` + stale notice + Confirm consent, three NEW cues (edits that subtract or re-route, the on-the-spot learned/not-learned status line, the deletable list). `AGENTS.md`: the enrolment pointer updated to surfaces 1–10 / v2, a NEW pointer for review edits and phrase learning. `CHANGELOG.md`: the Phase 5 entry. Every claim above was traced to its call site by the architect before writing (the docs-accuracy class). Tests: the two delegations (recorded in the handoff bullet).
- [ ] 🟨 5.5: **Gate-doc R3 clarification** (`docs/testing/shipping-gate.md` "What is measured" + the R3 line in the plan's Task 9.1 table header, both plans): with Remove available: denominator = the assertions in the note at the scoring point BEFORE any removal but AFTER every proposal decision (transcript assertions + confirmed proposals — the population rubric v1 already scores); numerator = EFFECTIVE removals (still in force at the scoring point — an undone removal and a move's removal leg do not count) + lines still needing deletion at signing; manual additions count in neither; thresholds untouched; four text-free worked examples in the doc (proposal decisions only, remove → undo, a move, and a genuinely proposal-only note — peer round 36 PR-LOW-039) — PR-MED-015; the practitioner initials the clarification on this task. Verification: re-read; no number changed.
  - **Built 2026-09-16 16:55 (architect, leg stage-5-arch-f1).** `docs/testing/shipping-gate.md`: the status paragraph says both clarifications landed on 2026-09-16 and that the practitioner initials the R3 one; "Preparing the transcript set" now states the two-voice mock definition (the practitioner speaks only the clinician part, a second real person the patient part, a third voice for the three-speaker consultation — one person acting both parts cannot be scored for separation or auto-confirm; per Task 6.1's protocol); "Running it" step 4 says the scoring point is after any Remove / Move / Undo the practitioner would make before signing; "What is measured" gains the R3 block: denominator, numerator (effective removals — an undone removal and a move's removal leg do not count; a removal made, undone and remade counts once — plus lines still needing deletion), hand-added lines in neither, and — stated as a clarification, not a change — a MOVED line counts as wrongly routed for R1 exactly as before Move existed; the text-free worked examples (1/8, 0/7, 1/7, and — added at peer round 36 PR-LOW-039 — a genuinely proposal-only 1/3, with example (1) relabelled "proposal decisions only, no edit"); the initialling line `Clarification initialled by the practitioner: ________ (date ________)` left BLANK for the practitioner. `.cursor/plans/plan-phase3a-note-pipeline.md` Task 9.1 scoring-table header carries the same population sentence with a pointer to the page. No threshold or count changed (re-read: 10 / 8–12 / 80 % / 20 % / R4 = 0 / majority all as ratified). **Still the practitioner's: the initials on the page** (the composer collects them at the smoke pause).

- [ ] 🟥 5.6: **Leaving a review with queued phrases says what was lost** `[pending-hardening]` — Include in plan from the Deferral Confirmation Gate at leg f8c (live smoke of Tasks 5.2/5.3, 2026-09-17, verbatim ask: "Learned phrases are not showing up after saving in the practioner profile."; answer 2: "it had the phrases there and then told me to go to another screen and press save"): the queue is written ONLY by Save note (D9 as amended) and every other exit — Cancel review and regenerate, Delete note and complete without one, a new transcript — drops it silently through `clear()`. The Note tab now names Save note on this tab while phrases are queued (leg f8c, one site); what remains is multi-surface: after Cancel / Delete-and-complete the Transcript screen's status line should say "N queued phrases were not learned (only Save note learns)", and the `closeEvent` refusal text while a review is busy should mention queued phrases — no modal (design-system: a status line, never a prompt). Verification: offscreen tests — Cancel and Delete with a non-empty queue produce the line on the Transcript screen; an empty queue produces nothing. Scoped `/review-plan` before build (the marker).
- [ ] 🟥 5.7: **Common clinical openers are not refused as names** `[pending-hardening]` — PRACTITIONER DECISION 2026-09-17 (option (ii), the architect's recommendation, chosen over (i) keep the rule and (iii) drop the opener): `refuse_learning_candidate`'s name heuristic refuses any candidate whose first word is capitalised and outside the transcript's common-starter set, so imperative/clinical instruction lines ("Your home exercise …", "Keep …", "Try …", "Ice …") never teach a phrase. Add a learner-local EXEMPTION list of common clinical/imperative openers (keep, try, avoid, continue, rest, ice, use, start, stop, your, on, for, with, at, in, before, after — the final list is the scoped review's), applied at the REAL first word only: an exemption ADMITS listed forms only, a new form still fails toward refusal, and the refusal filter otherwise stays byte-identical (the enforcing control; tested as a class). Done when: the list is a named constant with a test per admitted opener and a test that an unlisted capitalised opener is still refused; the threat model's surface 10 names the exemption. Not spawnable until a scoped `/review-plan` hardens this task and removes the marker.

### Phase H — Hardening stage
- [ ] 🟥 H1: `/review-loop` (or `/review` → `/fix`) to convergence over Phases 1–5 as one surface
- [ ] 🟥 H2: `/simplify` — log findings; trivial → `/fix`, substantial → scoped `/review-plan`
- [ ] 🟥 H3: `/security-review` — log findings; same impact-tiered routing (custody, consent, the D4 residual, the cue-file input surface)
- [ ] 🟥 H4: final cross-family `/peer-review` (codex) re-check to confirm convergence

### Phase 6 — Measurement and gate resumption (PRACTITIONER-owned)
- [ ] 🟥 6.1: **The single recording set.** Per the protocol in `Agreed Scope`: ~10 mock consultations recorded through the app AND in parallel to labelled 16 kHz WAVs (Audacity role-label tracks; one three-speaker consultation), plus one enrolment WAV; **voices (PR-MED-013):** the practitioner speaks ONLY the clinician part and a second real person (a colleague or family member; mock content, no real patient) speaks the patient part, with a third real voice for the three-speaker consultation — one person acting both parts cannot be scored for speaker separation or auto-confirm, because enrolment recognises a person, not a part; the gate doc's mock definition is clarified accordingly at Task 5.5 (thresholds untouched); practitioner-accepted 2026-09-05; the retention decision for the WAVs settled and recorded in the Phase 3A plan's Task 2.3.
- [ ] 🟥 6.2: **Run the harness** (`scripts/measure-speakers.py --enrolment <wav> <dir>` from a normal terminal); record the three conditions' numbers and the auto-confirm correctness here AND in the Phase 3A plan's Task 2.3; decide D-S1 there; if auto-confirm correctness is below the shipped role accuracy, re-open D4's margin-gated alternative as a scoped `/review-plan`.
- [ ] 🟥 6.3: **Resume the Task 9.1 gate run** in the Phase 3A plan against rubric v1 with the same consultations (the gate scoring happened at recording time in 6.1 — the sheet under Task 9.1 is filled from it); the pause pointer from 0.1 closed.

## Retained Follow-Up Items
(Not applicable while plan is Active.)

## Follow-Up Continuation Notes
(Not applicable while plan is Active — populated at completion.)

---
*Plan saved to: .cursor/plans/plan-practitioner-profile.md*
*To resume in a new session: open a fresh Agent (Ctrl+I), run /start-session, then run /load-plan*
