"""PySide6 status app entry point (`scribe-app`).

Phase 1 scope: registration status + secure-storage self-test (built in
later plan tasks). This stub exists so the entry point installs and the
package imports cleanly from Step 1.
"""

from __future__ import annotations


def main() -> int:
    print("scribe-app: Phase 1 desktop shell (UI arrives in plan Step 8)")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
